# HANDOVER 2026-04-28

**Session:** Pixel-audit + token-isolering

---

## Vad som levererades

### 1. Feature-flags borttagna (föregående session)
`scenesEnabled` och `portalEnabled` raderades från SaveGame, roundProcessor, alla tester. Scener och Portal är alltid på. Commits: `feea364`, plus tidigare.

### 2. Portal dark token-fix (commit `f761545`)
Alla 18 portal-komponenter (primary/secondary/minimal) använde light-theme-tokens på mörk bakgrund. Migrerade till `--bg-portal-*`, `--text-light`, `--text-light-secondary`, `--bg-leather`. Lade till dark defaults i global.css `:root`.

### 3. Portal useEffect cleanup (commit `e9a8024`)
PortalScreen's useEffect returnerar nu `removeProperty` för alla `--bg-portal-*` vid unmount. Portal-tokens hänger inte kvar på DOM:en när SceneScreen tar över.

### 4. Pixel-audit — 4 avvikelser fixade (commit `7173af3`)
- PortalScreen: `padding: '8px 14px'` → `'14px'` (mock krav)
- GameShell: BottomNav göms när `game.pendingScene` är satt (spec krav)
- CoffeeRoomScene: `'#1a1612'` → `var(--bg-scene)` (CSS-token-disciplin)
- SMFinalVictoryScene: `'#08060a'` → `var(--bg-scene-deep)` (CSS-token-disciplin)
- Ny tokens: `--bg-scene` och `--bg-scene-deep` i global.css

**Rapport:** `docs/sprints/SPEC_PORTAL_FAS_1_PIXEL_AUDIT.md`

---

## Implementationsstatus för de fyra specarna

| Spec | Status | Pixel-audit |
|---|---|---|
| SPEC_INLEDNING_FAS_1 | ✅ Komplett | ✅ Kod-verifierad |
| SPEC_SCENES_FAS_1 | ✅ Komplett | ✅ Kod-verifierad |
| SPEC_PORTAL_FAS_1 | ✅ Komplett | ✅ Kod-verifierad |
| SPEC_KAFFERUMMET_FAS_1 | ✅ Komplett | ✅ Kod-verifierad |

---

## Token-isolering: Portal vs Scenes

Verifierat i session:
- Scene-komponenter läser aldrig `--bg-portal-*`
- Portal-komponenter läser `--bg-portal-*` + delade dark-helpers
- Delade tokens (`--text-light`, `--bg-leather`) är statiska i `:root` — inte portal-specifika
- `--bg-scene` och `--bg-scene-deep` är nya statiska scene-tokens
- Cleanup säkerställer att portal-vars försvinner vid unmount

---

## Kvarstående

- **Citat-pooler** (SPEC_INLEDNING_FAS_1): 3 citat/klubb finns i `clubOfferQuotes.ts`, spec vill ha 3-5/klubb. Opus-jobb, inte Code-jobb.
- **Visuella skärmdumpar**: Audit gjordes som kod-verifierad CSS-jämförelse. CLAUDE.md kräver egentligen skärmdumpar i SPRINT_AUDIT.md för visuella ändringar. Behöver playtest för fullständig visuell verifiering.
- **Sprint 28-C**: Opus-only skärmdump-audit (Sprint 28 fas C) är fortfarande ej gjord.
- **Playtest-runda 4**: Ingen av Sprint 25f/g/h, 26, 27, 28, SPEC_INLEDNING/SCENES/PORTAL/KAFFERUMMET är verifierad i live-spel.

---

## Nästa session

1. Playtest-runda 4 (prio 1) — klicka igenom hela flödet: intro → klubbval → spel → scener → portal
2. Sprint 28-C (Opus-only, ~1h)
3. Citat-pooler om Jacob vill skriva dem (Opus-ledd text-session)
4. Motor-gap nästa om inga playtest-blockers

---

## Teknisk hälsa

- Build: ✅ 2.0s clean
- Tester: ✅ 2540/2540
- Hårdkodade hex: ConfettiParticles `CONFETTI_COLORS` (undantag OK, animationsdata)
- Filstorlekar: Alla under 150 rader
