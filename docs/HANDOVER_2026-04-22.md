# HANDOVER — Bandy Manager

**Senast uppdaterad:** 2026-04-22 kväll

## Vad som levererades den här sessionen

### 1. HalftimeModal — CTA döljs av lång Byten-flik
**Symptom:** "Andra halvlek →"-knappen klipptes under viewport på Byten-fliken.
**Rotorsak:** Hela modalen låg i ett enda scroll-flöde. Lång content tryckte ned CTA.
**Fix:** Tre-zon-layout: `flexShrink: 0`-header (poäng + flikar) → `flex: 1, overflowY: auto`-scrollbar → `flexShrink: 0`-footer med CTA. Modal-kortet fick `display: flex, flexDirection: column, maxHeight: calc(100dvh - 80px), overflow: hidden`.
Fil: `HalftimeModal.tsx`

### 2. GranskaScreen Spelare-flik scrollar inte
**Symptom:** Spelarbetygs-listan gick inte att scrolla; allt utom korta flikar klipptes av.
**Rotorsak:** `GameGuard` hade `overflowY: 'auto'` på sin Outlet-wrapper. `GranskaScreen` använder `height: 100%` på sin yttre div — men i ett `overflowY: auto`-parent löser `height: 100%` till contenthöjden (inte containerhöjden). Intern scroll triggade aldrig.
**Fix:** `overflowY: 'auto'` → `overflow: 'hidden'` i `GameShell.tsx` `GameGuard`.
**Ny lärdom i LESSONS.md lärdom 2:** Wrapper som ska fungera som klipgräns för inre scroll = `overflow: hidden`, inte `overflowY: auto`.
Fil: `GameShell.tsx`

### 3. Dashboard — för stort gap mellan CTA och BottomNav
**Symptom:** Onödigt tomt utrymme under "Spela omgång →"-knappen, ovan navbaren.
**Rotorsak:** `card-stack`-wrappern hade `paddingBottom: var(--scroll-padding-bottom)` = 120px, på toppen av GameShells egna nav-clearance på 60px. Totalt ~180px tomrum.
**Fix:** `paddingBottom: var(--scroll-padding-bottom)` → `paddingBottom: 8px` på DashboardScreens card-stack-div. Reducerade även build-textens `marginTop` från 16px → 6px.
Fil: `DashboardScreen.tsx`

### 4. DiamondDivider tog för mycket plats
**Rotorsak:** `marginBottom: 14` på DiamondDivider bidrog till det visuella glappet.
**Fix:** `marginBottom: 6`.
Fil: `DiamondDivider.tsx`

### 5. NewGameScreen — välj klubb-layouten
**Ändringar:**
- Rubriken "Välj klubb" ersatt med "Varje klubb har en historia, välj din:" i display-font (weight 400)
- Tagit bort duplikat-paragraf och "Starta karriären som..."-text
- CTA tightad mot sista klubbkortet (`padding: '8px 12px'`)
- Scroll-padding justerad

### 6. Kapten-händelse visas trots att kapten redan valts
**Symptom:** Efter att spelaren valt kapten i PreSeasonScreen kom ändå ett händelsekort "Utse kapten?" efter omgång 2.
**Rotorsak:** `characterPlayerService.ts` genererade captain-eventet vid `ledare`-trait + round 2, utan att kolla `game.captainPlayerId`.
**Fix:** Lade till `&& !captainPlayerId` i villkoret + skickar `game.captainPlayerId` från `communityEvents.ts`.
Filer: `characterPlayerService.ts`, `communityEvents.ts`

### 7. Shotmap — motståndarmålet för långt ned, saknar straffbåge
**Ändringar:**
- `GOAL_Y` sänkt 20 → 8 så toppmålet (MOTSTÅNDARMÅL) ligger lika nära överkanten som VÅRT MÅL i botten
- Lade till `<path>` med arc för straffbåge på båda mål (bulgar bort från respektive mål)
- Justerade label-y-koordinater för symmetri
Fil: `GranskaScreen.tsx`

### 8. Textfix — "bryter igenom" → "slår igenom"
`CounterInteraction.tsx`, `matchCommentary.ts`, `youthProcessor.ts` — alla förekomster ändrade.

### 9. Textfix — "brittningen" → "brottningen", "Dominerar sitt område" → "Dominerar sin yta"
Fil: `matchCommentary.ts`

### 10. Rensning — död MatchEventType-kod
**Rotorsak:** YellowCard, Shot, Injury, Suspension, FullTime finns som enum-värden men emitteras aldrig av matchCore. Ärvd fotbollskod som levt kvar och förvirrade kodläsaren.
**Fix:** Tog bort filterings- och renderingsgrenar för dessa typer ur 10 filer. Kommentarer `// never emitted` kvar i enum för förklaring.
**Bugfix inbakad:** `MatchDoneOverlay.tsx` räknade utvisningar via `MatchEventType.Suspension` (aldrig emitterat) istf `MatchEventType.RedCard` — utvisningsräknaren visade alltid 0.
Commit: `6580799`

---

## Aktiva sprintdokument

- **SPRINT_25B_1_PENALTY_SEPARATION.md** — väntar på mätning efter merge, sprint 25b.1 ej startad
- **Sprint 25b.2** — väntar på 25b.1-mätning

## Kvarstående efter den här sessionen

- Firebase Allsvenskan-skrapning (typer 11/23/10/107) — specificerat i KVAR.md, ska göras på fredag
- Sprint 25b.1/25b.2 (straff/utvisningskalibrering) — inte rört i sessionen

## Nyckelbeslut

- `GameGuard` ska använda `overflow: hidden` (inte `overflowY: auto`) för att låta nested screens hantera egna scroll-axlar — dokumenterat i LESSONS.md
