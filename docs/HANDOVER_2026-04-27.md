# HANDOVER — 27 april 2026

## Vad som levererades idag

### Sprint 28-A — Pension-impact på morale + kapten-vakuum (KOMPLETT)

Commit `701044a` — pushed.

**Ändringar i `seasonEndProcessor.ts`:**
- `let nextCaptainPlayerId` deklareras vid sessionstart, sätts till `undefined` om aktiv kapten pensioneras
- For-loop över `retiredPlayerIds`: för varje pensionerad kapten eller långvarig spelare (≥3 säsonger), beräknas gemensamma säsonger med kvarvarande lagkamrater via `seasonHistory`-intersection
- Morale-hit: kapten `min(15, 5 + sharedSeasons*2)`, legend `min(10, 3 + sharedSeasons)` — bara för lagkamrater med ≥2 gemensamma säsonger
- Inbox-narrativ i tre varianter: kapten + många kvar (affectedCount ≥ 3), kapten + få kvar, legend utan kaptenskap
- `captainPlayerId: nextCaptainPlayerId` propageras till `updatedGame`

Build ✅ · Tests: 1895/1895 ✅

---

### Sprint 28-B — Legend match-commentary + aktiv legend-flagga (KOMPLETT)

Commit `abee31c` — pushed.

**`playerDevelopmentService.ts`:**
- Ny funktion `updateActiveLegendFlags(players, managedClubId)` — markerar aktiva managed-club spelare med `isClubLegend: true` om ≥5 säsonger + ≥100 matcher. Körs varje säsongslut i `seasonEndProcessor.ts`. AI-klubbars legends sätts fortsatt av `worldGenerator.ts`.

**`matchCommentary.ts`:**
- Fyra nya pooler: `legend_goal` (12 strängar), `legend_assist` (4), `legend_gk_save` (3), `legend_late` (3). Variabler: `{lastName}`, `{seasons}`, `{totalGoals}`, `{minute}`.

**`matchCore.ts`:**
- `pickLegendCommentary()` helper — väljer pool baserat på event-typ, fyller template
- `assisterPlayerId` spåras nu per steg (var saknad tidigare) — sätts i alla tre goal-scoring-block (attack, transition, corner)
- Legend-check i `goalScored && scorerPlayerId`-grenen: scorer-legend → 70% override med `legend_goal` eller `legend_late` (minute ≥ 80, margin ≤ 1). Assister-legend → 70% med `legend_assist`
- Legend GK-check i `saveOccurred && gkPlayerId`-grenen → 70% med `legend_gk_save`
- Bara managed club (`scorerIsManaged` / `gkIsManaged`)

Build ✅ · Tests: 1895/1895 ✅

---

### Taktisk audit — read-only rapport

Genomgång av hur taktiska val representeras i matchmotorn. Ingen kod ändrades.

**Slutsatser:**
- Starkaste mekaniska effekter: Tempo (sekvensfördelning), Press (transitions/fouls), situation-AI (trailing/controlling)
- Svagaste: Formation (fit-faktorer, <2% effekt), mentalitet (direkt multiplikator)
- Fem variationer motorn inte kan särskilja: libero vs man-to-man, stor man i boxen, corner stack-effekt, spelbredd (kantar), press-trötthet kumulativt

---

## Sprint 27 — retrospektiv (levererad förra sessionen)

Commit `be33b3b` (fas D+E) + `a814314` (text-pass).

**Fas D — Pension/Legend-roller:**
- `ClubLegend` fick `playerId?` + `role?: 'youth_coach' | 'scout' | 'farewell'`
- `setLegendRole` EventEffect → `eventResolver.ts` hanterar roll + mekaniska effekter (youthQuality +5, scoutBudget +3)
- Legend-lön: 500 kr/vecka per aktiv roll, beräknas i `economyProcessor.ts`, visas i `EkonomiTab.tsx` + `DashboardScreen.tsx`
- Kafferum-pooler: `youthCoachPool` (6 kurerade), `scoutPool` (6 kurerade), `generalPool` (5)

**Fas E — Karriärs-tidslinje:**
- `CareerJourney.tsx` — vertikal tidslinje, grupperad per säsong (nyast först), max 4 entries + overflow-räknare
- Integrerad i `PlayerCard.tsx` som sektion ⑦

---

## Aktiva jobb just nu

**Sprint 28-C — Skärmdump-vänlighet-audit (ÅTERSTÅR)**
- Opus-jobb, ingen ny kod
- Output: `docs/SCREENSHOT_AUDIT_2026-04-26.md`
- Vyer att granska: 10 st (MatchLiveScreen, GranskaScreen, SeasonSummaryScreen, PreSeasonScreen, PlayerCard/CareerJourney, pension-ceremoni, TabellScreen, kafferum-quote, inbox-rad, klubb-sidan)

**Sprint 28 AUDIT.md (ÅTERSTÅR)**
- Skapas när fas C är klar (inkluderar A+B+C)

---

## Nyckelbeslut fattade idag

- **Active legend generation:** `isClubLegend` för managed-club-spelare sätts dynamiskt varje säsongslut via `updateActiveLegendFlags()` — inte statiskt som för AI-klubbar. Rätt arkitektur: AI-klubbar seedad en gång, managed club uppdateras kontinuerligt.
- **Legend commentary priority:** Legend-check sitter FÖRE final/semifinal/derby/storyline/contextual-kedjan. 70% (inte 100%) för att behålla variation.
- **assisterPlayerId** lades till som per-steg variabel i matchCore.ts — korrekt plats, parallell struktur med `scorerPlayerId`.

---

## Kvarstående frågor

1. Sprint 28-C och SPRINT_28_AUDIT.md — återstår
2. Motor-gap kvar: `awayWinPct` 43.9% vs 38.3%, `cornerGoalPct` 26.2% vs 22.2%, `playoff_final` mål/match 9.17 vs 7.00
3. `pickSeasonHighlight()` dead code i `seasonSummaryService.ts` — fortfarande kvar (5 min städjobb)

---

## Föreslagen ordning nästa session

1. Läs `CLAUDE.md`, `LESSONS.md`, `DECISIONS.md`, `KVAR.md`, denna fil
2. Sprint 28-C — skärmdump-audit (Opus-only, ~1h)
3. Skapa `SPRINT_28_AUDIT.md` och stäng Sprint 28
4. Välj nästa sprint: motor-gap (`cornerGoalPct` / `awayWinPct`) eller playtest-runda 4 (skandaler/kafferum/klack/press i live-spel)
