# HANDOVER — 26 april 2026

## Vad som levererades idag

### Verifiering: Sprint 25L + Sprint 25E (bekräftade som redan levererade)

Inledde sessionen med att verifiera att Sprint 25L (kalibrerings­finish KVF/SF goalMod) och Sprint 25E (powerplay-effektivitet) var committade — båda bekräftade via git log:
- Sprint 25L: commit `a6e24b4`
- Sprint 25E: commit `4362ebf feat: powerplay-effektivitet — riktig comeback-mekanik (Sprint 25-E)`

---

### Sprint 26 — Cross-system skandalreferenser (KOMPLETT)

Åtta filer ändrade. Commit `11802e1` — pushed.

**Del 1 — Dashboard-kafferum:**
- `coffeeRoomService.ts`: `SCANDAL_DASHBOARD_OWN` (21 utbyten, 7 skandaltyper × 3 format) + `SCANDAL_DASHBOARD_OTHER` (21 utbyten, {KLUBB}-substitution)
- Triggerlogik: `seed % 4 === 0` (25% chans) vid aktiv skandal innevarande säsong, `triggerRound >= round - 1`
- `small_absurdity` exkluderas från alla scandal-triggers

**Del 2 — Klack-commentary:**
- `matchCommentary.ts`: `supporter_scandal_recent` (8 strängar med `{leader}`/`{members}`)
- `matchUtils.ts`: `ownScandalThisSeason?: boolean` i `StepByStepInput`
- `matchCore.ts`: 20% trigger vid step 0 (kickoff) och step 30 (halvtid)
- `MatchLiveScreen.tsx`: beräknar och skickar `ownScandalThisSeason`

**Del 3 — Pressfrågor:**
- `pressConferenceService.ts`: `minScandalThisSeason?: boolean` på `PressQuestion`, 7 nya frågor spridda på bigWin/win/loss/bigLoss/draw-poolerna, filterlogik vid rad 548

**Del 4 — Motståndartränare:**
- `opponentManagerService.ts`: `SCANDAL_AFFECTED_LOST` (4), `SCANDAL_AFFECTED_WON` (3), `SCANDAL_AFFECTED_GENERIC` (1), `hasScandal?` parameter
- `GranskaScreen.tsx`: `opponentScandal` beräknas och skickas som tredje argument till `generatePostMatchOpponentQuote`

---

### Sprint 26 Audit — kod-verifierad simulation (KOMPLETT)

Commit `6bbf8ca` — pushed.

- Audit-script kördes från projektroten mot faktisk produktionskod
- Del 1: alla 14 utbyten (7 OWN + 7 OTHER) verifierade med faktisk output och seed 108
- Del 2: alla 8 supporter_scandal_recent-strängar listade, {leader}-substitution verifierad, distribution 6/8 unika index på 30 seeds
- Del 3: filter­logiken testades isolerat (full `generatePressConference` krashade pga minimal SaveGame-mock utan `players`-array — dokumenterat i audit)
- Del 4: alla 8 scandal-quotes verifierade via 200-seed × 4-persona-sweep
- Edge cases: `small_absurdity` triggar inte, tom `scandalHistory` inga undefined-fel, gammal säsong filtreras, `{KLUBB}` aldrig exponerat

**Build + tester:** ✅ `1895/1895 (165/165 filer)`

---

## Nyckelkommandon
```bash
npm run build
npm test
npm run stress -- --seeds=3 --seasons=2
```

## Commit-historik idag (senaste först)
```
6bbf8ca docs: Sprint 26 audit — kod-verifierad simulation
11802e1 feat: cross-system skandalreferenser — coffee-room, klack, press, motståndarcoach
```

Sprint 25E (föregående session):
```
4362ebf feat: powerplay-effektivitet — riktig comeback-mekanik (Sprint 25-E)
```

---

## Tekniska noter

**`ownScandalThisSeason` i StepByStepInput:** Lagen måste flytta information om aktiv skandal in i matchflödet eftersom `matchCore.ts` inte har tillgång till `game`-objektet. Beräknas i `MatchLiveScreen.tsx` och skickas in via `StepByStepInput`. Mönstret speglar hur `isDerby`, `rivalInfo` m.fl. redan hanteras.

**3-parts dialog i 4-tupel:** Flerspelar-dialoger encodas i `ex[3]` med `" SPEAKER: "` inline-syntax för att utnyttja befintlig template `"${ex[1]}" — ${ex[2]}: "${ex[3]}"` utan att ändra datastrukturen.

**pressConferenceService crash:** `game.players.some(...)` vid rad 630 kraschar med minimal SaveGame-mock. Filter­logiken testades isolerat istf helkörning — dokumenterat som "ej end-to-end verifierat" i audit.

---

## Kvarstående inför nästa session

1. Läs `CLAUDE.md`, `LESSONS.md`, `DECISIONS.md`, `KVAR.md`, denna fil.
2. **Motor-kalibrering kvarstår:**
   - `awayWinPct`: motor 43.9% vs target 38.3% (−5.6pp)
   - `playoff_final mål/match`: motor 9.17 vs target 7.00 (+2.17)
   - `cornerGoalPct`: motor 26.2% vs target 22.2% — eskalerar i slutspel
3. **Playtest Sprint 25h + 26:** skandaler, riskySponsorContract, WageOverrunWarning, coffee-room scandal-quotes — ingen av dessa är verifierade i live-spel. Playtest-runda 4 är nästa speltest.
4. **Kandidater nästa sprint:**
   - Sprint 25e v2 (awayWinPct/cornerGoalPct — restmotorika)
   - Sprint 25d (fas-konstanter KVF/SF/Final mot slutspelsdata)
   - Fredagsjobb: utvidga `scrape-allsvenskan.mjs` med skott/räddningar + `loggingQuality`
