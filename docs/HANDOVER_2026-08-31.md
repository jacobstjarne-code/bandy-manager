# HANDOVER 2026-08-31

Ingen handover sedan 2026-06-26 — det här täcker hela den sammanhängande sessionen sedan dess, tyngdpunkten på de senaste dagarnas audit-svep (2026-08-29 till 2026-08-31). Fyra spår: framgångskurvans ekonomi, förtroendeblockerarna (A1–A4/A-H3), audit-svansen (HIGH 5–11), och en löpande verifieringsdisciplin som hittade fem reella buggar i redan "klar" kod innan commit.

---

## Spår 1 — Framgångskurvan (anspråk 1–4) + förtroendeblockerarna

**Levererat (pushat), i ordning:**
- `f5c3c8c6` A-H2b villkor 2 — klubbframgångsgrind för retentionsmekaniken.
- `ab7eb08d` A-H2b dörr (c) — tröskel för förbättrad placering, ren tabellbrus stängdes ute (D035).
- `af760bec` Kontrollklubbens ryktekollaps-spiral — WEEKLY_BASE_FLAT 3000→8000 (D036, senare omvärderad, se nedan).
- `69259aad` HistoryScreen — karriär över två klubbar renderades mot fel klubb.
- `e765efd5` A-M8/O13 — tränarmarknaden: avsked blir ett kapitel istället för spelets slut.
- `a3860d4d` Audit A1+A2 — falska SM-guld till cupmästare (tre separata call sites delade samma `isNeutralVenue`-bugg), cupscenen låste Hem permanent (`cup_final_victory` skrevs aldrig till `shownScenes`).
- `71f7ab2d` Audit A4 — gemensam sanningssnapshot (`SeasonBoardTruth`), årsbok och Game Over kan inte längre säga emot varandra.
- `765fdcb7` Audit A3/CRITICAL 3 — konditionsspiralen: `fitnessRecoveryService.ts` omskriven, autofyll tystnar aldrig under fitness-golvet. **Visade sig senare (se nedan) vara den EGENTLIGA rotorsaken till att D033/D036:s siffror slutade reproducera — inte något som fixades efteråt.**
- `1447c514` Audit HIGH 7 — presskonferensens fritextsvar bytte kontext utan gate; `templateEligibilityService.ts` + `TAG_ELIGIBILITY`.
- `96deea39` Anspråk 4 — communityStanding dyrare att hålla när klubben växt (`csUpkeepFactor`/`csExpectationDrag`, D037). Framgångskurvans fjärde och sista anspråk.
- `368d135b` Audit HIGH 9 — skadad-spela-vidare-kortet re-validerar sin precondition varje omgång istället för att lita på en stale flagga.
- `f0dbed97` Jacobs egna textändringar (license-notis, Game Over-tvåsanningen) — verifierade och committade separat.

**Väg B (basekonomins intäkt) byggdes parallellt** (utanför denna fils commit-lista, redan dokumenterat i `docs/DOM_AH2_BASEKONOMI_INTAKT_2026-08-28.md`): kontinuerlig positionsbonus + vidgad formBonus i `economyService.ts`, D033.

**Det stora fyndet — 2026-08-30 ommätning (`0c438cd4`):**
Två mätförgiftande buggar fixades FÖRST, per Jacobs prioriterade order:
- `3914a5e6` socialMedia-ryktesticken (+1 var 5:e omgång, frikopplad från resultat — ett mittenlag nådde rykte 100 på tre säsonger) grindad på faktisk topp-3-placering.
- `06b86b29` mätharnessen (`autoResolvePendingScreen`) svarade aldrig på `game.pendingEvents` — patronen var aktiv 0/20 säsonger i alla tidigare mätningar trots CS 92. Ny `autoResolvePendingEvents`.

Sedan ommättes väg B och anspråk 4 mot rätt rykte + aktiv patron (`0c438cd4`). **Huvudfyndet:** D033/D036:s tal hade slutat reproducera — men INTE av de två nyss fixade buggarna. Bisection mot låsta originalskript över hela commit-kedjan visade noll kronors rörelse i 12 av 13 commits; all rörelse låg i `765fdcb7` (A-H3:s konditionsspiral-fix, redan ovan). D036:s "3/11 game over"-motivering för WEEKLY_BASE_FLAT=8000 reproducerar inte längre (0/11 game-overs även med flat=3000 kontrafaktiskt) — spiralen fixen skrevs mot var redan löst.

Två ytterligare fynd, båda rapporterade och behandlade:
- **Patron-hypotesen refuterad.** Jacobs egen order antog "patronen vidgar kriterium-1-gapet". Mätningen visade gapet smalnar marginellt (298 805 → 291 252 kr/säsong) — patronens kassabidrag är två storleksordningar för litet för att straffa coasting-sidan asymmetriskt. Jacob bekräftade rättningen rakt av.
- `0901b89e` **transferBidReceived-soft-locken** — hittad UNDER ommätningen, i redan levererad kod (`06b86b29`). Fallback-policyn (`choices[0]` när inget `noOp` finns) accepterade tyst varje inkommande bud (transferBidReceived saknar `noOp`), dränerade en trupp under 11 spelare, `autoSelectLineup` kunde inte bygga en laguppställning → säsongen fastnade permanent. Fixat: explicit `rejectTransfer`-policy + `autoSelectLineup` rapporterar synligt istället för att tyst baila.

**Öppet, medvetet inte byggt:**
- Anspråk 4:s tredje spak (verklig driftskostnad/ort-tiers) — väntade på Jacobs mandatutvidgning. Domen kom (`docs/DOM_ANSPAK4_TREDJE_SPAK_NYHET_2026-08-29.md`): nyhetstretmill för aktiviteter, bägge sidor ska svida. **Byggs som ETT konsoliderat pass** tillsammans med WEEKLY_BASE_FLAT:s omhärledning och kriterium-1-mätningen — Jacob var uttrycklig om att INTE punktfixa detta ovanpå de två redan avslöjade rörliga golven (rykte, patron). Obeblockerat, väntar på att tas upp.
- `docs/incoming/RAPPORT_OMMATNING_VAGB_ANSPRAK4_TRE_FYND_2026-08-30.md` (`9c6c577d`) samlar hela ovanstående — markerad historisk, alla tre fynd behandlade.

---

## Spår 2 — HIGH 10: Burnout som båge

`docs/DOM_HIGH10_BURNOUT_BAGE_2026-08-29.md`.

**Levererat (`7cde05f1`):** decay-gaten (`if (delta === 0) delta -= DECAY`) var strukturellt död — mätt till blockerad i 100,0 % av alla omgångar. Ombyggd: press (förlust+fatigue+inkorg) och återhämtning (seger+decay) räknas separat, decay dras ovillkorligt. `BURNOUT_NATURAL_DECAY` 3 → 14 (mätt sweep, D026). Nya beats: `shouldShowBurnoutRelief`/`shouldShowBurnoutClose`, orsakshärledning (`deriveBurnoutCause`), `lastShownBurnoutZone`-spårning så samma beat inte återpresenteras.

**Byggagenten hittade själv en andra bugg** utanför sin egen spec, flaggad istället för tyst fixad-och-utökad: `computeBurnoutPress` filtrerade fixtures på `homeScore !== undefined`, men `scheduleGenerator` sätter `homeScore`/`awayScore` till 0 redan vid SKAPANDET — burnout drevs alltså aldrig av faktiska matchresultat, bara fatigue/inkorg. Fixat med `status === FixtureStatus.Completed`.

**Jag hittade en TREDJE bugg innan commit**, allvarligare än de två ovan: render-lagret (portalkorten) läste samma `shouldShowBurnoutMark`-predikat en ANDRA gång, mot samma profil-tillstånd roundProcessor precis stämplat `lastShownBurnoutZone` i. En omkörning mot ett redan stämplat tillstånd jämför zonen mot sig själv → alltid nej. Konsekvens: alla tre burnout-korten hade blivit permanent osynliga, RETROAKTIVT även det redan skeppade eskaleringskortet (sedan C-MK1) — inget test fångade det eftersom testerna prövar predikaten isolerat, aldrig de två lagren mot varandra. Fix: en tredje läsväg i `narrativeBeatLog` (`wasLoggedThisRound`) — render-lagret läser loggen istället för att återköra beslutspredikatet. Verifierat med ett fristående reproduktionsskript mot produktionskoden, inte bara läsning.

Text (`BURNOUT_CAUSE_LINES`/`BURNOUT_RELIEF_LINES`/`BURNOUT_CLOSE_LINES`) lämnad tom per CLAUDE.md — mekaniken är fullt wirad, korten tänds automatiskt när Opus fyller poolerna.

**Öppet, inte löst:** zon-pendling kring 70-tröskeln kan ge mark→relief→mark på ett par omgångar (formellt en äkta zonändring, kan läsas som återpresentation). Hysteres vore fixen, en designfråga utanför denna dom.

---

## Spår 3 — HIGH 11: Dashboardens beslut i tre nivåer

`docs/DOM_HIGH11_DASHBOARD_NIVAER_2026-08-29.md`.

**Levererat (`d4f1d8d5`):** tre nya axlar — tier (must/month/background, stängd måste-lista per Jacobs dom: kontraktsdeadline + licenskrav), mode (notis/dilemma/brytpunkt, oberoende av tier), och en resolve-or-expire-rollover som ersätter den tysta engros-nollställningen av `deferredDecisions`.

**Kritiskt fynd under bygget:** `tryQueueDecision` har NOLL produktionsanropsställen — den faktiska throttlen var ett separat inline-block i `roundProcessor.ts` ("KF3"). Måste-undantaget hade varit en no-op om det bara byggts mot `tryQueueDecision`. Extraherat till `partitionInterruptBudget()`, undantaget tillagt DÄR.

**Jag hittade en bugg innan commit:** `seasonEndProcessor.ts`:s nya inbox-cap använde `.slice(-75)` på en OSORTERAD array, mot `roundProcessor.ts`:s etablerade konvention (sortera nyast-först, `slice(0, MAX_INBOX=50)`). Ofarligt idag (kombinerad längd långt under 75) men skulle tappat de NYASTE posterna istället för de äldsta om gränsen någonsin nåtts. Fixat till samma konvention.

Rollover kör de riktiga effekterna genom `resolveEvent` (samma motor som spelet), med en uttrycklig `rejectBid`-policy för `transferBidReceived` — samma fälla som `06b86b29` gick i, undveks explicit här. `seasonRolloverStaleEvents.test.ts`:s no-leak-garanti stod kvar, förstärkt med en icke-vakuum-grind.

**Följdfix samma dag (`f333e61e` + `4baba5e4`):**
- Du levererade texten till `mustDeadlineWarningText.ts`/`deferredRolloverText.ts` — wirad, ett test som asserterade den avsiktliga tomma luckan uppdaterat till att verifiera interpolationen istället.
- **Bakgrundsnivån undantagen throttlen** (ditt uttryckliga handoff): mätt till att nå 3 samtidiga (hela taket) och kunna svälta ut månadskön inom en säsong, trots att bakgrunden aldrig syns. Samma mönster som måste, fast åt andra hållet — i både `getThrottledActiveDecisionCount`/`canAddDecision` och den faktiska produktionsmekanismen `partitionInterruptBudget`.
- **communityEvent fick en resolverbar yta.** Separat rotorsak: den matchade ingen av Granskas tre kategorier och föll till `'inbox-only'` — riktiga val med riktiga effekter (t.ex. veteran-avsked, +3 samhällsstöd) blev olösbara, observerat kvarliggande 62 raka omgångar. Tillagd i `CRITICAL_GRANSKA_TYPES`. Tier stannar `'background'` — ingen dashboard-läcka, bara Granska-routningen ändrad.

**Öppet, flaggat i doktrinfilen, inte löst:** om ett obesvarat måste-kort ska få monopolisera primärplatsen fram till sin frist (troligen avsett men ett medvetet ja, inte implicit).

---

## Spår 4 — HIGH 8: Språkläckor

**Levererat:**
- `d036e51f` Akademins genombrottsreplik (`academyBreakthroughText.ts`, din text) — poolad per spelare istället för en hårdkodad rad återanvänd för alla.
- `b617b603` `enumLabels.ts` (din text, taktikaxlarna) wirad in i `FormationView.tsx` (som hade EGNA, ofullständiga duplicerade label-maps med råläckande fallback) och `GranskaSpelare.tsx` (rådant `{p.position}`). Två raka "CA"-läckor fixade till den redan etablerade termen "Styrka" (`AkademiTab.tsx`, `CallupModal.tsx`).

**LÖST (2026-08-31, Opus):** positionskonflikten är avgjord — `format.ts` är enda källan, och `enumLabels.ts`:s positionssektion (POSITION_LABELS/POSITION_LABELS_SHORT + `positionLabel`) är BORTTAGEN. `format.ts` har redan alla tre former (kort MV/B/YH/MF/A kanon, lång, bestämd) och mina icke-kanon kortformer skulle bara ha återskapat de tre strata filen finns för att döda. `enumLabels.ts` är nu taktik-only. **Kvar, en liten terminologi-nit (Jacobs kall, ingen brådska):** `format.ts`:s långformer skiljer sig marginellt från orden Jacob gav 2026-08-29 — Half "Ytterhalv" (Jacob sa "Yttermittfältare"), Midfielder "Mittfältare" (Jacob sa "Central mittfältare"); Forward "Anfallare"/"forwarden" är redan inom Jacobs okej-spann. Opus rek: lämna `format.ts` som den är (kortare, kanon, markerar redan kant vs central). Vill Jacob ha sina exakta ord byts de i `format.ts`, det enda stället — aldrig i en ny dubblett.

**Besvarat, inget nytt behövs:** leader/veteran har redan en komplett `TRAIT_META`-tabell (`playerTraits.ts`) — traits lagras som svenska strängar i domänen, inte engelska enum-värden, så inget rått läckage finns där.

---

## Genomgående mönster värt att notera

Fem reella buggar hittades i "byggd och testad" kod under den här sessionen, alla av samma familj — grönt i isolerade tester, fel när två lager (skriv-beslut och läs-beslut, eller mätharness och produktionskod) prövas MOT VARANDRA:
1. `06b86b29`/`0901b89e` — mätharnessens fallback-policy accepterade tyst fel val.
2. `765fdcb7` felattribuerades först till de två ryktesfixarna innan bisection visade den sanna rotorsaken.
3. HIGH 10:s render-lager läste samma predikat två gånger mot ett tillstånd som redan muterats mellan de två läsningarna.
4. HIGH 11:s inbox-slice tog fel ände av en osorterad array.
5. HIGH 11:s `tryQueueDecision` var dekorativ kod ingen i produktion faktiskt anropade.

Ingen av de fem hittades av `npx vitest run` ensamt — alla krävde antingen ett fristående reproduktionsskript mot produktionskoden, en bisection över commit-historiken, eller att man faktiskt spårade vilken funktion som anropas VAR (VERIFIERINGSPROTOKOLL, inte "grönt test = klart").

---

## Föreslagen ordning nästa session

1. ~~`enumLabels.ts`:s positionskonflikt mot `format.ts`~~ — **LÖST (Opus 2026-08-31):** dubbletten borttagen, `format.ts` enda källan. Kvar bara en frivillig terminologi-nit (Half/Midfielder-långformer), Opus rek = lämna som är.
2. ~~ANSPÅK 4 VÄG C~~ — **STÄNGD (`6b0a8f3e`, Opus dömde ACCEPTERAT 2026-08-31).** Konsekvensen flyttad från CS till publik (`freshnessFactor`), mätning near-equilibrium −45 tkr/säsong för dominant (från +288 tkr fel håll). Opus-dom: near-equilibrium ÄR "bägge sidor svider" — genuin avvägning, ingendera dominerar; att pressa kostnaden lägre skulle trivialisera den, holdbarhet kapar andra sidan. **FRAMGÅNGSKURVANS BÅGE STÄNGD** — alla fyra anspåk klara. (Öppen BACKLOG-rad: pressa `ACTIVITY_RENEWAL_BASE_COST` lägre — Opus rek: nej, låt stå.)
3. **HIGH 5 LÖST (Code, oincheckat):** `roundLabel.ts` (nytt, ej `format.ts` — drar in cup/playoff-service), sex+en playoff-dubbletter konsoliderade, roundNumber härledd (`nextPlayoffStart`, kan aldrig hårdkodas isär igen), LedgerFrame-masthead tar nu label ej rå siffra (= "OMG 8"-buggens källa), seasonSummary rad 148 keyMoment-"Omgång 8"-derbyt fixat. **Öppet residual (lågprio, eget litet pass — rör inte under ekonomipasset):** `splitOldInboxItems` blandar skalor (`createdRound` league 1–22 mot `currentMatchday` global) → en läst post kullas för tidigt. Är en bugg, inte bara ett cull-fönster-produktval — men fixen slår mot 3 M7-tester som spikar skal-blandningen; normalisera till matchday-skala, uppdatera testerna. `InboxScreen`-fallthrough redan fixad (ingen gissad label). Nice-to-have: `seasonEndProcessor`/`transferService`/`eventResolver` stämplar `createdRound` så deras poster återfår rond-chippet.
4. HIGH 6 (instrumentera lösta DecisionCards som årsbokskandidater) — enda punkten från ursprungsordern som aldrig påbörjats.
5. Polishbatchen (MEDIUM 13–16 + de visuella fynden från `docs/incoming/BANDY_MANAGER_AUDIT_5_SASONGER_KUL_STICKINESS_VISUELL_2026-08-29.md`) — oberoende, kan tas när som helst. Plus det lilla inbox-cull-passet ovan (HIGH 5-residualen) när ekonomipasset inte längre rör worktree:t.
