# HANDOVER 2026-09-07 — till nästa Opus-chat

Läs detta först, sedan root `CLAUDE.md`, `docs/MASTER_OPPET.md`, `docs/dom/DOMLOGG_2026-08-31.md §1`, `docs/LESSONS.md`, `docs/handover/HANDOVER_2026-09-04-kvall.md` (föregående). Verifiera workspace-verktygen vid start (de varierar). Jacobs preferenser gäller: exekvera direkt, inga man-timmar, avsluta inte konversationer, skriv svensk text direkt (spec:a den aldrig till Code), en dom = en rad + en fil + DOMLOGG-index. **Nytt sedan sist:** `docs/POST_LAUNCH.md` finns — flytta medvetet uppskjutet dit i stället för att låta det tynga MASTER.

## Vad det här passet var

Ett långt pass som mest handlade om att STÄNGA, inte öppna. Tre spår parallellt: (1) Jacobs beslutskö betad från ~80 rena beslut till NOLL, fem i taget med Opus-förslag på varje; (2) illustrationssviten färdigställd — alla 16 momentbilder låsta; (3) Opus-domar som låste upp Code (klocka, karriärbyte, license-event, missing-check-kontraktet, mitt-i-säsongen-varningen). Codex körde ARCH-001 refaktorn parallellt (roundProcessor 2 562 → 1 787 rader över pass 5–9) plus liggar-spåret. Matchflödet — det Jacob var mest keen på — byggt och pushat.

## Det dominerande MÖNSTRET (läs detta, det återkom 6+ gånger)

**Opus gissar status i stället för att verifiera, och blir rättad varje gång — av Jacob eller av en agent.** Nyår stängdes på minne (var ogjord), sedan öppnades felaktigt, sedan var den ändå gjord. Porträtt kallades "fungerar" för att FILER fanns (de renderas som SVG-aliens, PNG:erna är inte inkopplade). Sommaren höll jag på att låsa osedd. Orsak/verkan ville jag avföra (det är en av projektets STARKASTE signaler — varje GPT-test + Design). Design-mocken för matchflödet trodde jag var svag (den var den tydligaste som kommit). **Lärdomen för nästa Opus: läs verkligheten INNAN du dömer eller påstår status, åt BÅDA hållen — "det är gjort" och "det är inte gjort" är båda påståenden som kräver verifiering. Filer på disk ≠ fungerar. En rad som säger klar ≠ klar.** Agenterna tillämpade denna disciplin bättre än Opus flera gånger (Codex korrigerade EkonomiTab-fallbacken i klock-domen, fångade två PRE-SPEC-fel i matchflöde-briefen, hittade att tre "att göra" redan var gjorda).

## Illustrationerna — ALLA 16 MOMENT LÅSTA

Låsta i dag: sommaren, nyår, game over, bygget klart, cupguld (inkopplad). Katalogen (`ILLUSTRATIONER_KATALOG_2026-09-04.md`) har greppen. **Serielärdomar inskrivna:**
- **Generationsförlust:** editera aldrig en bild mer än ett par varv — varje edit läser förra komprimerade utfallet, kornet degenererar till JPEG-sönderfall. Behöver en bild mer → omgenerera från prompt, från tomt.
- **Bandymål (web-verifierat, serievaktpost):** 3,5 m brett × 2,1 m HÖGT, grund snedställd kasse. BREDARE än högt men INTE lågt. Opus beskrev upprepat innebandymålet (litet, lågt, djup låda) — fel. Rätt mål i varje framtida prompt.
- **Utsnitts-drift:** 16 planvyer i olika ljus blir monotoni även när var för sig är rätt. Variera utsnittet medvetet.
- **Konkret referens slår abstraktion:** "en fotbollsplan som den sköts på sommaren" gav rätt gräs; "maintained lawn" gav golfbana/äng varje gång.
- **Anti-akvarell + rent korn:** "flat blocks of colour, crisp edges, NOT watercolour, no digital noise" i varje prompt.

**Kvar (bild):** sex tysta interiörer (två klara i dag: hallprövning + ultimatum; kvar: mecenatmiddag, presskonferens, valet, akademin — går snabbt, inga folkbild-fällor, bryter utsnitts-driften) + **porträtt-passet** (`inv-5-fas5-portrattgenerator`: SVG-aliens visas, PNG:erna inte inkopplade — Opus+Jacob-bild-pass, INGEN generator att bygga, lös layout-kravet + generera via Gemini).

## Opus-domar fällda i dag (alla på sina MASTER-rader)

- **Klockan (`currentChronology`):** route genom `buildSeasonCalendar(season)`, inte live `game.fixtures` (byts vid rollover). Löpande vs historisk säsong. BYGGD (`1500d1e5`). Codex korrigerade min premiss om EkonomiTab-fallbacken (skyddar `round<1`, inte cross-season — behåll den).
- **Karriärbyte (`switchManagedClub`):** nollställningen STÅR. **Rykte = vad världen vet om dig (följer), tålamod/buffert = vad EN styrelse känner (nollställs).** Tabellen hade buntat ihop dem. BYGGD (`0e97bdd3`).
- **License-event:** FALSK MOTSÄTTNING — `licenseStatus` (levande ackumulator) och `license_event` (historisk post) existerar sida vid sida. `getSeasonLicenseConsequence` väljer källa efter tidsfråga. Samma distinktion som klockan/karriärbytet.
- **Missing-check-grinden (kontraktet):** varje textmall som påstår att något hände ska DEKLARERA sin bevis-källa (`provenBy`), grinden verifierar att källan finns OCH säger rätt. DEL 1 byggd (18 liggarmallar, `14dcaf22`). `matchCommentary`-kaptensbuggen fixad (`56e7ec1c`). DEL 2 (events/ + press) POST-LAUNCH. **Princip: `@cites` (syntaktisk) och `provenBy` (körtid) ska KONVERGERA till ett kontrakt.** Full population: `RECON_PASTAENDEMALLAR_UTANFOR_LIGGAREN_2026-09-07.md`.
- **Mitt-i-säsongen-varningen:** skrivs INTE till liggaren — en varning är en LÖPANDE SIGNAL (oro), inte en historisk händelse (dom). Krönikan minns domen, inte oron på vägen.
- **c1-kurering:** karriärvändpunkter kureras från SÄSONG 3 (säsong 2 = etableringsår, boardMeetingCopy A-stat). BYGGD.
- **Letter-Krönikan:** Opus skrev caset + texten direkt i `clubMemoryService.ts` ("{Namn} skrev. Ett brev om det som varit — och att det räknades för någon."), Code verifierade+committade (`613204ca`).

## Beslutskön — TÖMD

Började ~80 rena Jacob-beslut, nu **noll**. Allt avgjort: byggt / avfört / flyttat till POST_LAUNCH / domat. Kvar med "Jacob" på sig är bara PLAYTEST (14 — Jacob spelar, kör GPT-playalong) och MÄTNING (6 — Codex kör). **Ingen ren dom kvar att fatta.** Nästa naturliga Jacob-steg är det stora playtestet (samla de 14 till en spelbar checklista — ej gjord än, Jacob ville ta det "när vi är klara").

## Release-kritiska spår (status)

- **Bygget grönt:** KLART — sju Granska-typografiträffar fixade (`ea7abf47`), `npm run build` passerar. Publicering nu MÖJLIG.
- **Vercel:** LÖST — `vercel.json` `deploymentEnabled.main:false` (`fdedd509`). Push auto-deployar INGET; publicering är manuell (`vercel --prod`), ett medvetet val. main är ensam sanning (release-branchen raderad). CLAUDE.md deploy-avsnittet omskrivet.
- **CI-baselines:** Design pixel-dömde runda 2 mot exporten (`9bcba406`): 45 acceptera / 18 Code-läsning / 2 timeout. Rotorsak = abs-säsongsetiketten byter säsongsheltalet → etikett + reseed + matchdatum (ingen ny copy). **Kvar (Code):** avgör 2 saker före seed — primary-scenerna (avsedd retune vs regression?) + board-a/b/c+upptakt (säsongs-seedad vs oseedad slump?), fixa ev. hög C, trigga `visual-baselines.yml` en gång för alla 65.
- **Backend/push:** mandat givet (Render + Postgres), Codex V1. **10k-avskedsmätningen GATED** — får ej köras (~18h) förrän det lilla passet träffar Heros 55–65% + mittklubbarna <50% (nu 100%/65%, modellen för hård). Codex separerar avskedsorsakerna på det lilla passet först. `AVSKEDSKALIBRERING_DIAGNOSTIK_2026-09-07.md`.

## Byggt av Codex/Code i dag (allt pushat till main)

Matchflödet (taktik-som-steg, autofyll-ett-klick, vinjett — `4fcb93ef`), notisinställningar hel vertikal skiva (`4fcb93ef`), klockan (`1500d1e5`), karriärbyte-tabellen (`0e97bdd3`), liggar-spåret (board-verdict/license/facility/community/letter Krönika-konsumenter, `5e5f1e5e`+`613204ca`), missing-check DEL 1 (`14dcaf22`), kaptensbuggen (`56e7ec1c`), ARCH-001 pass 5–9 (roundProcessor → 1 787 rader; narrativeProcessor, eventProcessor, communityProcessor extraherade). Notis-categoryFor-familjerna: copyn FINNS (register §2/§3), Code wirar mallarna till `ForwardPushPayload` + utökar payloaden med deklarerade fält — INTE Opus-skrivning.

## Design-kön (25 öppna) — INTE pekad, står stilla

Design plockar inte själv (till skillnad från Code). Det är den lucka som inte syns förrän man frågar. Fem har Jacob-namn (de fyra `sluttest-14-*` + CI-baselines — alla inbakade i CI-baseline-passet, inte styckvis). Resten: verkliga design-uppdrag att peka (`redesign-klubbminnet`, `flode-02-loop-andning`, `design-p1-tysta-ytor` = interiörerna som pågår) + småhantverk i klump (emoji-konsekvens, opponentform-tomt, SM-final-skarv, transfer-nudges). **Opus erbjöd att samla dem till en överlämningslista men Jacob pekade inte än — gör den listan om Jacob ber.**

## Opus öppna trådar (min trav)

- **orsak/verkan-scoping** (`sluttest-audit-orsak-verkan`): projektets starkaste återkommande signal (varje GPT + Design). Har ett BEFINTLIGT `orsakVerkanService.ts` att bygga på — passet är läs-och-speca-gapet, inte skapa-från-noll. EJ PÅBÖRJAD, väntar på Jacobs go.
- Design-överlämningslistan (om Jacob ber).
- Interiör-prompterna (fyra kvar) när Jacob vill måla vidare.

## Ocommittat vid handover

Opus dagens docs: MASTER-rader, `POST_LAUNCH.md`, `RECON_PASTAENDEMALLAR_*`, denna HANDOVER, katalogen, CLAUDE.md deploy-avsnitt — bara docs + ett kod-case (letter i clubMemoryService, redan committat av Code `613204ca`). Codex committade flera Opus-domar på köpet (delade rader kan inte delas över commits). **Nästa Opus/Jacob: verifiera att dagens docs är committade — Opus domar har legat ocommittade och räddats av Codex på köpet, inte av rutin.**

## Godkänt-när (oförändrat från 09-04)

GPT kör minnes-slutprovet (fyra säsonger): spelet skriver karriärmeningen själv ur årsböcker + Krönika; Jari-mot-oss och återkomsten till gamla klubben får sina rader; ingen avslutad båge återkommer som ny; ingen händelse bär fel omgång. Release-tröskeln för det som gör spelet till vad det är. Nu STÄRKT av missing-check-grinden (påståenden måste bevisas) och den säsongssäkra klockan (ingen post bär fel omgång — byggt).
