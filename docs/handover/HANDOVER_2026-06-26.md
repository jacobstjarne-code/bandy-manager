# HANDOVER 2026-06-26

Tre spår rörde sig idag. Två är parkerade på andra (Opus/Jacob), ett på din playtest.

---

## Spår 1 — Typografi-kanon

**Levererat (pushat):**
- `7d5e1548` foundation: `.h-num-sm/.h-num/.h-num-lg` (display + tabular-nums, färg inline), `.h-quote-sm`, `.h-label-light`/`.h-quote-light` + token `--text-quote-light`.
- `.h-name` + `.h-micro` (Ruling 1+2 låsta): name-roll 15px färg inline, micro 9px vikt+färg inline. Mono-meta → `// ds-exempt`.
- `d53f85b7` roll-klassificeringskarta: alla 346 siter klassificerade per fil/roll i `design-system/briefs/MAP-TYPOGRAFI-MIGRERING-2026-06-26.md`.
- `6aee3f82` **GranskaOversikt** — skärm 1 av Granska-flödet. 9 siter (7 klassade, 2 ds-exempt). Filens typografi-guardräkning = 0.

**Parkerat på Jacob:** playtesta GranskaOversikt (Granska → Översikt). Tre flaggor att avgöra:
1. Media-rubriken (📰 MEDIA) blev *italic* av `.h-quote` — tål den det, eller exempta?
2. M15-värdet "Dina val · Utfall" lät jag stå bespoke 18/800 (exempt) — konvergera till `.h-num-lg` (700) eller behåll emfas?
3. Stat-labels (SKOTT/HÖRNOR) nu 600/2.5px — rätt konvergens?

**Nästa (efter playtest):** rulla samma mönster över resten av Granska (Analys → Spelare → Shotmap → Screen), en skärm + playtest i taget. Mönsterjusteringar billigast nu, innan skärm 2–5. Efter hela passet: sänk ds-guard-baslinjen + koppla `lint:design-guard` i build.

---

## Spår 2 — Tillträdet + Klubbpärmen

Instruktion: `docs/CODE_INSTRUKTION_TILLTRADET_KLUBBPARMEN_2026-06-26.md`.

**Levererat (pushat):**
- `5478f0ec` Tillträdet foundation + skelett: `game.onboardingComplete`-flagga, `markOnboardingComplete()`, `/tilltrade`-route, `TilltradeScreen` 4-stegs container. **F1 + F4 färdiga** (Opus-text ordagrant, coach-namn templatat ur `game.assistantCoach`). F2/F3 WIP-pass-throughs.
- `e74d3abd` Klubbpärmen shell + registry: `klubbparmContent.ts` (6 kapitel, unlock-predikat mot riktig state), `KlubbparmOverlay.tsx` (läder-band, flik-index, 🔒 på låsta). Content stubbat med `[Opus]`.

**Medveten avvikelse:** ArrivalScene-rerouten flippas **sist**, inte först (instruktionens steg 1) — så `main` inte får halvbyggt onboarding. `/tilltrade` nås bara direkt/dev tills flödet står.

**Parkerat på Opus/Jacob:**
- **F2** (wira riktig lineup-yta) + **F3** (wira CornerInteraction övningsläge) — Jacob kör med Opus. F3 har ett beslut: `CornerInteraction` hårdkodar 5s-timern, men F4-texten säger "så lång tid du ville" → practice ska ha timern AV. Kräver additiv `practice`-prop i `InteractionShell` (default 5s kvar för match). Coachtipset behöver inte röras (komponenten genererar `generateCoachQuote` internt när den får `coach`).
- **Kapiteltext** (Klubbpärmen, alla 6) — Opus skriver i protokoll-röst, se `klubbparmContent.ts`-headern.

**Parkerat på Code (när ovan landat):**
- Steg 6: koppla Klubbpärmen i menyn + ta bort HelpOverlay (efter Opus-text).
- Steg 7: flippa `ArrivalScene.onComplete` → `/tilltrade` (när F2/F3 klara).
- Steg 8: Portal-gating + pensionera CoachMarks (när Tillträdet verifierat i kontext).

**Separat städning:** `OnboardingHint`/`ContextualNudges`/`DashboardNudges` är bekräftat död kod (noll importer) — `git rm` när Jacob vill.

---

## Spår 3 — MatchLive-omdesign (ej påbörjat)

Instruktion: `docs/CODE_INSTRUKTION_MATCHLIVE_OMDESIGN_2026-06-26.md`. 5 grepp + dockad interaktion, perception-tung, flätas ihop med typografi-passets MatchLive-site. Väntar — startas när Jacob säger till. Lägst-risk första steg = grepp 4 (LedgerFrame phase=spela slim-rad).

---

## Föreslagen ordning nästa session

1. Jacob playtestar GranskaOversikt → avgör 3 flaggor → Code rullar Granska-flödet vidare.
2. Opus skriver Klubbpärm-kapiteltext + kör F2/F3 med Jacob.
3. När F2/F3 + text landat: Code stänger Tillträdet-kön (steg 6–8).
4. MatchLive-omdesign som eget spår när Jacob prioriterar in det.
