# HANDOVER 2026-04-30 — Portal Fas 2 levererad, texter klara

**Commits:** `b7e5a0d` (steg 1–3) + `03db307` (slutliga texter + logikfixar)
**Status:** Steps 1–3 av SPEC_PORTAL_FAS_2_DRAMATURGI levererade och pushade. Step 4 väntar på Jacobs playtest-feedback.

---

## Vad sessionen levererade

### Buggfixar (bakgrund från föregående session, inbakade i b7e5a0d)

| Bugg | Rotorsak | Fix |
|------|----------|-----|
| CTA saknas på portal (rapporterad 4+ gånger) | `DashboardOrPortal` returnerar alltid `<PortalScreen />` — `DashboardScreen.tsx` renderades aldrig, alla fixes hamnade i fel fil | Lade CTA-logik direkt i `PortalScreen.tsx` |
| Tomma ronder för cup-bye-lag | Cup-lag som är direktkvalificerade till KVF fick "Spela omgång 1" och klickade igenom en ronde de inte spelade | `useEffect` på mount som auto-skippar om nästa matchday saknar managed match |
| Horisontellt streck i NextMatchCard | `var(--border)` odefinierad i portal dark theme | Lade `--border`/`--border-dark` overrides i `NextMatchPrimary.tsx` |
| Secondary-kort livlösa | TabellSecondary visade bara position + tiny sträng; InjuryStatusSecondary visade ett namn | Richer layout med formdottar + kontextrader i båda |

### SPEC_PORTAL_FAS_2_DRAMATURGI — Steg 1–3

**Steg 1 — SituationCard (ny komponent + service)**
- `src/domain/services/situationFragments.ts` — 7 pure fragment-builders
- `src/domain/services/situationService.ts` — omskriven, assembly via `joinFragments()`, 15+ states i prioritetsordning (SM-Final, slutspel, cup, vinstsvit 4+, förlustsvit 4+, etc.)
- `src/presentation/components/portal/SituationCard.tsx` — visuell komponent, accent-left border, uppercase label, italic display-font body

**Steg 2 — PortalBeat (narrativa nedslag)**
- `src/domain/data/portalBeats.ts` — 6 beats: `season_opener`, `first_win`, `first_derby`, `halftime`, `transfer_window_open`, `last_league_round`
- `src/domain/services/portalBeatService.ts` — `getActiveBeat()` + `getBeatKey()`
- `src/presentation/components/portal/PortalBeat.tsx` — emoji + italic text + × dismiss
- `src/domain/entities/SaveGame.ts` — `shownBeats?: string[]` lagt till
- `src/presentation/store/actions/gameFlowActions.ts` — `dismissBeat()` action

**Steg 3 — Kontextuella secondary-kort**
- `TabellSecondary.tsx` — `getContextLine()` med playoff-gap, ledaregap, "säsongen börjar nu" vid omg 0
- `InjuryStatusSecondary.tsx` — `isKeyPlayer`-check, kontextrad under spellistan

### Slutliga texter + tre logikfixar (03db307)

**Texter:** Alla fragment, beats och situationService-rader bytta mot Opus-versioner. Kortare, mer direkt. Bort: "möttes ni vann ni", dead ternary, hårdkodad placeringstext.

**Logikfix 1 — `first_derby`-beat:** Triggade vid varje derby, inte bara första. Ny trigger kollar `completedDerbies.length === 0` för `f.season === currentSeason`.

**Logikfix 2 — `first_win`-beat:** `<= 5` rounds-begränsning borttagen. `wins === 1` räcker.

**Logikfix 3 — `getOpponentStandingFragment`:** Dead ternary `${oppPos - myPos > 0 ? '' : ''}` ersatt med `Math.abs(myPos - oppPos)`, returnerar `null` vid stor skillnad uppåt.

---

## Vad som är öppet

### Steg 4 — Inbox-prioritering (SPEC_PORTAL_FAS_2_DRAMATURGI)

Specen: kategorisera inbox i Critical (alltid full), Notiser (korta, grupperbara), Bakgrund (gömt, expanderbart). Visa "3 kritiska + 11 övriga". Väntar på Jacobs playtest-godkännande av steg 1–3 innan det startas.

### Pixel-audit för SituationCard + PortalBeat

Ingen HTML-mock producerades inför dessa komponenter (specen kom in utan mock). Formellt sett bryter det princip 4. Auditet kan bara göras via webbläsare — Jacob behöver spela och avgöra om den visuella riktningen är rätt. Tills dess är komponenterna "fungerande men ej pixel-verifierade mot kanon".

### Kapitel C-öppningar (från KVAR.md)
- `rumorFrequencyMultiplier` + `incomingBidMultiplier` ej i rumorService/transferService
- `underdogBoost` (dream_round) ej i matchEngine

### Sprint 28-C (Opus-only)
Skärmdump-vänlighets-audit. Inget Code-arbete.

---

## Teknisk skuld av intresse

`DashboardScreen.tsx` — 1093 rader, renderas aldrig (AppRouter returnerar alltid PortalScreen). Kan tas bort när Portal är playtest-verifierat som stabil ersättare. Listades redan i filstorleks-auditen från 2026-04-28 som "bör tas bort".

---

## Nästa session — föreslagen ordning

1. Läs `CLAUDE.md`, `LESSONS.md`, `DECISIONS.md`, `KVAR.md`, denna fil.
2. **Playtest** — öppna webbläsaren, ny manager, spela 3 omgångar. Skärmdumpa SituationCard i tre olika states (seriepremiär, mittfas, slutspurt eller vinstsvit). Skriv 2–3 meningar om det känns kontextuellt.
3. Om playtest godkänner steg 1–3 → **Steg 4 (inbox-prioritering)**.
4. Om playtest avslöjar regress → buggfixar först.
