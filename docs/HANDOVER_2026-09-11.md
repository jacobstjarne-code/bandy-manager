# HANDOVER — 2026-09-11 kväll (Opus, helhetsgenomgång)

## Vad som gjordes

Helhetsgenomgång av kod, persistens, backend, bygge/PWA, CI, docs och IP. Rapporten levererades i chatten; allt som var iteration-fritt fixades direkt på disk och ligger **ocommittat i arbetsträdet**. Det som kräver build/test-loop står i `CODE_INSTRUKTION_GENOMGANG_2026-09-11.md` och som sju `rapporterad`-rader i `MASTER_OPPET.md` (sista KÄLLA-sektionen, räknaren 3→9).

## Ocommittade Opus-edits (Code: verifiera med HELA sviten, committa)

- `server.js` — `trust proxy`, health-version från `RENDER_GIT_COMMIT`
- `server/attention/routes.js` — `timingSafeEqual` på cron-hemligheten
- `src/infrastructure/persistence/saveGameStorage.ts` + test — snapshot-rotation per orsak
- `vite.config.ts` — bilder ur precache, CacheFirst-runtimeCaching för porträtt/illustrationer
- `index.html` — `maximum-scale` borttagen
- `src/presentation/components/SectionLabel.tsx` — emoji `aria-hidden` (kan påverka `getByText`-tester, se instruktionen §0)
- `render.yaml`, `.github/workflows/app-ci.yml`, `visual-baselines.yml` — Node 22
- `CLAUDE.md` — Spelets värld rättad (klubbnamnen ÄR verkliga föreningars), kommunvapen-regel
- `README.md` — ny

## Nyckelfynd som inte fixades (Code, iteration)

1. Huvudchunk 2,79 MB utan code-splitting; ~24 MB bilder precachades vid install (det senare är nu stoppat i vite.config, bildkonvertering kvar).
2. Node 20 EOL — flyttat till 22 i konfig, toolchain-bump (Vite/vitest) kvar.
3. docs-roten: 352 filer, MASTER_OPPET:s räknarstycke oläsbart — omflyttning bara i claim-fritt fönster.

## Jacob-beslut som väntar

- bandygrytan-datan i repot eller gitignorad (databasskydd)
- mätbart soft-launch-mål ur analytics-röret

## Nästa session

Code: §0 i instruktionen först (commit av Opus-edits), sedan §1 code-splitting och §2 webp — båda påverkar första installationen på mobil och bör ligga före soft-launch även om release-definitionen inte kräver dem.
