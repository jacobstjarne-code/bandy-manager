# HANDOVER 2026-06-09

## Sammanfattning

Två spår slutförda: **Spår A (MatchLive/motorkänsla)** och **LedgerFrame (chrome för rond-flödet)**. Spår A levererade ärlig MomentumBar, HalftimeModal-ombyggnad, Spak B och Playwright visuell-regressions-harness. LedgerFrame-passet byggde chrome-komponenten och wrappade Spela-fasen. Förbered och Granska är hållna på Design-beslut.

Avslutades med ett `/simplify`-pass (4 fynd applicerade, commit `76f99ea`). Sprint-audit: `docs/sprints/SPRINT_MOTOR_LEDGER_2026-06-09_AUDIT.md`.

---

## Levererat (commits)

### Spår A — MatchLive/motorkänsla

| Commit | Vad |
|--------|-----|
| `d0768ab` | Steg 0 — exponera motortillstånd på MatchStep (surfacing, ej dynamik) |
| `c301327` | Ärlig MomentumBar — nål = `homeInitiative`, inte skott-proxy |
| `23d00b4` | Två 🟥 in-match-modaler → mörk Stålvallen-panel |
| `fa6d014` | Spår A A1 — `pauseLean` modulerar post-paus-fönstret (`PAUSE_LEAN_FACTOR`) |
| `a5e2ce7` | Spår A C — MomentumBar brytpunkter ur `BRYTPUNKT` (Opus-copy, `{lag}`) |
| `7db0761` | Spår A A2/A3/A4 — HalftimeModal paussnack + preview + mörk LED-panel |
| `dfa771e` | Spak B — sent matchningsval som feed-kort (gate + mentality-väg + amber-glow) |
| `a052bec` | Dev-scener + bilaga — MomentumBar, 🟥-modaler, Spak B (32 ytor) |
| `f481958` | Playwright visuell-regressions-harness för /dev/scenes (Linux-seedad) |

### LedgerFrame — liggare-krom

| Commit | Vad |
|--------|-----|
| `0ad82ab` | Incoming-triage — mocks + handoff arkiverade |
| `4c2cf10` | LedgerFrame — masthead/RPS/marginal/stämpel, inga nya tokens |
| `1b1577e` | MatchLiveScreen wrappas i LedgerFrame (phase=spela) |
| `7d0827a` | DESIGN-DECISIONS §LF-1–5 låst + CODE-LEVERANS-2026-06-09 |

### Simplify-pass

| Commit | Vad |
|--------|-----|
| `76f99ea` | `useManagedClub()`, halftime onClick no-op, `PHASE_INDEX`/`PERF_DOTS` module-constants, `PAUSE_LEAN_FACTOR` smalnad |

---

## Nyckelbeslut

1. **§2-ärlighets-princip** — MomentumBar nål = `homeInitiative` (inklusive alla multipliers), inte ett parallellt skottproxy. Varje UI-motorindikator spårad till verklig motorvariabel.
2. **PAUSE_LEAN_FACTOR** — samma konstant driver preview i HalftimeModal och sim i andra halvlek. Inga parallella speglar.
3. **Spak B delar taktikbyte-budgeten** (MAX_TACTIC_CHANGES = 3). Kortet visas bara om `creditsLeft > 0`. Dokumenterat i DECISIONS.md.
4. **Playwright Linux-baselines** — Mac-renders gitignoreras. Baselines seedas via `workflow_dispatch` (visual-baselines.yml) på GitHub Actions. CI-jobbet `visual-regression` hoppar over om baselines saknas (ingen falsk röd).
5. **LedgerFrame §LF-1–5** (se DESIGN-DECISIONS.md) — rond-flödet + dokument, INTE dashboards. Stämpel = funktionell status. Inga nya tokens. LED-tavlan = enda mörka ytan.
6. **Förbered och Granska hållna** — Förbered: gaffel wizard vs tabbar Trupp|Taktik, väntar Design-mock + Jacobs beslut. Granska: gaffel konsolidera vs ledgra rapport, väntar Design-flödesbeslut.

---

## Kvarstår

### Obligatorisk playtest (CLAUDE.md — perception-tunga vyer)
- **MatchLive med LedgerFrame-chrome** — masthead + RPS + marginal i live-spelläge. Bar-animation, Spak B i live-feed, amber-glow timing, preview-pil i HalftimeModal vs faktiskt 2H-utfall.
- **Spår A-stacken isolerat** — MomentumBar bandwidth (mörkt matchläge), BRYTPUNKT-text i rätt situation, HalftimeModal dark panel vid halvtid.

### Infrastructure
- **Seeda Linux-baselines** — GitHub → Actions → `visual-baselines` → Run workflow. Genererar `*-linux.png` och committar. CI `visual-regression`-jobbet aktiveras.

### Pending Design-beslut
- **Förbered-children** — Trupp-fliken vid riktig densitet + Jacobs beslut (wizard vs tabbar).
- **Granska-children** — flödesbeslut + Jacobs beslut (konsolidera vs ledger).
- **Säsongsflöde (CODE-LEVERANS-2026-06-09)** — 4-delssprintad fix (season_kickoff, boardMeetingStateResolver, currentMatchday-reset, ordningstals-svep). Redo att köras i Code-session.

### Det failande testet
`boardMeetingScene.test.ts > triggar säsong 2` — från Opus parallellström (ocommittad `boardMeetingScene.ts`). Åtgärdas när den strömmen committas (seasonSummaries.length === 0-logiken).

---

## Föreslagen ordning nästa session

1. **Playtest** — MatchLive med LedgerFrame (perception-krav per CLAUDE.md). Spela en match till slut, öppna HalftimeModal, se Spak B card i feed, granska stämpeln vid fulltime.
2. **Seed Linux-baselines** om Playwright ska aktiveras i CI.
3. **Säsongsflöde-sprint** (CODE-LEVERANS-2026-06-09 Del 1–4) om playtest inte blockerar.
4. **Granska-children** om Design-beslut finns. LedgerFrame tar emot dem direkt via children/tabs-prop.
