# Handover 2026-06-10

## Vad som levererades

### Sprint 1 — Ledger-vocab + volunteerMorale (commit `b1c7ca3`)
- `BidModal` och `RenewContractModal` använder LedgerFrame CSS-vokabulär
- `volunteerMorale` i CS-systemet: mean-reversion drift (3% / turn) förhindrar permanent drift mot extremerna

### Sprint 2 — Tech-debt E-SC1/SC2/SC3 (commits `f7901b4`, `fedd663`, `aa5569f`)
- `seededPick`/`seededPickNoRepeat` konsoliderade i `src/domain/utils/random.ts` — 5 duplikat borta
- `seasonTrendStroke` helper i `roundCharacter.ts` — ersätter inline last-vs-first-beräkningar
- Portal-eskalering och playoff-kontext förberäknas en gång i `PortalScreen` (var tidigare re-computed per render)

### Sprint 3 — A3 Match-laddning beat (commit `b5e845e`)
Nytt `'laddning'`-steg i `MatchScreen` mellan "Spela omgång X"-knappen och uppställning.

**Hur det fungerar:**
- `computeLaddningBeat(game, fixture)` i `matchLaddningGrind.ts` avgör tiers:
  - `scene` → fullbleed (isFinaldag / isAnnandagen / isNyarsbandy / isCup / pre_derby / premiere)
  - `band` → slim förändrings-markör (vinstsvit/förlustsvit ≥3, milestone [3,5,7,10], bruten svit)
  - `none` → hoppar direkt till lineup (ogrindad match)
- `MatchLaddningScene` — 100dvh, illustration/placeholder, dubbla scrims, eyebrow §9.1-färg, charge+relation ur textpool
- `MatchLaddningBand` — mörkt skärmfyllande, vänster-border warm, charge ur textpool
- `matchLaddningBandShown` på SaveGame håller change-marker state mellan omgångsnavigering

**§9-kontrakt hållet:**
- Eyebrow `var(--gold)` BARA på final; derby → `var(--warm-light)`; övriga → `var(--accent)`
- CTA = `btn btn-primary` (ej `btn--hero`), radius 14 (scen) resp. 8 (band)

## Assets-status
| Tillfälle | Status |
|-----------|--------|
| annandagen | `/assets/illustrations/annandagen.jpg` — finns |
| final | `/assets/illustrations/final.jpg` — finns |
| derby | Placehållare (beställd men ej i repo) |
| nyarsbandy | Placehållare (beställd men ej i repo) |
| cup, premiar | Placehållare (inget asset planerat ännu) |

## Nyckelbeslut fattade

Inga nya arkitekturbeslut — allt följer befintliga mönster (seededPick, IllustrationScene/Placeholder, gameStore Zustand-spread).

## Kvarstående frågor

1. **Manuell playtest** av A3 — visuell rendering mot mock, placeholder-fallback, stakeText-pil (kräver säsongskontext relegation/topRace)
2. **derby.jpg / nyarsbandy.jpg** — när kommer de till repo? Spec-ref: `docs/CODE-LEVERANS-MATCH-LADDNING-A3-2026-06-10.md`
3. **Transfers Sprint 1** (plan-fil `logical-puzzling-wave.md`) — Del A/B/C/D fortfarande ej körda

## Föreslagen ordning nästa session

1. Playtest A3 scen/band mot mock i levande app — pixel-jämförelse annandagen och final (de 2 assets som finns)
2. Transfers Sprint 1 om Jacob vill prioritera CSS-städet
3. Manuella playtester av övriga levererade system (pixel-audit) per KVAR.md backlog
