# HANDOVER 2026-05-17 (uppdaterad — full dag)

**Sessions-tid:** 2026-05-17 morgon → kväll
**Nästa session ska kunna starta utan att läsa chathistoriken** — allt nödvändigt finns här.

---

## Commits idag (kronologisk)

| Commit | Beskrivning |
|--------|-------------|
| `cf7a3f0` | fix: fyra playtest-fynd batch 1 |
| `a069915` | docs: CLAUDE.md BACKLOG/KVAR-distinktion |
| `ec8ca1b` | fix: ratings regression rev2 + clock overshoot |
| `099a147` | fix: decision-stacking vid cup-omgångar |
| `bbbe301` | fix: bugs 7-9 — UTVIS/ojämn form/väder |

---

## Vad som levererades

### Batch 1 (cf7a3f0)
- Ratings-regression fix för snabbsim-path (matchday-diskriminator)
- Cup-round text dold i portal
- Cup-pass beats filtrerade
- Händelsetidslinje tom-rad-bug (side-effect av ratings-fix)

### Batch 2 (ec8ca1b, 099a147)
- Ratings regression rev2 (ytterligare fix)
- Klocka stannar vid sista steget (`return m` istf `return m+1`)
- Decision-stacking: snapshot/restore av pendingEvents vid auto-advance-loop

### Batch 3 (bbbe301)
- **Bug 7**: UTVIS på scoreboard — `ScoreboardStalvallen` Module 2 nu kopplad med `PenaltyEntry[]` byggd från displayedSteps RedCard-events (10 min utvisning, remaining beräknat i realtid)
- **Bug 8**: "Ojämn form · —" — `LineupStep.tsx` la alltid till formation oavsett om den sattes. Bara `ClubStyle.Balanced` hade formation i `buildTactic()`. Fix: formation läggs till villkorligt
- **Bug 9**: Väder-koherens — `matchMoodService` kollade temp före condition → fog-matcher fick frost-beat. MatchHeader visade iceQuality vid fog/snow/thaw. Fix: condition-beats prioriterade i matchMoodService; header visar conditionLabel vid fog/snow/thaw

---

## Nyckelbeslut
- Penalty-strip countdown är statisk (secondsLeft är beräknat vid render, inte tickande). Tillräckligt för UTVIS-display.
- WeatherHeader visar nu conditionLabel (Dimma/Snöfall/Töväder) vid dessa conditions, annars iceQuality (Bra is/Utmärkt is etc). Ger koherens med beat och advice.

---

## Kvarstående / ej åtgärdat
- **Bug 4** (WeeklyDecision header kvarstår efter resolve): stacking-fix minskade frekvensen. Verifiera i nästa playtest — separat fix om det fortfarande syns.
- **Bug 10** (styrelse+kommun+kafeterian tjatar): i BACKLOG C-P1, ej prioriterat
- Penalty-strip secondsLeft uppdateras inte i realtid under pågående match (statisk snapshot vid varje render av currentMatchStep). Rimligt för nu.

---

## Föreslagen ordning nästa session
1. Playtest — verifiera alla fixes (UTVIS, form, väder, stacking, clock)
2. Om ren → ny sprint ur BACKLOG
