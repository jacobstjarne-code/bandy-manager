# HANDOVER 2026-05-30

## Vad levererades (commits `7c30dd9` → `8ee2e17`)

### Score Våg 2 — VictoryScore + ScoreBlock hero
- `VictoryScore.tsx` migrerad till `ScoreBlock variant="gold"` (regressionsmisstanke bekräftad via dev-galleri — 16px default för litet)
- Ny `size`-prop på ScoreBlock: `'compact' | 'default' | 'hero'`. `compact` boolean kvar som deprecated alias
- `size="hero"`: 48px mono 700, ingen border-left, subtil border-bottom. `gold + hero` = guld utan ram
- `VictoryScore.tsx` + `CupFinalVictoryScene` + `SMFinalVictoryScene` plockar upp automatiskt

### Dev scene gallery `/dev/scenes`
- DEV-gatad route (lazy import), monterar fingerade game-states utan att behöva spela dit
- Scener: Cup Victory, SM-Final Victory, SeasonArcCard (toppa omg 16), Portal Cards (mörk yta), Efterklang
- `scripts/dev-screenshots.mjs` — Playwright headless, tar PNG:er av alla scener
- `screenshots/` — senaste körning sparad i repot

### Periodisering-rensa
- `SEASON_FORM_FITNESS_SLACK` unifiat till 3 (var 5 i processor, 3 lokalt i evaluator)
- Bänk-recovery kapas mot `seasonForm + SLACK` (tidigare ingen cap)
- `recentlyInjuredUntil?: number` på Player — sätts vid läkning (nextRound + 3)
- `getReaction()` tar `currentMatchday` (default 0) — varnar "Ramp först" på Bygg/Toppa under ramp-perioden

### MatchResult revert
- MatchResultScreen byggdes (LED → Block-hero), playtesten visade regression (dubbelvisning, bröt Granska)
- Reverterad: advance/MatchLive/MatchScreen → `/game/review`, MatchResultScreen raderad
- **Efterföljande bugfix:** `{ replace: true }`-varianten i MatchScreen.tsx + RoundSummaryScreen.tsx missades av reverten — fixade i `8ee2e17`

### Efterklang V2
- `EfterklangMemory` utökat: `objectName`, `sinceMatchday`, `threadEntries`
- Header: `EFTERKLANG · {objektnamn}` istf `N minnen`
- Subrad: tidsanvisning ("Sedan omg 4 · 12 omg")
- Tap → `EfterklangThreadModal` (bottom sheet, dark portal surface, kronologisk lista)
- `EFTERKLANG_TYPE_ICON` extraherad till `efterklangText.ts` (var dubblerad i Secondary + Modal)
- `JOURNALIST_EVENT_LABEL` hoistrad till modulnivå i `pickEfterklang.ts`

### BoardMeeting-fix
- `sceneTriggerService.ts` delegerar nu till `boardMeetingScene.shouldTriggerBoardMeeting(game)` (säsong ≥ 2, matchday 0)
- Var hardcodat `return false` sedan 2026-05-10 — scenen triggade aldrig för säsong 2+
- Guard mot dismissal-loop: returnerar false om `pendingScene.sceneId === 'board_meeting'`
- Migration rensar stale S1-saves
- `clearSeasonSummary`: stale `pendingScreen: BoardMeeting` borttagen

### Granska basics
- Card-margin 6→3px (tätare)
- MATCHEN-kort bottom-padding 12→16px
- GameShell: `/game/match-result` borttagen ur `isMatchRoute` (rutten raderades)

### Kodgranskning (5 fynd)
- F1+F2: döda `/game/match-result`-navigationer (MatchScreen onClose + RoundSummaryScreen matchkort)
- F3: BoardMeeting dismissal-loop guard
- F4: TYPE_ICON-duplikat extraherad
- F5: journalistEventLabel hoistrad

## Nyckelbeslut
- `MatchResultScreen` existerar inte längre. Granska är och förblir hemskärmen post-match.
- BoardMeeting-scenen är nu aktiverad för säsong 2+ men kräver `currentMatchday === 0` — detta inträffar vid säsongsstart (roundProcessor + seasonStartProcessor). Säsong 1 gatas ut.
- Efterklang modal visar journalist-tråd fullt ut. Andra typer har bara en rad ("historik saknas för de flesta typer").

## Kvarstår (öppna frågor)
- **C-V1 OpponentForm** — Våg 3 portal sparklines: OpponentForm inte migrerad, väntar playtest-verifiering
- **Våg 4** (Ekonomi, SeasonSummary, PlayerCard med ScoreBlock/Sparkline featured) — öppet i C-SY2
- **C-SY1 orsakskrok** — reaktiva texter utan orsakskoppling, kräver designrunda
- **BoardMeeting flow** — scenen triggar nu korrekt, men `currentMatchday === 0`-villkoret innebär att scenen bara kan triggas exakt vid säsongsstart. Verifierat i test men ej via playtest. Kräver att spela en hel säsong för att bekräfta.
- **docs/CODE_UPPDRAG_DOMARANALYS_2026-05-30.md** — uppdrag inkommet från Jacob under kodgranskningssessionen, ej påbörjat

## Föreslagen ordning nästa session
1. Läs `docs/CODE_UPPDRAG_DOMARANALYS_2026-05-30.md` — adressera detta uppdrag
2. Playtest av BoardMeeting-flödet (säsong 2 trigger)
3. C-V1 OpponentForm sparklines om tid finns
