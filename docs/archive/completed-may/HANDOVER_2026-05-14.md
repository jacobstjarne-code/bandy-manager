# HANDOVER 2026-05-14

## Vad levererades

### Buggfixar (från playtest-spec CODE_INSTRUCTION_POST_PLAYTEST_2026-05-13.md)

| Fix | Beskrivning | Commit |
|-----|-------------|--------|
| FIX-47 | Scoreboard V1 — klocka i mitten (1fr auto 1fr), sparar ~35px höjd | `f78eaad` |
| FIX-48 | Slutskärm inline — revert FIX-33 overlay, FeedEndRow som första rad i feed | `7d6b9d4` |
| Scoreboard minuter | Visa bara minuter (ej MM:SS) i klockan | `fad3c17` |
| Commentary kaskad | Timer-effekt cascade efter interaktion i commentary-läge — interactionData rensas i setSteps, setCurrentStep hoppas i commentary-mode | `37f7e8f` |
| Plan-vy tom | isInconsistent-check i init useEffect, triggade autoFill om startingIds inte matchade lineupSlots | `01afe85` |
| Plan-vy siffror | Spelarnamn (7 tecken) visas under positionscirklar i pitch-läget | `01afe85` |
| Fyll bästa elvan | Visuell bekräftelse (grön ✓ Uppdaterad i 1.5s) | `b2cfc59` |

### FIX-49 + inline-fixar (2026-05-14)

| Fix | Beskrivning | Commit |
|-----|-------------|--------|
| Kontrast scoreboard | 8 CSS-värden höjda: team-code, period-mark, line-title, tick-label, stats-footer-label, momentum-side-label, match-control-tactic-label, match-controls-label | `ab5df7e` |
| PitchLineupView cirklar | Bakgrund 18%→50%, border 55%→75%, text `var(--bg)`→`var(--text-primary)` | `ab5df7e` |
| FIX-49 timers | CornerInteraction 3s→5s, PenaltyInteraction 3s→5s, CounterInteraction 4s→6s, FreeKickInteraction 2s→4s | `ab5df7e` |
| Kafeterian | "Kafferummet" → "Kafeterian" i 4 filer: coffeeRoomService, CoffeeRoomSecondary, mediaService, scandalService | `ab5df7e` |

### FIX-50 — Lineup default = förra matchens elva

| Komponent | Ändring | Commit |
|-----------|---------|--------|
| FormationView.tsx | Nullar otillgängliga (skadade/avstängda) slots vid rendering | `aee4aa4` |
| FormationView.tsx | handleAutoFill fyller bara tomma slots, position-medveten | `aee4aa4` |
| LineupStep.tsx | useEffect auto-fill triggas bara om lineupSlots saknas helt (ej om null-värden finns) | `aee4aa4` |
| PitchLineupView.tsx | Pulse-animation körs alltid på tomma slots, inte bara vid aktiv selection | `aee4aa4` |
| generateMatchStory | Utbruten till src/domain/utils/matchStory.ts (från MatchReportView) | `7d6b9d4` |

## Nyckelbeslut

- **Commentary-kaskaden:** Roten var att `cornerInteractionData` (m.fl.) inte rensades från steget inuti `setSteps`. `steps`-ändringen re-triggar timer-effekten → ny auto-resolve → ny `setSteps` → loop. Fix: rensa interactionData-fält + skippa `setCurrentStep` i handlers när `isCommentaryMode`.
- **Slutskärm inline:** MatchReportView-overlay (FIX-33) revertad. FeedEndRow som första rad i CommentaryFeedStalvallen. Detaljerat innehåll hör hemma i Granska-vyn.
- **Lineup default:** Sparat `lineupSlots` används direkt; skadade/avstängda nullas vid rendering. `handleAutoFill` kompletterar, ersätter inte.

## Kvarstående frågor

- **Pixel-audit** mot match-live-bundle-mockar (FIX-47, FIX-48) inte gjord manuellt av Jacob — FIX-47 CSS matchar spec-värden exakt, FIX-48 CSS matchar spec.
- **Granska-vyn** bör ta emot events-lista, spelarbetyg, POTM, hörn-band som specs. FIX-48 noterar att de ska dit men det är eget arbete.
- **BATCH E (press/media-separation):** Fortfarande parkerad.

## Ordning för nästa session

1. Browser-playtest av FIX-47/48 och FIX-50
2. Eventuella följdfixar från playtest
3. BATCH E beslut om integration-yta
