# AUDIT_PASS_7 — 2026-04-26

## Utförda steg

### Steg 1–4 (Phase 1, committat separat)
- Q-fact schema tillagt i SCHEMA.md
- 208 Q-facts skapade via backfill (finding 001–046)
- Validator uppdaterad för Q-facts
- Pipeline-steg A, B, C implementerade

### Steg 5 — Backfill-granskning (Opus)
- 208 Q-facts granskade
- 1 duplikat länkad
- 2 exkluderade som för-vaga
- Inga closed-markeringar gjorda — alla 208 bedömdes som meningsfulla frågor

### Steg 6 — Verifieringsfält och UI-badge
- `.verified-badge` CSS-klass skapad i `global.css` (grön text, 0.75rem, inline bredvid `.finding-meta`)
- `bandy-brain/src/pages/findings/001/index.astro` uppdaterad med verified_by/verified_at variabelstöd och badge-rendering — mönster som används vid framtida verifiering
- `bandy-brain/src/pages/findings/index.astro` uppdaterad med filter-knappar (Alla / Verifierade) och `data-verified`-attribut på varje kort
- Filter-knapparna: "Alla" aktiv, "Verifierade" disabled med title (inga findings är verifierade ännu)
- JavaScript-toggle döljer/visar kort baserat på `data-verified`-attribut

### Steg 7 — /sources/ redirects
- Gamla `/sources/`-sidor existerar fortfarande som statiska Astro-sidor (inte borttagna)
- Redirects tillagda i `bandy-brain/astro.config.mjs`:
  - `/bandy-manager/sources/` → `/bandy-manager/bandy/`
  - `/bandy-manager/sources/rules/` → `/bandy-manager/bandy/`
  - `/bandy-manager/sources/stats/` → `/bandy-manager/bandy/`
  - `/bandy-manager/sources/design_principles/` → `/bandy-manager/spelet/`
  - `/bandy-manager/sources/world_canon/` → `/bandy-manager/spelet/`

### Steg 8 — Länkverifierare + CI-integration
- `scripts/check_links.py` skapad
- Kördes: 285 fact-IDs laddade, 32 brutna refs hittades (alla var F-refs)
- Rotorsak: F-prefix (F002, F005 etc.) är finding-korsreferenser, inte fact-IDs — `factHref()` hanterade inte F-prefix
- Fixa 1: `bandy-brain/src/lib/facts.ts` — `factHref()` utökad med F-ref-hantering (`F005` → `/findings/005/`) och `Finding NNN`-format
- Fixa 2: `scripts/check_links.py` — F-refs utesluts från fact-validering (de pekar på findings, inte facts)
- Efter fix: 285 fact-IDs laddade, 0 brutna refs
- CI-integration: blocking `Verify fact links`-steg tillagt i `.github/workflows/bandy-brain-deploy.yml` (körs innan `Install dependencies`, efter `Setup Node`)

### Steg 9 — /questions/ sida
- `bandy-brain/src/pages/questions/index.astro` skapad
- Läser alla Q-facts från `docs/findings/facts/questions/*.yaml` via `fs.readFileSync` + `js-yaml`
- Visar 208 öppna Q-facts, sorterade på `spawned_at` desc
- Varje rad: Q-id (bold), claim-text, länk till finding, "öppen sedan X dagar"
- Rubrik: "Öppna frågor"
- Filterar bort status != 'open' (inga sådana finns ännu)
- Länkad från footern i `Base.astro` som "Öppna frågor"
- Sidan finns i bygget som `/questions/index.html`

## Validator-status

```
Facts totalt:        288
Struktur OK:         288/288
Invarianter OK:      108
Invarianter FAILED:  0
Manuell granskning:  7
Hoppade (deprecated):209

STATUS: RENT
```

MAN-varning D024 är pre-existerande (dict-syntax i invariant), inte orsakat av Pass 7.

## Build-status

`npm run build` i `bandy-brain/` — 208 sidor byggda, 0 fel, 0 varningar.

## Committar i Pass 7

- `feat: pass 7 — verifiering, länkverifierare, /questions/ sida`
