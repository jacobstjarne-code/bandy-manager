# PÅSTÅENDEKARTAN — svep batch 2/8

Agent 2 av 8. Tilldelade funktioner: TabellScreen, TilltradeScreen, GranskaAnalys,
GranskaOversikt (+ dittValCornerText), GranskaShotmap, helpers.generateQuickSummary,
MatchLiveScreen, academyActions, matchActions, transferActions.

| ID | Fil:rad | Funktion | Vad den påstår | Fält den läser | Sanning eller proxy? | Art | Föreslagen fix |
|---|---|---|---|---|---|---|---|
| `TabellScreen.TabellScreen` | `src/presentation/screens/TabellScreen.tsx:488-497` (cupstatus), `:398-449` (rivalitetshistorik/h2h) | `TabellScreen()` | Screenen är i huvudsak tillståndsvisning (tabellplacering, EJ PÅSTÅENDE) men innehåller genuina utfallspåståenden: cupstatus ("🏆 Cupvinnare!", "Utslagen i kvartsfinalen") och klubbrivalitet ("3 raka segrar", head-to-head-resultat per match). | `bracket.winnerId`/`m.winnerId` (satt i `cupService.ts:233` från faktiskt matchresultat `homeWon`), `game.rivalryHistory[clubId].currentStreak` (skrivs i `narrativeProcessor.ts` efter match), `fixture.homeScore`/`awayScore` direkt för h2h-listan. | Sanning | — | Redan korrekt. |
| `TilltradeScreen.TilltradeScreen` | `src/presentation/screens/TilltradeScreen.tsx:150-390` | `TilltradeScreen()` | EJ PÅSTÅENDE — hela flödet är ett skriptat introflöde (F1 ankomst / F2 sätt elvan / F3 öva hörna / F4 klart) med fast dialog och en markerad ÖVNING-hörna (`practiceCorner`). Ingen text påstår något om vad som hänt i spelvärlden; F3:s `resolveCorner`-utfall är en live-genererad tutorial-interaktion, inte ett historiskt utfall. | N/A (fast dialogtext + `resolveCorner()` på syntetiska övningsspelare) | — | — | — |
| `GranskaAnalys.GranskaAnalys` | `src/presentation/screens/granska/GranskaAnalys.tsx:149-174` (Nyckelinsikter), `:108-147` (Formspelare) | `GranskaAnalys()` | Påstår konkreta matchutfall: målkonvertering ("Effektivt anfallsspel — 55% målkonvertering"), hörnstryk, POTM ("{Namn} utsågs till matchens bästa spelare"), samt formspelare (bästa/sämsta betyg bland startelvan). | `fixture.report.shotsHome/Away`, `myScore`/`theirScore` (props, ur verkligt fixture-resultat), `potm`-prop, `fixture.report.playerRatings` filtrerat på `managedLineup.startingPlayerIds`. | Sanning | — | Redan korrekt. |
| `GranskaOversikt.dittValCornerText` | `src/presentation/screens/granska/GranskaOversikt.tsx:150-181` | `dittValCornerText()` | "Du valde {säkra/aggressiva} hörnor. Det gav X mål." — påstår ett samband mellan hörnstrategival och faktiska hörnmål denna match. | `cornerStrategy` (från `fixture.homeLineup/awayLineup.tactic.cornerStrategy`), `totalCorners` (`fixture.report.cornersHome/Away`), `cornerGoalMinutes` (filtrerat på `MatchEventType.Goal && e.isCornerGoal && e.clubId === managedClubId` — riktiga matchhändelser). Returnerar `null` om `totalCorners <= 0` (ingen gissning utan underlag). | Sanning | — | Redan korrekt. |
| `GranskaOversikt.GranskaOversikt` | `src/presentation/screens/granska/GranskaOversikt.tsx:869-891` (`bench_fit`-grenen i "DINA VAL · UTFALL") | `GranskaOversikt()` | "Vilad {spelare}: {utfallstext}" — påstår att kvittot speglar ERSÄTTARENS faktiska matchinsats, inte lagets resultat (redan medvetet fixat 2026-08-24 enligt kommentar rad 872-877). Men: `findRotationSubstituteRating()` kan returnera `undefined` (0 eller ≥2 kandidater efter disambiguering) — då faller koden tillbaka på `kvittoDir` (hela lagets matchutfall) trots att texten fortsatt talar om just den vilade spelarens ersättare. | `findRotationSubstituteRating(fixture, game, entry.playerId)` → `fixture.report.playerRatings[substitute.id]`; fallback: `kvittoDir` (lagresultat, fel entitet i just den grenen). | Delvis | VAR-fel-entitet | Vid `subRating === undefined`: släng raden istället för att falla tillbaka på lagresultatet, eller mjuka texten så den inte längre utger sig för att vara ersättarens insats (samma "hellre tyst"-princip som redan används i `findRotationSubstituteRating` själv). |
| `GranskaShotmap.GranskaShotmap` | `src/presentation/screens/granska/GranskaShotmap.tsx:274-298` (INSIKT), `:205-240` (DITT SKOTTMÖNSTER) | `GranskaShotmap()` | Påstår skott/mål/konverteringsjämförelser ("ni konverterade 55% ... motståndaren bara 12%") och säsongssnitt. Prickarnas exakta (x,y)-position i SVG:n är slumpmässigt genererad (`nextPos`/`seededRand`) och EJ ett påstående om var skottet faktiskt togs (ingen sådan data finns) — ren visuell dekoration, ej text-claim. | `fixture.report.shotsHome/Away`, `scoredCount` (räknat direkt ur `fixture.events` filtrerat på mål+klubb), `savedCount` (`fixture.report.savesAway/Home`), säsongsaggregat ur `game.fixtures` filtrerat på `managedClubId`. | Sanning | — | Redan korrekt (positionerna bör ev. dokumenteras tydligare som "illustrativ, ej geodata" men är inget påstående-brott). |
| `helpers.generateQuickSummary` | `src/presentation/screens/granska/helpers.ts:51-146` | `generateQuickSummary()` | Genererar snabbsammanfattning ("En övertygande seger.", "{Namn} dominerade målprotokollen med 3 mål.") för snabbsim-läget (som saknar live-kommentar helt). | `fixture.homeScore/awayScore`, `fixture.penaltyResult`/`overtimeResult` (för korrekt vunnet/förlorat vid straff-/förlängningsavgörande — dokumenterad rotorsaksfix 2026-08-12), `fixture.events` filtrerat på mål för målskyttar. | Sanning | — | Redan korrekt. |
| `MatchLiveScreen.MatchLiveScreen` | `src/presentation/screens/match/MatchLiveScreen.tsx:1054-1067` (pausbeslut→logg) | `MatchLiveScreen()` | Orkestrerar live-matchen; skriver pausbeslutet (`effectiveLean`, spelarens faktiska val i `HalftimeModal`) till `setHalftimeDecisionForLog`, som `matchActions.ts` sedan loggar som `pep_talk`-post. Detta är exakt PÅSTÅENDEKARTANS rad 1-fynd (Granska läste tidigare `htTempo`/`htPress`/`htMentality` istf det faktiska paussnacket) — nu åtgärdat enligt kommentar (H2-uppföljning, commit 5c9a7a8b, 2026-08-24): `effectiveLean` loggas rakt av istf en härledd taktik-gissning. | `pauseLean`-state (satt av spelarens val i `HalftimeModal`) → `effectiveLean` → `setHalftimeDecisionForLog(effectiveLean)` → skickas som `halftimeDecision`-param till `saveLiveMatchResult` (matchActions.ts). | Sanning | — | Redan korrekt (fixad samma dag som denna kartläggning). |
| `academyActions.academyActions` | `src/presentation/store/actions/academyActions.ts:260-271` (inboxItem.body, `promoteYouthPlayer`) | `academyActions()` | Vid `timing === 'late'`: "{Namn} har vänt på ett par uppkallningar — nu är det dags att ta steget." Detta påstår en KONKRET HISTORIK (flera tidigare, avböjda A-lagsuppkallningar) för just den spelaren. Ingen sådan historik existerar någonstans i datamodellen — `timing` är bara en engångsklassificering av spelarens NUVARANDE `currentAbility`/`confidence`/`age` vid just detta promotion-anrop, inte en logg över tidigare tillfällen spelaren kallats upp och avböjt. | `youthPlayer.currentAbility > 35 && confidence > 70 && age >= 17` → `timing = 'late'` → hårdkodad flavor-text som påstår upprepade tidigare händelser. | Proxy | SANNINGEN-SAKNAS | Antingen (a) skriv om texten till att bara beskriva NUET ("mogen sedan ett tag — perfekt tillfälle", ingen historikreferens), eller (b) om historiken faktiskt är önskad, instrumentera en riktig logg (t.ex. antal omgångar spelaren varit "promotable" utan att kallas upp) och citera den siffran istället för "ett par". |
| `matchActions.matchActions` | `src/presentation/store/actions/matchActions.ts:58-130` (T3 managerChoiceLog-byggaren), `:264-276` (abandoned match-inbox), `:351-359` (walkover-inbox) | `matchActions()` | Denna funktion är primärt SKRIVAREN, inte en yta som själv påstår — men den bygger `managerChoiceLog` (`captain`/`started_tired`/`bench_fit`/`pep_talk`/`halftime_tactic`) som är den nedskrivna sanningen `GranskaOversikt.tsx` sedan citerar. Där den genererar text direkt (abandoned-match-inbox: "Du lämnade matchen innan den var klar ... Resultat: {score}", walkover-inbox: "Matchen gick till motståndaren, {score}") är påståendena korrekt sourcade. | `game.captainPlayerId`, `player.fitness` (tröskel <40/>80), `halftimeDecision`-param (live pauseLean), `game.lastHalftimeDecision` (quicksim); `completed.homeScore/awayScore` för inbox-texterna. | Sanning | — | Redan korrekt — detta är källan de andra ytorna (GranskaOversikt) numera korrekt citerar. |
| `transferActions.transferActions` | `src/presentation/store/actions/transferActions.ts:45-80` (`renewContract`) | `transferActions()` | EJ PÅSTÅENDE i huvudsak — ren mutations-/affärslogik (bud, kontrakt, scouting, sponsring) utan historiska utfallspåståenden. Den enda text som liknar ett "påstående" är avslagsmeddelandet "{Namn} avslår — kräver minst {belopp}" i `renewContract`, vilket är en direkt, korrekt beräknad konsekvens vid anropstillfället (inte en proxy för något annat). | `minSalary` beräknad live ur `player.currentAbility`, `club.reputation`, `player.dayJob`. | Sanning | — | Redan korrekt. |

## Sammanfattning
11 funktioner granskade. 1 tydligt Proxy-fynd (`academyActions.promoteYouthPlayer`, SANNINGEN-SAKNAS — påstår obefintlig uppkallningshistorik), 1 Delvis-fynd (`GranskaOversikt`, VAR-fel-entitet i `bench_fit`-fallbacken när ersättaren inte kan identifieras entydigt). Övriga 9 är antingen Sanning (flera redan fixade samma dag som denna kartläggning, med spårbara kommentarer i koden — pauseLean/pep_talk-kedjan, dittValCornerText, generateQuickSummary) eller EJ PÅSTÅENDE (ren tillståndsvisning/skriptad intro/mutationslogik).
