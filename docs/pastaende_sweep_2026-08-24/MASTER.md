# PÅSTÅENDEKARTAN — omsvepet, master (2026-08-24)

**Metod:** `scripts/measure-claim-coverage.ts` genererade en mekanisk, reproducerbar kandidatlista (85 modulnivå-funktioner i `src/presentation` + `src/domain/services` + `src/domain/data` + `src/application/services` som innehåller minst en prosaliknande sträng med preteritum/numeriskt utfall/kausal koppling) — inte 18 agenter som fritt letade över 282 ytor. Listan sparad i `candidates.json`, partitionerad i 8 batcher om ~11 funktioner, en agent per batch. Varje agent läste den FAKTISKA källkoden för sina tilldelade funktioner och skrev sin tabell direkt till en egen fil (`batch-01.md`…`batch-08.md`) — inte till chatten. Alla 8 filer verifierade på disk efteråt.

**Detta ersätter det förlorade "runda 2"-svepet (43 fynd, 22 aldrig itemiserade).** Populationen här (85 mekaniskt genererade kandidater) är INTE identisk med den ursprungliga (18 agenter som själva letade upp ytor) — den är en ANNAN, men fullständig och reproducerbar, population. Se `docs/PASTAENDEKARTAN_2026-08-24.md`:s VARV 3-tillägg för varför den gamla siffran (15 av 55 klassificerbara) inte dög.

## Totalsiffror

- **85 kandidat-funktioner granskade, samtliga i faktisk källkod (0 gissade ur filnamn).**
- Uppdelat i **~135 distinkta påstående-rader** (flera funktioner gör mer än ett strukturellt separat påstående — att slå ihop dem till en rad per funktion hade gömt fynd, exakt vad batch-agenterna varnade för och undvek).
- **~72 Sanning** (redan korrekt sourcat — flera av dessa är SAMMA fixar byggda tidigare denna session, nu bekräftade i verklig, körd kod: pep_talk-kedjan, dittValCornerText, resolvedChoices-kedjan, championClubId/eliminatedByClubId, matchday-sorteringarna, `deriveUtfall`).
- **~38 EJ PÅSTÅENDE** (tillståndsvisning/framtid/atmosfär/uppmaning — korrekt uteslutna per kartans egen definition).
- **25 konkreta Proxy/Delvis-fynd** — se tabellen nedan.

## De 25 fynden, klassificerade

| # | Fynd | Fil | Art | Status |
|---|---|---|---|---|
| 1 | HalftimeModal "förra året"-raden i HT_WIN_BIG-poolen | `HalftimeModal.tsx` | SANNINGEN-SAKNAS | **FIXAT (kvitterat 2026-08-27, ej committat).** `HalftimeModal.tsx:103-108`. Raden struken — samma princip som #16/#25 (kan påståendet inte beläggas ska det inte göras): ingen säsongskoll fanns, så "förra året" kunde fyras i en klubbs allra första match, och halvtidsställning saknas i historisk data även när ett "förra året" finns. `HT_WIN_BIG` innehåller nu bara belagda, tidlösa rader. |
| 2 | SeasonSummaryScreen retiredPlayers `farewell`/`bestMoment` — kan hävda specifikt karriärögonblick som aldrig verifierats | `SeasonSummaryScreen.tsx` | SANNINGEN-SAKNAS | **FIXAT (åtgärdat 2026-08-27, ej committat).** `retirementService.ts:120-142`: `generateBestMoment` läser nu hatTrick → goals50 → games100 i tur och ordning (alla tre har riktiga, lagrade `description`-fält från `statsProcessor.ts`; bara hatTrick lästes tidigare). Fallback-poolen (rad 51-59) krympt från sex till tre rader — de fyra som hävdade en specifik ohärledd händelse (straff i cupfinalen, bortamatch i snöyran, semifinalsäsong, "mot rivalerna") struckna; kvar bara påståenden sanna för VARJE spelare med ≥10 matcher (garanterad A-lagsdebut). 7/7 tester gröna. |
| 3 | HistoryScreen `allTimeRecords` — uppdateringstidpunkt ej fullt spårad | `HistoryScreen.tsx` | ÅTKOMST-FANNS-ANVÄNDES-INTE (ej bekräftat) | **AVFÄRDAT (kvitterat 2026-08-27).** `updateAllTimeRecords` har ETT enda anropsställe (`seasonEndProcessor.ts:1730`, synkront vid säsongsslut), HistoryScreen bara läser. Ingen tvetydighet fanns — ursprungsfyndets "ej bekräftat"-status var korrekt försiktig, men koden visar sig redan vara entydig. |
| 4 | SeasonSummaryScreen `verdictText`/`expectationVerdict` — möjlig sammanblandning med portalens `boardPatience` | `SeasonSummaryScreen.tsx` + `boardService.ts` | ANNAT (design-fråga, ej bekräftad bugg) | **INTE EN BUGG (kvitterat 2026-08-27).** `boardService.ts:532-545` dokumenterar Jacobs dom 2026-08-26: årsbokens säsongsbetyg och styrelsens tålamak ska MEDVETET hållas isär (till skillnad från #13, inkorgskortet, som fick tålamåndet tillagt). Redan avgjort — bara statuskolumnen var stale. |
| 5 | SeasonSummaryScreen `narrativeSummary`/`keyMoments` — ej fullt spårad till underliggande händelser | `SeasonSummaryScreen.tsx` | ÅTKOMST-FANNS-ANVÄNDES-INTE (ej bekräftat) | **FIXAT 2026-08-26.** Kärnpåståendena (score/hattrick/late-winner) var redan korrekt spårade mot MatchEvent-data, inget fynd där. Ett verkligt, separat fynd: resolvade arc-berättelser fick `type:'bigWin'` som placeholder — SeasonSummaryScreen.tsx:s ikonval läser `type`, så en BITTER avresa (contract_drama_resolved) kunde visas med en ✅-ikon. Ny `type: 'storyline'` (neutral 📖-ikon) i `SeasonSummary.ts`/`seasonSummaryService.ts`/`SeasonSummaryScreen.tsx`. |
| 6 | GranskaOversikt `bench_fit`-fallback när ersättaren inte kan identifieras entydigt | `GranskaOversikt.tsx` | VAR-fel-entitet | **FIXAT (kvitterat 2026-08-27, ej committat).** `GranskaOversikt.tsx:878-891` + `helpers.ts:186-230` (`findRotationSubstituteRating`): returnerar `undefined` vid 0 eller ≥2 kandidater, och `bench_fit`-raden `continue`:ar (hoppas över) istf att falla tillbaka på lagets resultat. Kod-kommentar citerar fixen explicit: "Hellre ingen rad än en rad om fel entitet." |
| 7 | `academyActions.promoteYouthPlayer` — påstår obefintlig uppkallningshistorik ("vänt på ett par uppkallningar") | `academyActions.ts` | SANNINGEN-SAKNAS | **FIXAT (kvitterat 2026-08-27, ej committat).** Nytt spårat fält `Academy.roundsReadyForPromotion` (`Academy.ts:29`, inkrementeras i `academyService.ts:276-284`). Text nu: "{Förnamn} har varit redo i {N} omgångar" (under 3 omgångar: "{Förnamn} är redo"), `academyActions.ts:263-279`. Regressionstest i `academyService.test.ts`. |
| 8 | `arcService` derby_echo — "satte tonen för resten av säsongen", overifierad kausal efterhandskonstruktion | `arcService.ts` | SANNINGEN-SAKNAS | **FIXAT (kvitterat 2026-08-27, ej committat).** `arcService.ts:389-395`. Kausalpåståendet struket. Ny text, sant i nuet: "Derbysegern mot {Motståndare} pratas det fortfarande om." / "Derbyförlusten mot {Motståndare} sitter kvar i omklädningsrummet." Låst av Jacob 2026-08-27 (se BLOCK B-svar samma datum). |
| 9 | `arcService` hungrig_breakthrough peak-event — målsvälte-villkoret re-verifieras aldrig mellan trigger och avfyrning (2 matchdagar) | `arcService.ts` | NÄR-mutation (delayed-fire-variant) | **FIXAT 2026-08-26** (mutationVerificationGate-utökning, kod fanns redan i arbetsträdet vid genomgången 2026-08-26 — bekräftad, ej duplicerad) |
| 10 | `arcService` veteran_final_season dagboksrader — helt oberoende av vad som faktiskt hänt spelaren | `arcService.ts` | SANNINGEN-SAKNAS | **FIXAT (kvitterat 2026-08-27, ej committat).** `arcService.ts:736-755`. Var `currentMatchday % 4` (ren rotation). Nu fyra grenar mot verklig spelardata: `p.morale < 30`, `p.morale >= 70`, `p.seasonForm >= 75`, annars fallback — fyra texter låsta av Jacob 2026-08-25/27 (se BLOCK B). Kod-kommentar (rad 740-748) flaggar ärligt att seasonForm-tröskeln (≥75) saknar kodbas-prejudikat, symmetriskt vald kring baslinje 60. |
| 11 | `arcService` contract_drama peak-event — budstatus re-verifieras aldrig innan avfyrning | `arcService.ts` | NÄR-mutation (delayed-fire-variant) | **FIXAT 2026-08-26** (mutationVerificationGate-utökning, kod fanns redan i arbetsträdet vid genomgången 2026-08-26 — bekräftad, ej duplicerad) |
| 12 | `boardObjectiveService.cupRun` — hårdkodat "15 år" oberoende av klubbens faktiska cup-historik | `boardObjectiveService.ts` | SANNINGEN-SAKNAS | **FIXAT (kvitterat+åtgärdat 2026-08-27, ej committat).** `boardObjectiveService.ts:140`. Jacobs dom: släpp "15 år" (obelagt, ingen `cupHistory`/`lastCupWin`-data finns i `src/`), säg "Det var länge sedan sist." — sant i nuet, kräver ingen historik. Ny text: `"SEMIFINAL! Det var länge sedan sist."` |
| 13 | `boardService.generateSeasonVerdict` — positionsbaserat betyg, läser aldrig `boardPatience` | `boardService.ts` | VAR-fel-fält | **FIXAT 2026-08-26, Jacobs dom.** Till skillnad från #4 (årsboken, medvetet separat) är detta inkorgskortet ett AKTIVT meddelande — fick en lägesmening TILLAGD efter den oförändrade säsongsdomen (`seasonVerdictZoneLine`, boardService.ts), med det slutgiltiga säsongsslut-uppdaterade `boardPatience`-värdet. Ingen sammanvävning, två satser. |
| 14 | `economicCrisisService` — "Holmström Bygg... elva år" hårdkodat, `game.sponsors` aldrig läst trots att den finns i scope | `economicCrisisService.ts` | ÅTKOMST-FANNS-ANVÄNDES-INTE | **FIXAT (kvitterat 2026-08-27, ej committat).** `economicCrisisService.ts:82-93`. Läser nu `game.sponsors` — väljer aktiv sponsor (`contractRounds > 0`) med högst `weeklyIncome`, `'Huvudsponsorn'` som fallback. "Elva år"-tenure struket helt (inte ersatt med gissning) — `Sponsor.signedRound` finns men dess scoping (global/per-säsong) var overifierad, så uteslutning valdes framför en påhittad siffra. |
| 15 | `eventFactories.hesitantPlayerEvent` — reputation-jämförelsen `contentContract.ts` dokumenterar som avsedd trigger görs aldrig | `eventFactories.ts` | VAR-fel-fält | **FIXAT (kvitterat 2026-08-27, ej committat).** `postAdvanceEvents.ts:528-533`: `buyingClub.reputation >= sellingClub.reputation` gate infört (skippar om budgivarens rykte inte är lägre), exakt matchande `contentContract.ts:222`s dokumenterade trigger. Tidigare gate jämförde `currentAbility` mot truppsnitt — fel fält, ingen reputation-koll alls. |
| 16 | `postAdvanceEvents.buildSponsorOfferEvent` — rivalTenureLine, ovillkorlig ekonomisk jämförelse utan underlag | `postAdvanceEvents.ts` | SANNINGEN-SAKNAS | **FIXAT 2026-08-26, Jacobs dom.** "{RivalSponsor} var med när det var tunnare än nu." struken (obelagt, samma princip som #1). Ny låst text: "{RivalSponsor} får beskedet av er." — sant i nuet, kräver ingen tenure-data. |
| 17 | `matchCore.simulateMatchCore` legend_goal-kommentar — `careerStats.totalGoals` uppdateras EFTER matchen, kommentaren under matchen saknar matchens egna redan gjorda mål | `matchCore.ts` | NÄR-mutation | **FIXAT (kvitterat 2026-08-27, ej committat, dock daterad 2026-08-25 i kod-kommentar).** `matchCore.ts:290-320`: `pickLegendCommentary` beräknar nu `(player.careerStats?.totalGoals ?? 0) + goalsThisMatchSoFar` — adderar matchens egen löpande tally (uppdaterad av `trackGoal()` innan kommentaren avfyras) till den frusna försäsongsbasen. |
| 18 | `matchMoodService.getFinalWhistleSummary`/`getMatchHeadline` — noll produktionsanrop, möjlig text-utan-yta | `matchMoodService.ts` | ANNAT (orphan, äganderättsbeslut) | **RADERAT 2026-08-26.** Superseterad, inte text-utan-yta (CLAUDE.md §7) — `generateQuickSummary` (granska/helpers.ts) och `generatePostMatchHeadline` (journalistService.ts) gör redan samma jobb live, ur riktig fixture-data. |
| 19 | `narrativeService.generateFirstGoalEntry` — aldrig wire:ad, ingen inbyggd koll mot spelarens målhistorik | `narrativeService.ts` | MISSING-GATE + ÅTKOMST-FANNS-ANVÄNDES-INTE | **FIXAT (kvitterat 2026-08-27, ej committat).** `statsProcessor.ts:182-191`: anropas nu bakom `prevCareerGoals === 0 && goals >= 1`, där `prevCareerGoals = p.careerStats.totalGoals` (rad 77) — riktig data. Regressionstest: `statsProcessorFirstGoal.test.ts`. |
| 20 | `playoffNarrativeService.generateSemiFinalEvent` — "första gången" utan koll mot mecenatens historik | `playoffNarrativeService.ts` | NÄR-idempotens | **FIXAT 2026-08-26.** "Det är första gången hen säger något sådant" struken — samma princip som #1/#16, koden kollade bara att en aktiv mecenat finns, aldrig historiken. Resten av repliken (offert om finalbiljetter) orörd. |
| 21 | `seasonSignatureService.summarizeSignature` — ogated hårdkodad fallback när `observedFacts` är tom | `seasonSignatureService.ts` | ANNAT (lågrisk edge-case) | **FIXAT 2026-08-26.** Bekräftat oåtkomlig i produktion idag (createSeasonSignature seedar alltid ≥1 faktum) — fixad ändå som billig försäkring. Fallbacken hittade tidigare på ett specifikt faktum ("Is och minusgrader satte tonen"); returnerar nu bara rubrikmeningen ensam om inget faktum observerats, ingen påhittad text. |
| 22 | `seasonSummaryService.computeKeyMoments` — spelarnamn slås upp i klubb-filtrerad lista, missar sålda spelare | `seasonSummaryService.ts` | ÅTKOMST-FANNS-ANVÄNDES-INTE | **FIXAT (kvitterat 2026-08-27, ej committat).** `seasonSummaryService.ts:185-186,202-203`: hattrick/sent-avgörande-uppslag läser nu `game.players.find(...)` (hela rostern), inte den klubb-filtrerade `managedPlayers`-parametern. Sålda spelares namn resolveras nu korrekt. |
| 23 | `seasonSummaryService.generateSeasonSummary` — `topScorer`/`topAssister`/m.fl. beräknas bara över spelare kvar VID säsongsslut, missar sålda spelare | `seasonSummaryService.ts` | VAR-fel-entitet | **FIXAT, MED EN DOKUMENTERAD KVARSTÅENDE LUCKA (kvitterat 2026-08-27, ej committat).** `seasonSummaryService.ts:262-311,463-495`: topScorer/topAssister/topRated härleds nu ur `clubFixtures`s MatchEvent/playerRatings (säsongslångt, händelsekällat) istf `player.seasonStats`/`managedPlayers` — sålda spelare räknas nu med. KVAR: `mostImproved` (rad 498-510) filtrerar fortfarande på `managedPlayers` (rostern vid säsongsslut) — dokumenterat explicit (rad 359-362) som en känd, ofixad avvikelse som kräver en säsongsstart-truppsnapshot. Var en del av original-fyndets "m.fl." men täcktes inte av denna fix. |
| 24 | `situationService.getSituation` — segersvit/förlustsvit cappas av ett 5-matchers fönster satt FÖRE strecket räknas | `situationService.ts` | NÄR-fönster | **FIXAT 2026-08-26.** Räknas nu ur hela `completedLeague` (obegränsad), samma mönster som `csPressEventService.ts`s redan korrekta `computeCSStreak`. 3 nya tester bevisar en 7- och 12-matchers svit nu visas korrekt (tidigare cappade båda till "5 RAKA"). Samma bugg finns kvar, oåtgärdad, i `mediaService.ts:132` — loggad i BACKLOG.md (E-M24-1), out-of-scope för denna namngivna fil. |
| 25 | `weeklyDecisionService` ismaskin-raden — "tre vintrar" hårdkodat, ingen tjänstetid-räknare finns | `weeklyDecisionService.ts` | SANNINGEN-SAKNAS | **FIXAT 2026-08-26, Jacobs dom.** "Tre vintrar" → "många vintrar". `supporterGroup.founded`-proxyn avvisades explicit (mäter klackens ålder, inte hans — falsk precision). Tredje efterfrågan på en funktionärs-/spelar-tenure-räknare, loggad i BACKLOG.md. |

**Kvitteringsnot (2026-08-27):** samtliga 14 rader ovan verifierade direkt mot koden i arbetsträdet, funktion för funktion. **INGEN av fixarna har en sha** — `git log` visar HEAD fortfarande på `5c9a7a8b` (2026-08-24), och `git status` visar 203 filer / ~94 600 rader okommitterat. Alla "FIXAT 2026-08-26/27"-rader existerar bara i arbetsträdet. Utfall: 11 bekräftat fixade, 1 delvis fixad (#2, hattrick-vägen klar men mallfallbacken kvar), 1 avgjord som "inte en bugg" (#4), 1 fortfarande obekräftat trasig (#12). Se `docs/BACKLOG.md` för risken i att så mycket arbete ligger okommitterat.

**Utöver dessa:** `trainerArcService.updateTrainerArc` sorterar fortfarande på `roundNumber` (fungerar korrekt idag av en dokumenterad schema-koinsidens, men är den uttryckligen förbjudna genvägen enligt CLAUDE.md — bör bytas till `matchday` som hygien, inte som en akut bugg). Tre owired-men-korrekta funktioner (`generateFirstGoalEntry` delvis, `getAwayTripNarrative`, `getFinalWhistleSummary`) är text-utan-yta (§7 i CLAUDE.md) — dödmarkera eller wire:a, radera inte.

## Fördelning per art (de 23 NYA fynden, exklusive de 2 redan kända)

| Art | Antal |
|---|---|
| SANNINGEN-SAKNAS | 7 |
| VAR-fel-fält | 1 |
| VAR-fel-entitet | 2 |
| NÄR-mutation (inkl. delayed-fire-variant) | 3 |
| NÄR-idempotens | 1 |
| NÄR-fönster | 1 |
| ÅTKOMST-FANNS-ANVÄNDES-INTE | 5 |
| ANNAT (design-fråga/lågrisk/orphan-beslut) | 3 |

**SANNINGEN-SAKNAS (7 av 23, störst enskild grupp) är en art varken den ursprungliga VAR-tesen eller NÄR-omdiagnosen namngav som egen kategori** — det är varken "läser fel fält" (inget fält finns att läsa fel) eller "läser rätt fält vid fel tid" (inget fält finns alls). Motorn måste börja skriva ner sanningen innan någon citat-grind kan kräva den. Detta var redan namngivet i den ursprungliga PASTAENDEKARTAN.md ("Kan påståendet inte beläggas ska det inte göras") men syns nu som den STÖRSTA gruppen i en fullständig, mekaniskt genererad sweep — värt att notera för nästa persons prioritering.

## Rådata

Full detalj per funktion (samtliga 85, inklusive alla Sanning/EJ PÅSTÅENDE-rader med motivering) i `batch-01.md` till `batch-08.md` i samma katalog.
