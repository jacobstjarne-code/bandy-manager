# PÅSTÅENDESVEP — Batch 8

Agent 8 av 8. Läst källkod (inte gissat på funktionsnamn) för samtliga åtta tilldelade funktioner.

| ID | Fil:rad | Funktion | Vad den påstår | Fält den läser | Sanning eller proxy? | Art | Föreslagen fix |
|---|---|---|---|---|---|---|---|
| `situationFragments.getSeasonPhaseFragment` | `src/domain/services/situationFragments.ts:214-227` | `getSeasonPhaseFragment()` | EJ PÅSTÅENDE — "Säsongen börjar nu"/"Halvtid"/"Sista omgången" är tillståndsvisning av var i säsongen spelaren befinner sig just nu, inte ett påstående om ett specifikt utfall som redan hänt. Ingen preteritum-händelse, inget namn, ingen kausal koppling. | `game.fixtures` filtrerat på `status==='completed' && !isCup` för managed klubb, `.length` | — | — | — |
| `situationService.getSituation` | `src/domain/services/situationService.ts:29-43, 141-176` | `getSituation()` | Påstår segersvit/förlustsvit ("4 RAKA SEGRAR", "3 matcher utan poäng") som ett konkret antal matcher i rad. | `completedLeague` sorteras på `matchday` (rätt fält, B12-korrekt) men `.slice(0,5)` **innan** strecket räknas ut (rad 34) — svitlängden `streak` kan aldrig bli större än 5 även om den verkliga svängen är längre. | Proxy | NÄR-fönster | Räkna svitet mot hela `completedLeague` (ingen slice-cap före streak-loopen), inte bara de senaste 5 matcherna — annars visas "5 raka segrar" när det egentligen var 8. |
| `supporterRituals.getAwayTripNarrative` | `src/domain/services/supporterRituals.ts:134-173` | `getAwayTripNarrative()` | Påstår vad som hände på bortaresan (vann/förlorade, stämning i bussen hem) baserat på matchutfallet. | `won: boolean`-parametern som skickas in av anroparen — funktionen själv gör ingen egen slagning mot `homeScore`/`awayScore`. | Sanning (given korrekt anrop) | — | Redan korrekt i sin egen scope — men `grep -rn "getAwayTripNarrative"` visar noll anropsställen i hela `src/`. Funktionen är oanvänd/owired (text-utan-yta, Princip 7 i CLAUDE.md) — koppla in den vid en faktisk bortamatch-scen, radera inte. |
| `trainerArcService.updateTrainerArc` | `src/domain/services/trainerArcService.ts:29-59, 89-107` | `updateTrainerArc()` | Påstår tränarens form ("4 raka förluster", "5 raka segrar") som skrivs som `reason` i `arc.history`-transitions och som `consecutiveWins`/`consecutiveLosses` — konsumeras vidare av `klackPresenter.ts` och `roundCharacter.ts` för spelarnära citat om segersvit/förlustsvit. | Sorterar/filtrerar på `f.roundNumber` (rad 42, 94) för att hitta "senaste matchen" — **inte** `matchday`, trots att `CLAUDE.md` uttryckligen förbjuder `roundNumber` för ordning ("Använd ALDRIG roundNumber... All ordning via matchday", `Fixture.ts:111` "matchday // global play order — sort by this"). | Sanning (funktionellt, i nuvarande schema) | — (verifierat: ligarundor har `roundNumber` 1-22 oberoende av cupinflikning, slutspel fortsätter `roundNumber` från 23 utan reset — se `playoffService.ts:54,85` och `scheduleGenerator.ts:48`, så ordningen råkar sammanfalla med matchday för alla icke-cup-fixtures) | Byt ändå `roundNumber`→`matchday` i sorteringarna på rad 42 och 94 (samt `md`-beräkningen rad 34-36) — inte för att det är fel idag, utan för att det är exakt den förbjudna genvägen och en framtida schemaändring (t.ex. omspel, extra rundor) kan tysta bryta ordningen utan att något test fångar det. |
| `transferService.buildTransferStory` | `src/domain/services/transferService.ts:26-49` (flaggor satta `297-303`) | `buildTransferStory()` | Påstår varför en spelares avgång är historiskt betydelsefull — kapten, klackfavorit, fostrad i akademin, "epok över", oavslutad story-arc. | `game.captainPlayerId === soldPlayer.id`, `game.supporterGroup?.favoritePlayerId`, `game.activeArcs` (fas ≠ 'resolving'), `soldPlayer.careerStats.totalGames/totalGoals`, `soldPlayer.isHomegrown && academyClubId === managedClubId` | Sanning | — | Redan korrekt — samtliga fem flaggor läser verkligt nedskrivna fält på rätt entitet (spelaren som faktiskt säljs), inte proxies. |
| `weeklyDecisionService.makeDecisions` | `src/domain/services/weeklyDecisionService.ts:47-201` (ismaskin-raden `146-153`) | `makeDecisions()` | Merparten av poolen är frågor om framtida beslut ("Bidra med 3000 kr?", "Acceptera?") — EJ PÅSTÅENDE (uppmaning/framtid). Undantag: `ismaskin_offer`-frågan påstår "{veteran} har spolat isen för hand i tre vintrar och frågar varje vecka" — ett konkret talpåstående om vad som hänt. | Ingen — "tre vintrar" är en hårdkodad textsträng, inte kopplad till något fält (ingen `veteran`-tjänstetid, inget säsongsräknat värde). | Proxy (för ismaskin-raden) | SANNINGEN-SAKNAS | Antingen skriv om raden till en tidlös observation utan siffra, eller inför ett verkligt fält (t.ex. antal säsonger karaktären funnits i klacken) och citera det — annars är "tre vintrar" en gissning maskerad som fakta. |
| `nextOpponentHookText.buildNextOpponentHook` | `src/domain/data/nextOpponentHookText.ts:30-62` | `buildNextOpponentHook()` | Påstår motståndarens obesegrade hemmasvit, förra mötets resultat denna säsong, poängskillnad i tabellen. | `facts: NextOpponentTeaserFacts` — verifierat i `nextOpponentTeaserService.ts:49-116`: `opponentUnbeatenStreakAtVenue`/`previousMeetingThisSeason` beräknas från `game.fixtures` sorterat på `matchday` (rad 63, 91), poäng/position från `game.standings`. | Sanning | — | Redan korrekt — hela kedjan från fixture till renderad rad går via matchday-sorterad, verkligt spelad data. |
| `journalistRelationshipScene.buildOutlookText` | `src/domain/data/scenes/journalistRelationshipScene.ts:47-62` | `buildOutlookText()` | Merparten är rådgivande/framtid ("Fortsätt svara ärligt", "Det krävs tid") — EJ PÅSTÅENDE. Konkret undantag: "{refusals} nekade presskonferenser. Det syns i rubrikerna." | `journalist.pressRefusals` | Sanning | — | Redan korrekt — `pressRefusals` är en verklig, kumulativ räknare som inkrementeras i `journalistService.ts:70` (`recordPressRefusal`) och medvetet aldrig nollställs (kommentar rad 197 samma fil). |

## Sammanfattning

8 funktioner granskade. 2 rena Sanning utan anmärkning (`buildTransferStory`, `buildNextOpponentHook`), 2 funktioner där huvuddelen är EJ PÅSTÅENDE men innehåller en Sanning-rad (`buildOutlookText`) respektive en Proxy-rad (`makeDecisions`/ismaskin), 1 ren EJ PÅSTÅENDE (`getSeasonPhaseFragment`), 1 verifierat Sanning trots konventionsbrott (`updateTrainerArc` — läser `roundNumber` istf `matchday`, fungerar idag men är den förbjudna genvägen), 1 owired Sanning (`getAwayTripNarrative` — korrekt logik men noll anropsställen, text-utan-yta), och 1 bekräftad Proxy-bugg (`getSituation` — svitlängd cappas av ett 5-matchers fönster som sätts innan strecket räknas, NÄR-fönster).
