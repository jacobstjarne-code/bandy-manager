# PÅSTÅENDEKARTAN — Batch 4

Agent 4 av 8. Läsning gjord i faktisk källkod (öppnad och spårad), inte gissning ur funktionsnamn.

| ID | Fil:rad | Funktion | Vad den påstår | Fält den läser | Sanning eller proxy? | Art | Föreslagen fix |
|---|---|---|---|---|---|---|---|
| `boardService.generateSeasonVerdict` | `src/domain/services/boardService.ts:417-448` (anropas `src/application/useCases/seasonEndProcessor.ts:97`) | `generateSeasonVerdict` | "Styrelsen ger dig betyget X av 5" — ett explicit påstående om hur nöjd styrelsen är med säsongen, pushat till inboxen som `BoardFeedback`. | `computeSeasonVerdictRating(expectation, finalPosition, totalTeams)` — enbart `finalPosition` och `boardExpectation` denna säsong. Läser aldrig `game.boardPatience` (den ackumulerade, löpande tålamods-mätaren som `updateRunningBoardPatience`/`computeBoardPatienceUpdate` i samma fil underhåller). | Proxy | VAR-fel-fält | Detta är exakt radens "Styrelsens dom"-fynd i PÅSTÅENDEKARTAN.md (rad 34): positionsbaserat betyg används som stand-in för `boardPatience`. Läs `game.boardPatience` (eller ett derivat av den) i texten, eller döp om betyget till vad det faktiskt mäter ("måluppfyllelse denna säsong") och lägg `boardPatience` som en egen, separat rad i samma inbox-meddelande. |
| `coffeeRoomService.pickCoffeeRoomLegendReaction` | `src/domain/services/coffeeRoomService.ts:411-443` | `pickCoffeeRoomLegendReaction` | Grammatiskt uppfyller poolen "omfattas"-kravet (namn + preteritum: "{legend.name} var nere på akademiträningen igår", "köpte korv", "ringer fortfarande när vi vinner" osv). | `legend.name`, `legend.role` (för att välja pool: allmän/ungdomstränare/scout) — själva den konkreta handlingen (var på akademin, köpte korv) läses inte från något spårat fält. | — | — | EJ PÅSTÅENDE — motivering: detta är ambient bifigur-flavour om en legend-karaktär, strukturellt identiskt med annan bakgrundsdialog i samma fil (klackecho, kioskvakt-repliker). Ingen sanning är avsedd att finnas nedskriven för en legends dagliga aktiviteter (ingen "akademiträning-logg" existerar eller borde existera) — jämförbart med atmosfär, inte ett verifierbart utfallspåstående kopplat till spelmekanik. Flaggas ändå här eftersom den bokstavliga definitionen (namn+preteritum) träffar — om detta bedöms fel, bör texten skrivas om till presens/generisk karaktärisering för att undvika skenbar specificitet. |
| `contextualSponsorService.checkContextualSponsors#top4` | `src/domain/services/contextualSponsorService.ts:33-54` | `checkContextualSponsors` (top4-grenen) | "Topplaceringen vid halvtid har inte gått obemärkt förbi" — sponsorerbjudande motiverat av lagets tabellplacering vid omgång 11. | `standings.find(s => s.clubId === managedClubId)?.position`, gated på `currentRound === 11` (matchar "halvtid" i texten). | Sanning | Redan korrekt | — |
| `contextualSponsorService.checkContextualSponsors#cs70` | `src/domain/services/contextualSponsorService.ts:56-82` | `checkContextualSponsors` (cs_over_70-grenen) | "Kommunen erkänner klubbens roll i orten och beviljar 80 000 kr" — kommunalt engångsstöd motiverat av samhällsanseende. | `game.communityStanding` (`cs`), gated `cs > 70` vid specifika omgångar. | Sanning | Redan korrekt | — |
| `contextualSponsorService.checkContextualSponsors#attendance` | `src/domain/services/contextualSponsorService.ts:84-118` | `checkContextualSponsors` (attendance_1000-grenen) | "Snittet på {avgAttendance} åskådare per hemmamatch har väckt intresse" — cateringsponsor motiverad av publiksnitt. | `avgAttendance` beräknat direkt från `game.fixtures` (completed, home, `attendance`-fält) upp till `currentRound`. | Sanning | Redan korrekt | — |
| `cornerInteractionService.resolveCorner` | `src/domain/services/cornerInteractionService.ts:31-117` | `resolveCorner` | Beskriver hörnutfallet ("MÅL! ... {spelare} avslutade", "Försvaret klarerade direkt" osv), inklusive `scorerId`/`scorerName`. | Genererar utfallet själv via `rand()`-tärning mot beräknad `goalChance`; `scorerId`/`scorerName` sätts till samma `bestRusher.player` som avgjorde tärningsslaget. | — | — | EJ PÅSTÅENDE — detta är själva händelsegenereringen (en del av matchmotorn), inte en yta som i efterhand citerar/renderar en redan skedd händelse. Per B12-principen ("matchhändelserna är sanningen") är denna funktion producenten av sanningen, inte en konsument av en proxy — description skrivs samtidigt som utfallet avgörs. |
| `economicCrisisService.checkEconomicCrisis` (fas 2 — "pressure") | `src/domain/services/economicCrisisService.ts:73-75` | `checkEconomicCrisis` | "Vi har varit med i elva år" — Holmström Bygg (huvudsponsorn) hotar lämna klubben efter elva års partnerskap. | Ingenting — `mec`/sponsor-namnet "Holmström Bygg" och "elva år" är hårdkodade strängar i eventtexten. Funktionen har `game: SaveGame` (inklusive `game.sponsors`, som har `signedRound` — den faktiska varaktigheten för en riktig sponsor) i scope men läser den aldrig. | Proxy | ÅTKOMST-FANNS-ANVÄNDES-INTE | Om klubben har en riktig huvudsponsor i `game.sponsors` (tier `'fixed'`/högst `weeklyIncome`), citera dess `name` och beräkna faktisk varaktighet från `signedRound`/`game.currentSeason` istället för att hårdkoda "Holmström Bygg" och "elva år". Om ingen sådan koncept finns ännu — skriv ner en huvudsponsor-relation i motorn (vem, sedan när) innan eventet citerar den. |
| `communityActivitiesEvents.generateCommunityActivitiesEvents` | `src/domain/services/events/communityActivitiesEvents.ts:4-355` | `generateCommunityActivitiesEvents` | Samtliga elva grenar (kiosk, lotteri, julmarknad, loppis, bandyskola, ismaskin, funktionärsdag, fikakväll, bilbingo, anläggning) erbjuder ett FRAMTIDA val ("Vill ni starta...", "Kräver en dag men ger...") — inget påstår ett redan inträffat utfall. | `game.communityActivities`-flaggor (kiosk/lottery/julmarknad/bandyplay/functionaries) styr bara VILKEN fråga som visas, inte något faktapåstående om vad som hänt. | — | — | EJ PÅSTÅENDE — ren beslutsmeny/erbjudande-generator (uppmaningar och avsiktstext), matchar undantaget "framtid/avsikt" och "frågor/uppmaningar" i kartans definition rakt av. |
| `eventFactories.hesitantPlayerEvent` | `src/domain/services/events/eventFactories.ts:40-68` (gate: `src/domain/services/events/postAdvanceEvents.ts:507-524`) | `hesitantPlayerEvent` | "{playerName} är intresserad men tveksam — din klubb är ett steg ner i ambitionsnivå." — påstår att köpande klubb har lägre ambition/anseende än spelarens tidigare klubb. | Ingenting i själva funktionen jämför klubbarnas `reputation`. Anropsstället (`postAdvanceEvents.ts`) gatar bara på `target.currentAbility > avgCA` (spelarens förmåga vs. den egna truppens snitt) — ingen reputation-jämförelse görs alls, trots att `contentContract.ts:222` dokumenterar den avsedda triggern som "bid.buyingClubId har lägre reputation än spelarens nuvarande klubb". | Proxy | VAR-fel-fält (dokumenterad avsedd trigger använder `reputation`, koden läser aldrig `club.reputation` för vare sig säljande eller köpande klubb) | Läs `game.clubs.find(c => c.id === sellingClubId)?.reputation` och `game.clubs.find(c => c.id === bid.buyingClubId)?.reputation` — antingen i gaten (`postAdvanceEvents.ts`) eller i själva funktionen — och villkora eventet/texten på att köpande klubb faktiskt har lägre reputation, i linje med `contentContract.ts`s dokumenterade kontrakt. |
| `eventFactories.contractRequestEvent` | `src/domain/services/events/eventFactories.ts:120-156` | `contractRequestEvent` | "Nuvarande kontrakt löper ut efter säsong {N}." | `player.contractUntilSeason` — direkt läsning av det verkliga kontraktsfältet. | Sanning | Redan korrekt | — |
| `eventFactories.createEconomicStressEvent` | `src/domain/services/events/eventFactories.ts:608-655` | `createEconomicStressEvent` | Tre mikrobeslut (klubbor, buss, korvavtal) — situationsbeskrivningar som motiverar ett kommande val ("Fem klubbor gick sönder i veckan..."), inte påståenden om ett redan verifierat utfall. | `game.clubs.find(...).finances` (bara för gate `finances >= 50000`/`< -100000`), `game.lastEconomicStressRound` för cooldown. Själva flavour-texten (klubbor/buss/korv) är hårdkodad, inte kopplad till något spårat "klubb-slitage"-fält. | — | — | EJ PÅSTÅENDE — situationsflavour som ramar in en framtida mikroanskaffning/förhandling (samma mönster som `communityActivitiesEvents`), ingen konkret utfallsclaim. |
| `eventResolver.resolveEvent` | `src/domain/services/events/eventResolver.ts:32-2089` (exempel: sell-grenen `1470-1505`) | `resolveEvent` | Dispatcher/mutator för samtliga event-effekter — applicerar tillståndsändringar och skriver ibland åtföljande inbox-text (t.ex. "Akademijuvel säljs", sponsor-accept-texter). | Läser `event`/`choice` samt muterar `game` direkt (spelare, klubbar, sponsorer, board, m.m.); H3-fixet (rad 1470-1505) lade till en HÅRD assertion som kastar om mutationen (spelaren borttagen ur truppen) inte faktiskt genomfördes innan `Årsboken` senare kan citera den. | Sanning | Redan korrekt | Detta är motorns skrivpunkt för sanningen (B12-principen), inte en yta som citerar en proxy — `recordResolvedChoice` (rad 27-29) skriver `resolvedChoices` på alla fem exit-punkter som den nedskrivna sanningen andra ytor (Årsboken via `seasonDecisionCaptureService.ts`) ska citera. Ingen fix behövs i denna funktion; eventuella proxy-fynd hör hemma i de KONSUMERANDE ytorna (redan täckt av H3/O18-fixet för sell-grenen). |
| `hallProcessService.buildForankringEvent` | `src/domain/services/events/hallProcessService.ts:144-260+` (resolution: rad 158-198) | `buildForankringEvent` | "Röstlängden är räknad. Det blev ja." / bordlagd / nedlagd — slutresultatet av en hallförankringsprocess. | `finalSupport = clamp((trial.support ?? 50) + calcPassiveSupportDelta(...), 0, 100)` — `trial.support` är det verkliga, ackumulerade stödfältet; `calcPassiveSupportDelta` läser riktiga matchresultat via `f.matchday` (korrekt fält, inte `roundNumber`) inom processfönstret. | Sanning | Redan korrekt | — |

## Sammanfattning

11 tilldelade funktioner, 14 rader (tre funktioner gav flera distinkta påståenden). Fördelning: 4 Sanning, 4 EJ PÅSTÅENDE, 3 Proxy (varav 1 redan känt/dokumenterat i kartan — `generateSeasonVerdict` — och 2 nya: `Holmström Bygg`-hårdkodningen i `checkEconomicCrisis` och reputation-gapet i `hesitantPlayerEvent`), 3 rader "redan korrekt" som explicit motiverade Sanning-fall (inklusive `resolveEvent` som helhet).
