# PÅSTÅENDEKARTAN — svep 2026-08-24, batch 7

Agent 7 av 8. Tilldelade funktioner: `schoolAssignmentService.ts`, `seasonGoalService.ts`, `seasonSignatureService.ts`, `seasonSummaryService.ts`, `silentMatchReportService.ts`, `situationFragments.ts`.

| ID | Fil:rad | Funktion | Vad den påstår | Fält den läser | Sanning eller proxy? | Art | Föreslagen fix |
|---|---|---|---|---|---|---|---|
| `schoolAssignmentService.summarizeSeason` | `src/domain/services/schoolAssignmentService.ts:8-13` | `summarizeSeason` | Beskriver en tidigare säsong som "säsongen vi blev SM", "SM-final-säsongen" eller "säsongen vi slutade N:a" — ett påstående om vad som faktiskt hände den säsongen. | `s.playoffResult`, `s.finalPosition`, `s.season` på ett `SeasonSummary`-objekt | Sanning | — | Redan korrekt. `SeasonSummary.playoffResult`/`finalPosition` snapshottas rollover-säkert i `generateSeasonSummary` (samma fil-kluster, se rad 244-291 i `seasonSummaryService.ts`). |
| `schoolAssignmentService.generateSchoolAssignmentEvent` | `src/domain/services/schoolAssignmentService.ts:15-82` | Låter spelaren "berätta" om en tidigare framgångsrik säsong ("Vi slutade X:a i serien och vann SM") eller om en klubblegend ("{Namn} spelade N säsonger och gjorde M mål"). | `notableSeason.finalPosition`/`playoffResult`/`season` (från `game.seasonSummaries`), `clubLegend.name`/`seasons`/`totalGoals` (från `game.clubLegends`) | Sanning | — | Redan korrekt. Båda källorna är skrivna register (`SeasonSummary`-arkivet resp. `ClubLegend`-listan som byggs vid pensionering, se `clubMemoryService.ts`/`coffeeRoomService.ts`), inte proxies. |
| `seasonGoalService.deriveGoalOutcomeLine` | `src/domain/services/seasonGoalService.ts:215-223` | Årsbokens rad: "Du sa {mål} i somras. Du gjorde det / Det saknades inte mycket / Det blev inte så." | `record.outcome` (`SeasonGoalRecord.outcome`, satt av `evaluateSeasonGoal` mot verkliga säsongsslutfält: `game.standings`-position, `seasonStats.gamesPlayed`, matchtabeller mot rivalen, `facilityState`) | Sanning | — | Redan korrekt. Ren rendering av en redan verifierad `SeasonGoalRecord` — `evaluateSeasonGoal` (samma fil, rad 149-185) läser rätt fält vid rätt tillfälle (kommentaren rad 143-147 dokumenterar att `game` måste vara pre-reset). |
| `seasonGoalService.derivePersonChangeLine` | `src/domain/services/seasonGoalService.ts:287-293` | O18 fält 3: "{Namn} la av efter N säsonger" / "kom upp och blev kvar" / "gick från reserv till given." | `change.kind`, `change.name`, `change.seasons` på ett `SeasonPersonChange`-objekt | Sanning | — | Redan korrekt. Ren rendering — `deriveSeasonPersonChange` (samma fil, rad 244-284) har redan en idempotenskoll mot `seasonHistory` inbyggd (kommentaren rad 266-270 dokumenterar att detta specifikt lades till för att fixa en tidigare idempotens-bugg i "reserve→starter"-grenen). |
| `seasonSignatureService.summarizeSignature` | `src/domain/services/seasonSignatureService.ts:94-113` | Säsongsslutets rubricerande mening om säsongens "signatur", t.ex. "Skandalsäsongen {år}. {N} skandaler i ligan de senaste åren." | `signature.id`, `signature.observedFacts[0]` | Delvis | ANNAT | `observedFacts[0]` är i normalfallet en verklig nedskriven rad (`recordSignatureFact` ackumulerar under säsongen). Men om arrayen är tom faller funktionen tillbaka på en hårdkodad generisk mening per signatur-typ (t.ex. "Is och minusgrader satte tonen") som INTE är knuten till något verifierat spelhändelse — den påstår en specifik atmosfär utan källa. Risken är låg (facts[0] sätts alltid vid `createSeasonSignature`) men fallbacken är en ogated gissning. Fix: logga varning eller returnera `null` istället för en påhittad fallback-mening när `observedFacts` är tom, så att en tom array aldrig tyst blir ett konkret påstående. |
| `seasonSummaryService.generateStoryTriggers` | `src/domain/services/seasonSummaryService.ts:12-69` | Pekar ut en "akademistjärna", "hattrick-hjälte" eller "comebackkung" i truppen med konkreta siffror (antal matcher, snittbetyg, mål). | `p.promotedFromAcademy`, `p.seasonStats.gamesPlayed/averageRating/goals`, `p.careerMilestones` (type `hatTrick`, `season`), `p.diary` (type `injury`, `season`), `p.isInjured` | Sanning | — | Redan korrekt. Alla fält är faktiska ackumulerade spelarregister för innevarande säsong, inte proxies. |
| `seasonSummaryService.computeKeyMoments` | `src/domain/services/seasonSummaryService.ts:73-215` | Bygger säsongens "ögonblick": stor seger/förlust, derbyresultat, hattrick av namngiven spelare, sent avgörande mål av namngiven spelare, comeback — allt kopplat till en specifik match och omgång. | `deriveFixtureOutcome`/`countGoalsByPlayer`/`findLateWinnerGoal`/`isComeback` (matchUtils.ts, läser `f.homeScore/awayScore/events` per fixture), sedan `managedPlayers.find(...)` för namn | Delvis | ÅTKOMST-FANNS-ANVÄNDES-INTE | Matchutfallen (marginal, derbystatus, mål-minut) är sanning. Men spelarnamn för hattrick/sent avgörande mål slås upp i den FÖRFILTRERADE `managedPlayers`-listan (spelare med `clubId === managedClubId` VID SÄSONGSSLUT) — en spelare som gjorde hattricket men såldes senare under säsongen hittas inte där och blir "Okänd"/"Avslutning", trots att funktionen redan har hela `game`-objektet (med `game.players`, där spelarens namn fortfarande finns, bara med annan `clubId`) i sitt scope. Fix: slå upp namnet i `game.players` (utan clubId-filter) istället för i den redan klubb-filtrerade `managedPlayers`-parametern. |
| `seasonSummaryService.generateSeasonSummary` | `src/domain/services/seasonSummaryService.ts:219-655` | Hela säsongssammanfattningen: slutplacering, poäng, målskillnad, slutspelsresultat, styrelsedom, "{Namn} stod för N mål och var lagets viktigaste offensiva kraft" (`topScorer`), bästa assistmakare/betyg/mest förbättrad/bäste U21-spelare, störst seger/förlust, formtrend, ekonomisk förändring m.m. | `game.standings`, `game.playoffBracket` (snapshottat till `championClubId`/`eliminatedByClubId` osv.), `club.boardExpectation`, `clubFixtures` (riktiga matchresultat), samt `managedPlayers = game.players.filter(p => p.clubId === managedClubId)` för `topScorer`/`topAssister`/`topRated`/`mostImproved`/`youngPlayer` | Delvis | VAR-fel-entitet | Merparten (placering, slutspel, matchstatistik, ekonomi) läser rätt snapshottade fält — redan medvetet fixat mot rollover (se kommentarerna rad 246-254 om `championClubId`/`eliminatedByClubId`, skrivna specifikt med PÅSTÅENDEKARTAN i åtanke). Men `topScorer`/`topAssister`/`topRated`/`mostImproved`/`youngPlayer` beräknas bara över spelare som ÄR i klubben vid säsongsslut (`managedPlayers`) — en spelare som gjorde flest mål för klubben men blev SÅLD under säsongen (transferService.ts:263 sätter om `clubId` vid affären) räknas inte, och "lagets viktigaste offensiva kraft"-meningen kan då peka på fel spelare. Fix: räkna säsongens toppscorer m.fl. över alla spelare som HAR haft `clubId === managedClubId` under säsongen (t.ex. via en season-scoped trupplogg), inte bara de som är kvar vid slutsignal. |
| `seasonSummaryService.seasonTwoTruthsSentence` | `src/domain/services/seasonSummaryService.ts:710-735` | Kombinerar placeringsdomen med uppdragsutfallet: "{placeringsdom}, men N uppdrag missades" / "Uppdragen höll ni däremot" / "N uppdrag hängde löst ända in i mars." | `summary.expectationVerdict`, `summary.objectiveOutcome.{met,atRisk,failed}` | Sanning | — | Redan korrekt. Ren rendering av redan beräknade fält på `SeasonSummary` — funktionen själv gör ingen härledning, bara villkorad textval utifrån verifierade utfallssiffror. |
| `silentMatchReportService.generateSilentMatchReport` | `src/domain/services/silentMatchReportService.ts:7-53` | Sportjournalistisk matchrapport: resultat, "{klar/knapp} seger/förlust", vilka halvlekar målen kom i, minuttal. | `fixture.homeScore/awayScore`, `fixture.events` filtrerat på `MatchEventType.Goal` med `e.minute` | Sanning | — | Redan korrekt. Läser faktiska matchhändelser direkt ur fixturen; halvlek/förlängning-gränsen (≤45/≤90/>90) är redan korrigerad enligt kommentaren (M57, textaudit 2026-07-04) för att matcha B12:s minutkonvention. |
| `situationFragments.getLastMeetingFragment` | `src/domain/services/situationFragments.ts:82-106` | "Senast ni möttes vann ni {X}–{Y}" / "Senast: {Y}–{X} till dem" / "Senaste mötet {X}–{X}." om senaste inbördesmötet mot kommande motståndare. | Senaste `completed`-fixturen mellan `managedId` och `oppId` (sorterad på `matchday` fallande), `homeScore`/`awayScore` med korrekt `isHome`-attribution | Sanning | — | Redan korrekt. Rätt fixture (senaste avgjorda mötet, matchday-sorterad enligt CLAUDE.md:s arkitekturregel), rätt entitet (kollar `isHome` innan poäng attribueras till "ni" vs "dem"). |
