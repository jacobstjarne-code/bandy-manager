# PÅSTÅENDEKARTAN — Batch 6

Läst faktisk källkod för samtliga 11 tilldelade funktioner (ingen gissning utifrån filnamn).

| ID | Fil:rad | Funktion | Vad den påstår | Fält den läser | Sanning eller proxy? | Art | Föreslagen fix |
|---|---|---|---|---|---|---|---|
| `mecenatDinnerService.generateDinnerScene` | src/domain/services/mecenatDinnerService.ts:47 | `generateDinnerScene` | EJ PÅSTÅENDE — bygger en interaktiv middagsscen (setting + frågor/svarsalternativ) för ett framtida spelarval. Ingen preteritum-utsaga om vad som redan hänt; NPC-repliker som "Jag hörde att det finns intresse..." är fiktiv dialog i scenen, inte ett citerat sakförhållande. | `mec.businessType`, `mec.name` (bara för scensetting) | N/A | — | Redan korrekt (ingen påståendekomponent att fixa). |
| `narrativeService.generateFirstGoalEntry` | src/domain/services/narrativeService.ts:18 | `generateFirstGoalEntry` | Att detta var spelarens FÖRSTA A-lagsmål mot given motståndare. | Inga — funktionen tar bara `opponent/season/matchday` som parametrar, ingen koll mot spelarens mål-historik. Dessutom: 0 anropsställen i hela repot utanför den egna filen (aldrig wire:ad, till skillnad från syskonfunktionerna `generateDebutEntry`/`generateGoodMatchEntry` i samma fil som statsProcessor.ts faktiskt anropar). | Proxy | MISSING-GATE (ingen inbyggd koll) + ÅTKOMST-FANNS-ANVÄNDES-INTE (statsProcessor.ts har redan `prevCareerGoals` tillgängligt för milstolpar men anropar aldrig denna funktion för "första målet") | Antingen wire:a in i statsProcessor.ts med villkor `prevCareerGoals === 0 && goals >= 1` (samma mönster som debut-kollen två rader ovanför), eller ta bort som död kod om "första målet"-raden inte längre är tänkt. |
| `narrativeService.generateDebutEntry` | src/domain/services/narrativeService.ts:46 | `generateDebutEntry` | Att detta var spelarens A-lagsdebut mot given motståndare. | Anropas i statsProcessor.ts:169-170 med villkoret `prevCareerGames === 0 && p.promotedFromAcademy` — `prevCareerGames` = `p.careerStats.totalGames` före denna match. | Sanning | — | Redan korrekt. |
| `narrativeService.generateGoodMatchEntry` | src/domain/services/narrativeService.ts:81 | `generateGoodMatchEntry` | Att spelaren gjorde en storspelad match (visst betyg, visst antal mål) mot given motståndare. | Anropas i statsProcessor.ts:173-177 med `rating = fixture.report?.playerRatings[id]` och `goals` räknat direkt ur `fixture.events` (Goal-events för spelaren i just den matchen) — matchhändelsebaserat, samma princip som B12. | Sanning | — | Redan korrekt. |
| `opponentManagerService.generatePostMatchOpponentQuote` | src/domain/services/opponentManagerService.ts:69 | `generatePostMatchOpponentQuote` | Implicit att motståndarlaget vann/förlorade matchen, och ev. att en skandal påverkat dem. | Läser inte game-state själv — men anroparen (postMatchEventService.ts:37-54) beräknar `theyWon` korrekt ur `fixture.homeScore`/`awayScore` (margin < 0) och `hasScandal` ur `game.scandalHistory` filtrerat på klubb + innevarande säsong. | Sanning | — | Redan korrekt. |
| `playerVoiceService.getPlayerVoice` | src/domain/services/playerVoiceService.ts:25 | `getPlayerVoice` | EJ PÅSTÅENDE — samtliga grenar är tillståndsvisning/atmosfär (skadad, lågt/högt moral, form, kontraktsår, ålder, trait), ingen konkret preteritum-händelse eller siffra som beskriver ett utfall. | `player.isInjured`, `player.morale`, `player.form`, `player.seasonStats.goals`, `player.contractUntilSeason`, `player.age`, `player.trait`, `game.previousMarketValues` | N/A (se dock notering) | — | Redan korrekt som atmosfär. Notering: grenen "Bara varit här en månad" (rad 63-68) använder frånvaro av en `previousMarketValues`-post som proxy för "nyanländ" — det är bara sant den allra första omgången efter övergång och blir en falsk proxy därefter, men eftersom raden är atmosfär utan verifierbart utfall räknas den inte som ett kartan-påstående. Om den ska bli ett riktigt tidsanspråk bör den i stället läsa spelarens faktiska ankomstomgång/säsong. |
| `playoffNarrativeService.generateSemiFinalEvent` | src/domain/services/playoffNarrativeService.ts:31 | `generateSemiFinalEvent` | "Det är första gången hen säger något sådant" — att mecenaten aldrig tidigare lovat detta. | `mecenat.isActive`, `mecenat.name` — ingen historik/flagga kontrolleras för om detta löfte redan getts en tidigare säsong (spelet är flersäsongers, samma mecenat kan vara aktiv i flera semifinal-genomgångar). | Proxy | NÄR-idempotens | Lägg till en koll mot mecenatens historik (t.ex. en flagga `hasPromisedFinalAttendance` på Mecenat-entiteten) innan "första gången"-frasen används, eller ta bort tidsanspråket och gör raden neutral/upprepningsbar. |
| `playoffNarrativeService.generateFinalEvent` | src/domain/services/playoffNarrativeService.ts:47 | `generateFinalEvent` | EJ PÅSTÅENDE — atmosfärisk dialog inför finalen utan konkret, verifierbart utfallspåstående. Namnattributionen är korrekt källad men beskriver ingen historisk händelse. | `game.supporterGroup?.leader?.name` | N/A | — | Redan korrekt. |
| `pickEfterklang.pickEfterklang` | src/domain/services/portal/pickEfterklang.ts:56 | `pickEfterklang` | Flera konkreta historiska delpåståenden beroende på vilken typ som väljs, bl.a. "Ni sålde {spelare} till {klubb}", "{N} mål mot er den här säsongen", "Du missade styrelsens mål förra säsongen", "{N} år sedan {händelse}". | `game.activeAnniversaries` (significance/yearsAgo/originalEventText), `game.klackEcho.currentWeight`, `game.journalist.memory` (sorterad på `matchday`), `game.bandyLetters` (filtrerad på säsong), `game.boardObjectiveHistory` (senaste post, `result === 'failed'`), `game.nemesisTracker` (gated mot att nästa motståndare faktiskt ÄR nemesisen), `game.economicCrisisState` (fas/`resolvedMatchday`/`outcome`), `game.lastRivalSaleInfo`/`lastRivalSaleMatchday`. | Sanning | — | Redan korrekt. Detta är uppenbart den redan åtgärdade versionen av kartans egen "Årsboken"-rad ("Du sålde {Namn}" härledd ur `choiceId`) — rivalSale-grenen läser numera det dedikerade `game.lastRivalSaleInfo`-fältet, inte en choice-proxy. |
| `postVictoryNarrativeService.generateVictoryEcho` | src/domain/services/postVictoryNarrativeService.ts:44 | `generateVictoryEcho` | T.ex. "{score} mot {opponent}" och segertyp-specifika skämt/kommentarer. | `fixture.homeClubId`/`homeScore`/`awayScore` (hemma/borta-normaliserat efter M32-fixen 2026-07-03), `type` (klassificerat i `classifyVictory` från verkligt fixture-data: `matchday`, rivalry, scoreDiff). | Sanning (källmässigt) | — | Redan korrekt källmässigt. Notering utanför kartans scope: filens egen kommentar (rad 8-18) dokumenterar att `diaryLine` är TEXT-UTAN-YTA — tänkta konsumenten (`dailyBriefingService.generateBriefing()`) var själv död kod och texten når aldrig spelaren. Redan dödmarkerad och rapporterad 2026-07-21, kräver ingen ny åtgärd av detta svep. `coffeeLine` är levande sedan D4-fixen. |
| `pressConferenceService.generatePressConference` | src/domain/services/pressConferenceService.ts:671 | `generatePressConference` | Matchutfall (vann/förlorade/oavgjort, straff-/förlängningsmedvetet), dominans, sen kvittering, m.fl. kontextpåståenden beroende på vilken fråga som väljs. | `deriveUtfall(fixture, managedClubId)` (rad 364, straff-/förlängningsmedveten — inte rå score), `fixture.report.shotsHome`/`shotsAway` (dominans, samma tröskel `>4` som matchCore.ts:s `getMatchSituation`, se kommentar rad 454-459), `fixture.events` (`trailedAtHalf`, `lateEqualizer`, `youngsterScored`), `game.narrativeBeatLog` (idempotens för storyline-uppföljningsfrågor, rad 749-755). | Sanning | — | Redan korrekt. Filens egna kommentarer (daterade 2026-08-17/22/24) visar att flera av kartans egna fynd — bl.a. exakt "Pressfrågan / vem som dominerade" — redan är åtgärdade i just denna fil. |

## Sammanfattning
11 funktioner granskade. 1 bekräftad proxy-bugg (`generateSemiFinalEvent` — NÄR-idempotens), 1 orphan/aldrig-wire:ad funktion (`generateFirstGoalEntry` — MISSING-GATE + ÅTKOMST-FANNS-ANVÄNDES-INTE), 1 redan dödmarkerad text-utan-yta (`generateVictoryEcho.diaryLine`, kräver ingen ny åtgärd), 3 EJ PÅSTÅENDE (ren tillståndsvisning/atmosfär), 5 Sanning (korrekt källade, varav flera — `pickEfterklang`, `generatePressConference` — är synliga bevis på att motsvarande kartan-fynd redan åtgärdats i tidigare sessioner samma dag.
