# PÅSTÅENDEKARTAN — Batch 5

Agent 5 av 8. Elva tilldelade funktioner, lästa i sin faktiska källkod (inte gissade ur namn).

| ID | Fil:rad | Funktion | Vad den påstår | Fält den läser | Sanning eller proxy? | Art | Föreslagen fix |
|---|---|---|---|---|---|---|---|
| `patronEvents.generatePatronEvents` | `src/domain/services/events/patronEvents.ts:193` | `generatePatronEvents` (patron_influence_60-grenen) | "{Patron} har bidragit med {totalContributed} kr totalt" — ett ackumulerat historiskt bidragsbelopp. | `patronGame.totalContributed` (fallback `patronGame.contribution`) | Sanning | — | Redan korrekt. `totalContributed` är ett äkta löpande register (`Mecenat`/`Patron`-entiteten, `Community.ts:40`), inkrementeras `newTotalContributed = (patron.totalContributed ?? 0) + patron.contribution` vid varje säsongsslut (`seasonEndProcessor.ts:887-903`). Fallbacken `?? patron.contribution` är rimlig för patroner som ännu inte hunnit ett säsongsslut. |
| `postAdvanceEvents.generatePostAdvanceEvents` | `src/domain/services/events/postAdvanceEvents.ts:389-413` | `generatePostAdvanceEvents` (kaptenens why-now, "5h. Captain speech") | "Kaptenen håller tal efter tre raka förluster" — en kausal koppling mellan förlustsvit och händelsen. | `game.fixtures` filtrerat på `status==='completed' && !f.isCup`, sorterat `(b.matchday ?? 0) - (a.matchday ?? 0)`, `.slice(0,3)`, `.every(loss)` | Sanning | — | Redan korrekt (H2-uppföljning 2026-08-24, kommentar rad 376-388 i filen). Detta ÄR exakt kartans egen rad "Kaptenens why-now / härledde ur roundNumber-sortering" — bevisligen redan fixad till `matchday`-sortering i denna funktion. De tre andra sorteringarna i samma fil (unhappy players rad 140-143, day-job rad 204-210, player-media rad 334-337) använder samma korrekta `matchday`-mönster. |
| `postAdvanceEvents.buildSponsorOfferEvent` | `src/domain/services/events/postAdvanceEvents.ts:702` | `buildSponsorOfferEvent` (rivalTenureLine) | "{RivalSponsor} var med när det var tunnare än nu." — ett preteritum-påstående om att rivalsponsorn fanns med under en tidigare, ekonomiskt svagare period jämfört med nu. | `rivalSponsor` (bara att en aktiv sponsor i samma kategori finns — `activeSponsors.find(s => s.category === offer.category)`) | Proxy | SANNINGEN-SAKNAS | Ingen ekonomisk historik (klubbens finances vid den tidpunkt rivalsponsorn tecknades, jämfört med nu) finns nedskriven någonstans i kodbasen — raden visas ovillkorligt så fort en rivalsponsor existerar, oavsett om klubben faktiskt var svagare då. Rad är låst Jacob-text (SPEC-LYDNAD, kommentar rad 697-701) — kräver Opus/Jacob-beslut, inte en tyst Code-ändring: antingen skriv ner sponsorns tecknings-säsong + klubbens `reputation`/`finances` vid den tidpunkten och gatea raden på en faktisk jämförelse, eller mjuka upp texten till något som inte hävdar en specifik historisk jämförelse. |
| `sponsorEvents.generateSponsorEvents` | `src/domain/services/events/sponsorEvents.ts:4-45` | `generateSponsorEvents` (ICA Maxi-event) | EJ PÅSTÅENDE | — | — | — | Funktionen erbjuder ett återkommande (var 4:e omgång) affärstillfälle ("ICA Maxi erbjuder 5 000 kr extra"). Ingen preteritum-händelse, inget utfallstal om vad som HÄNT — det är ett stående erbjudande gatead på att en ICA Maxi-sponsor finns (`s.icaMaxi === true && s.contractRounds > 0`), korrekt läst state. |
| `freeKickInteractionService.resolveFreeKick` | `src/domain/services/freeKickInteractionService.ts:21-64` | `resolveFreeKick` | "Frislaget blev mål/räddning/utanför/hörna" — beskriver frislagets utfall. | Egna beräknade `rand()`-utfall mot `kicker.attributes.shooting/passing`, `gk.attributes.goalkeeping`, `data.distanceMeters`, `data.wallSize` | Sanning | — | Redan korrekt. Funktionen är inte en citerande yta utan SJÄLVA källan till sanningen (B12-principen: "matchhändelserna är sanningen") — `type` och `description` returneras tillsammans från samma beräkning i samma anrop, så de kan aldrig divergera. |
| `inboxService.createTrainingItem` | `src/domain/services/inboxService.ts:326-355` | `createTrainingItem` | "Omgång {roundNumber}: {typeLabel} ({intensityLabel})" + "{Spelare} skadades under träning." | `focus`, `roundNumber`, `injuredPlayers` — alla skickas in av anroparen | Sanning | — | Redan korrekt. Anroparen (`trainingProcessor.ts:56-66`) skickar in `trainingResult.injuredPlayerIds` direkt från samma `applyTrainingToSquad`-anrop som just beräknat träningsskadorna, och `nextRound` är omgången som faktiskt processas just nu (ingen stale sortering inblandad, ingen matchday/roundNumber-blandning eftersom det bara är en enkel etikett, inte en ordning över fixtures). |
| `insandareService.generateInsandare` | `src/domain/services/insandareService.ts:40-99` | `generateInsandare` | En läsarinsändares reaktion (positiv/negativ/reflektiv) på senaste matchens resultat och marginal, samt om det var derby. | `lastFixture.homeScore`/`awayScore` (via `myScore`/`theirScore`/`margin`), `getRivalry(homeClubId, awayClubId)` | Sanning | — | Redan korrekt. Läser matchens faktiska slutresultat direkt från fixturen och en äkta rivalitets-lookup, inte en proxy. Determinismen (`fixtureHash`) är för renderingsstabilitet, inte en datakälla. |
| `klackPresenter.pickQuote` | `src/domain/services/klackPresenter.ts:52-144` | `pickQuote` | EJ PÅSTÅENDE | `mood`, `consecutiveWins`, `consecutiveLosses` (skickas in av anroparen, `getKlackDisplay` från `game.trainerArc`) | — | — | Citaten är atmosfärisk supporterdialog ("Senast vi vann tre i rad köpte jag en ny fana") — ingen av replikerna hävdar ett specifikt, verifierbart utfall (inget namn i en händelsebeskrivning, inget utfallstal). Gate-villkoren (`win`/`lose`/`sad`/`happy`) läser dock redan de korrekta fälten (verkliga streak-/mood-räknare), så om man ändå vill räkna urvalslogiken som ett påstående är den Sanning. |
| `matchCore.simulateMatchCore` | `src/domain/services/matchCore.ts:1574-1576` (legend_goal-grenen, textpool `matchCommentary.ts:844`) | `simulateMatchCore` | "{totalGoals} mål för klubben nu" — ett specifikt utfallstal om spelarens ackumulerade karriärmål för klubben, sagt i nutid ("nu") vid ett mål-event mitt i matchen. | `player.careerStats?.totalGoals` (läst via `pickLegendCommentary`, rad 304) — samma player-referens hela matchen igenom | Proxy | NÄR-mutation | `careerStats.totalGoals` uppdateras ENDAST av `statsProcessor.ts` (rad 75-77), som körs EFTER att hela matchen är simulerad och resultatet processats — aldrig under själva matchsimuleringen. Legend-kommentaren "{totalGoals} mål för klubben nu" visas alltså med förra-matchens värde: om detta är legend-spelarens första mål i matchen saknas +1, om det är hens andra+ mål i SAMMA match saknas ytterligare mål. Fix: antingen räkna in matchens hittills-gjorda mål lokalt i generatorn (`scorerPlayer.careerStats.totalGoals + antal mål av samma spelare redan i denna match`) innan `pickLegendCommentary` anropas, eller mjuka texten till att inte hävda en exakt siffra "nu" (t.ex. "Han har gjort {totalGoals} mål för klubben" utan "nu", vilket redan är sant vid den tidpunkten). |
| `matchHighlightService.buildMatchNarrative` | `src/domain/services/matchHighlightService.ts:18-33` | `buildMatchNarrative` | Säsongens höjdpunktsmatch berättad i efterhand: sen vinnare i minut N, derbyseger, cupdrama/final, slutspelsavgörande, eller stor seger — med resultat. | `w.category`, `w.ourScore`/`w.theirScore`, `w.lateGoalMinute`, `w.f.roundNumber` (cup: `>= 4` = final) | Sanning | — | Redan korrekt. Anropas bara från `selectMatchOfTheSeason` i samma fil, som bygger `ScoredFixture` direkt ur riktiga matchhändelser (`f.events.find(goal, minute>=85, clubId, margin>0)`) och riktiga slutresultat. `roundNumber >= 4` för cupfinal är INTE samma fel som "sortera på roundNumber" — cupens `roundNumber` är en kategorisk rundidentitet (1-4, satt en gång i `cupService.ts:55`), inte en global spelordning; `matchday` används separat för säsongskalendern. |
| `matchMoodService.getFinalWhistleSummary` | `src/domain/services/matchMoodService.ts:115-197` | `getFinalWhistleSummary` | Efterhandssammanfattning av matchen: dominans/kontrollerad seger/vändning/hattrick/kaptenen avgjorde, m.m. — baserat på en `FinalWhistleContext`. | `ctx.margin`, `ctx.comeback`, `ctx.captainScored`, `ctx.hatTrickPlayer`, `ctx.potmName`, `ctx.cornerGoals`, `ctx.suspensionsUs` m.fl. — samtliga tillhandahållna av anroparen | Delvis | ANNAT | Funktionen själv gör ingen felaktig härledning — den renderar bara det den får in. Men INGEN produktionskod bygger och skickar in en `FinalWhistleContext`: `grep` visar noll anrop till `getFinalWhistleSummary` utanför filen själv och tester (samma sak för syskonfunktionen `getMatchHeadline`, rad 199). Ingen yta i `src/presentation` renderar detta — kan vara text-utan-yta (§7 i CLAUDE.md) eller en superseterad tidig version av matchsammanfattningen. Kräver ägarbeslut, inte en fältfix: antingen wira in funktionen i en verklig post-match-yta med en korrekt ihopbyggd `ctx` (från `fixture.report` + `fixture.events`), eller dödmarkera filen/funktionen enligt §7 om `Granska`-skärmens egen logik redan gör samma jobb. |
