# HANDOVER 2026-05-25

## Vad som levererades idag

### Motor

- **v1.3.0** — `chemMultiplier()` i matchCore, kemi appliceras på managed lag (AI ×1,0), kalibrering 9,09 vs 9,12 ✅
- **v1.3.1** — snabbare kemuppbyggnad (`/6000` + tröskel `0.08`), brus-filter i multiplikatorn (`CHEM_SIGNIFICANCE = 0.15` — neutral elva → x1,0)
- **v1.4.0** — sharpness + moral kopplat: `playerModifier` i `squadEvaluator.ts`, form-drift `playerStateProcessor.ts`

### Bugfixar / playtest-fynd

- **C-SP5** — `FinalIntroScreen` alla 4 slides → `position: fixed; inset: 0; zIndex: 300` (var `height: 100%` → GameHeader syntes ovanpå). `postIntroFade` + fadeIn-animation vid matchstart.
- **phaseMarksSeen** — `persistAutosave` saknades direkt efter `set({ game: markedGame })` → fas-kort återkom vid omstart
- **Media titel/body** — journalist-inbox: rubrik=title, byline=body (var rubrik i båda fält)
- **Stale derby-storycard** — `inboxToPortal.ts` guard: `if (!nextMatchIsDerby(game)) return null`
- **clubMemory dedup** — identiska händelserader (typ|matchday|text-nyckel) sammanslås; grundtruppsdebut bara för `promotedFromAcademy`-spelare
- **Bandy-poäng** — "tre poäng" → "två poäng" i journalisttexter, pep-talk, derby-inbox
- **Journalist cup-filter** — `pickHeadline` filtrerar bort poäng-språk i cup-rubriker
- **opponentQuote-titel** — "efter derbyt" / "efter matchen" istf namnlös ikon

### C-FT1 — Trötthetens synlighet (del a)

- `src/presentation/utils/lagstyrka.ts` — `computeLagstyrka()`: evaluateSquad med riktig vs 100% fitness, returnerar idag/utvilat/gap
- Trötthetsring på `PlayerDot` (orange 40–69%, röd <40%)
- Lagstyrka-rad i `SlotLineupView` och `FormationView`
- Lagstyrka-kort i `StartStep`, trött-banner ersätter antal med konsekvens ("Laget ligger X styrkepoäng under utvilat")
- Fitness-varning i SlotLineupView: <60% (var <40%), text differentierar
- Simplify-audit efteråt fixade: `useMemo` på computeLagstyrka i FormationView (körde annars vid varje slot-klick), setTimeout-läcka med `useRef`/`useEffect`-cleanup

### C-SD1 — Säsongsslutets koreografi (delvis)

- `regular_done`-anslaget skrivet av Opus, committat i `leagueAnslag.ts`
- Anslag triggas via `computeNextAnslag` i `anslagService.ts`
- Summer-dödkod borttagen atomiskt: `inSummerScene` + `seasonDoneAck` från SaveGame, `'summer'` ur SeasonEndPhase + migration
- Sommarscenariot (vad som händer i sommer-perioden) är **inte** byggt — parkerat

### Data / process

- `seasonSummaryElimText.ts` — textpool för var säsongen tog slut (kf/sf/smf/no_playoff), Opus-skriven
- Stresstest-output uppdaterad (`season_stats.json` + `season_analysis.md`) med v1.3.1-motor
- R3/R3+ endgame-portal pixel-verifierad mot `2026-05-16_design_endgame_klimax.html` — inga avvikelser

## Commits

```
638abbf docs: C-FT1 spec, audit, playtest-fynd + live-vs-sim script
77b67b3 feat: seasonSummaryElimText + uppdaterad stresstest-kalibrering
dde91f3 feat: C-FT1 trötthetens synlighet — lagstyrka i elva/taktik/startvy + trötthetsring
c291379 feat: kemi motor v1.3.1 — snabbare uppbyggnad + brus-filter i multiplikatorn
9362369 fix: clubMemory dedup — identiska händelserader sammanslagna
79bd14b fix: inboxToPortal — stale derby-storycard filtreras mot nästa match
9eae7b6 fix: journalist — "två poäng", cup-filter, titel/body-format, opponentQuote
e656619 fix: bandyregel — "tre poäng" → "två poäng" i derby-inbox och pep-talk
3b8da26 fix: regular_done-anslag committat (Vercel-build-fix)
6596dc2 feat: regular_done-anslag + ta bort summer-dödkod atomiskt
c965bec fix: C-SP5 — FinalIntroScreen position:fixed + fadeIn
```

## Nyckelbeslut

- **`season_done` kvar, `summer` borta** — `season_done` gör hela jobbet; sommarscenariot behöver aldrig en separat fas
- **Lagstyrka är "ärlig"** — `computeLagstyrka` använder exakt samma `evaluateSquad`-formula som motorn, pinnar bara fitness → talet är sant per konstruktion
- **FormationView useMemo** — `computeLagstyrka` kräver memo (evaluateSquad ×2), starterIds och lineupSlots memoifierade i samma pass

## C-U1 (denna session, commit e681a05)

- `suspensionCause?: { sinceMatchday, opponentName, matches }` på Player
- Sätts i `playerStateProcessor.ts` vid matchstraff-läggning, nollställs vid frigång
- Inkorg: `createSuspensionItem` interpolerar `SUSPENSION_INCIDENT_LINES`
- Trupp/chip: `getSuspensionText` renderar `SUSPENSION_AVAILABILITY_LABELS` med motståndare
- `SquadScreen.tsx` båda callers uppdaterade att skicka `player.suspensionCause`
- `FormationView.tsx` timer-typ fixad (`number | null` — löste TS2322 Vercel-conflict)

## C-SD1 + C-FT1 utredning (denna session, ingen kodändring)

- **C-SD1 FULLSTÄNDIGT VERIFIERAT:** trigger/typ/display alla korrekt, inga kodändringar
- **C-FT1 (b) VERIFIERAT:** AI tröttnar också (fitness-loop kör på alla), asymmetrin gäller selektion (CA-only sort)
- **C-FT1 (c) VERIFIERAT:** 25,5pp = extremfall, normalt spel ~8–12pp, formeln rimlig
- **Del 4 (portal sparkline) REDAN BYGGT:** `SquadStatusMinimal.tsx` har sparkline sedan tidigare sprint

## Kvarstående frågor

- **C-FT1 AI-selektion** — `generateAiLineup` sorterar på CA enbart: feature eller bugg? Om bugg: fitness-vikt i sorteringen
- **C-SD1 sommarscenario** — vad ska spelaren göra/se under sommaren? Arkitektur öppen
- **`seasonSummaryElimText`** — pickas av `pickSeasonElimText()` men inget anrop finns i SeasonSummaryScreen (ny fil, ej kopplad)

## Föreslagen ordning nästa session

1. Koppla `pickSeasonElimText` till SeasonSummaryScreen (litet, oblockerat)
2. C-SY1#4 manager-kvitto: ~3h Code, text klar i `managerKvittoText.ts`, `managerChoiceLog` finns (B8)
3. Resterande-tickets (gold-tokens, SMFinalPrimary guld, SimSummary tokens, C-SP5 crossfade) — ~10h Code
4. Jacob beslutar om C-FT1 AI-selektionsasymmetri
5. Nästa stora: se BACKLOG
