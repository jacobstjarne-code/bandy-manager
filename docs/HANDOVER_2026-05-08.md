# Handover 2026-05-08

## Vad som levererades

### Playtest-fixes batch A–G

**A1/G1 — CTA ej klickbar i CoffeeRoomScene + JournalistRelationshipScene**
Rotorsak: `flex: 1` på content-div inuti `display:flex, flexDirection:column, minHeight:100vh`. CTA pushades under GameShell:s scroll-container (höjd = 100vh minus GameHeader + PhaseIndicator). Borttagen `flex: 1` → CTA hamnar direkt efter exchanges.

**A2 — EventCardInline val-knappar ojämna**
Rotorsak: Första knappen fick `btn-primary` (solid koppar), övriga fick transparent. Fix: `btn-outline` för alla knappar när choices > 1, `btn-primary` bara vid ensamt val.

**A3 — HalfTimeModal CTA saknade `btn-cta`**
Enkelt: `btn-primary` → `btn-cta btn-primary`.

**B1/F3 — Tabell-position visades under cupfas (NextMatchCard)**
Rotorsak: `anyMatchPlayed` inkluderade cup-fixtures. `anyLeagueMatchPlayed` filtrerar på `!f.isCup && !f.isKnockout`. Var tredje miss i B1-serien — fem filer fixades i föregående session, NextMatchCard missades.

**G2 — Journalist-rubriker upprepade sig konsekutiva omgångar**
Rotorsak: Hash-seed `fixtureId + bucket + persona` med pooler på 5 varianter → ~20% kollisionssannolikhet. Matchday lagt till i seed.

**G3 — Kafferum-preview visade samma text som full scen**
Rotorsak: Preview tog `exchanges[0][1]` — samma textkälla som scenens första replik. Fix: `meta.subtitle` som preview-källa.

**F1 — Beslutsekonomi throttling**
Ny `decisionBudgetService.ts`: max 2 aktiva decisions, säsong 1 omgång 1 max 1. Gater i roundProcessor, eventProcessor (med 2-omgångars cooldown), mediaProcessor (3-omgångars cooldown). Migration lade till `pendingDecisions`, `deferredDecisions`, `lastRumorRound`, `lastEventQueueRound`.

**E3 — Strings-pool-inventering**
Rapport i `docs/STRINGS_POOL_INVENTORY.md`. 7 filer skannade. Fynd: `traitSuspensions.hungrig` 1-variant (åtgärdat), `FINALDAG_BRIEFING_SPECTATOR` 1-variant (avsiktligt per Jacob's direktiv). Högrisk-pooler för expansion: squadNuStrings injury/morale/contract (3 varianter, besöks varje matchdag), matchCommentary goal-situationspooler.

**Bug — `traitSuspensions.hungrig`**
1 → 3 varianter i `matchCommentary.ts`.

## Commits denna session
Se `git log` — alla batchar committades och pushades till origin under sessionen.

## Aktiva jobb
Inga öppna jobb.

## Nyckelbeslut
- `FINALDAG_BRIEFING_SPECTATOR` med 1 variant är avsiktligt — Jacob bestämde detta explicit i koden. Flaggas inte som bugg.
- `traitSuspensions.hungrig` var däremot ett genuint förbiseende — ensam variant utan avsikt.

## Kvarstående frågor
- Strings-pool-expansion (squadNuStrings, matchCommentary goal-situationer) — inget prioriterat datum, se `STRINGS_POOL_INVENTORY.md` för full lista.

## Föreslagen ordning nästa session
1. Läs CLAUDE.md, LESSONS.md, DECISIONS.md, KVAR.md, denna fil.
2. Playtest — verifiera A1/G1 (CTA klickbar i kafferum), A2 (likformiga val-knappar), F1 (max 2 decisions i portal).
3. Fortsätt på KVAR.md-listan från 2026-05-06 — nästa prioritet är `QFSummaryScreen:38` (letterSpacing) och DIAGNOS F2 (emoji-prefix på labels).
