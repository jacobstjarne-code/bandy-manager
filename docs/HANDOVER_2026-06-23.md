# HANDOVER — 2026-06-23

## Vad levererades

**KORRVANDA2 — pre-match-ytor** (`fabcd0a4`, pushad till origin)

Sprint baserad på `docs/CODE_INSTRUKTION_KORRVANDA2_2026-06-23.md` och fynd i `docs/KORRVANDA-2-FYND-2026-06-23.md`.

| Steg | Vad |
|------|-----|
| H2 | `PoliticianInteractionLog` invite-bugg: runda 0 är falsy → fixat med `!== undefined`-check |
| H5 | Touch-blockerare på `ClubScreen`/`TransfersScreen` scroll-container |
| G2 | Marknad tom-state + guide-text (placeholder `// OPUS_COPY`) |
| B2 | TabBar `variant='segment'` — equal-width tabs med underline-indikator (`tabs.css`) |
| C3/B3/C2 | `TabIntro`-komponent + `tabIntros.ts`-datatabell med `// OPUS_COPY` för Klubb/Trupp/Transfers |
| E3 | FÖRBERED i fas-navet — KOD-VERIFIERAT REDAN BYGGT, ingen ändring |
| E1/E2 | `FacilityTree`: solid-border available, `--bg-elevated` lyft, locked opacity 0.35, `🔒 Träningshall`-label, `facilityDescriptions.ts` (all `// OPUS_COPY`) |
| A1 | `CoachMarks` dim `rgba(0,0,0,0.65)` → `0.55` |
| A2 | `cursor:'default'` på CoachMarks overlay |
| C1 | AkademiTab upgrade-knapp `whiteSpace:'nowrap'` |
| D1 | `CoachMarks` monterat i `PortalScreen` vid `!game.coachMarksSeen` — var byggt men aldrig renderat |
| H1 | `OrtenTab scrollToSection`-prop + supporter-sektion (klack/veteran/ungdom/familj) + `KlackenSecondary` navigerar med `section:'klack'` |

**Incoming-triage** (samma session):
- `Sprint 1 - pre-match-ytor.html` → `docs/mockups/`
- `KORRVANDA-2-FYND-2026-06-23.md` → `docs/`
- `.DS_Store` borttagen

## Vad som inte gjordes

- **G1·G3** (Värvning-densitet + spelarrad-typografi) — EJ gjord. Var i STEG 2-scope men hoppades.
- Textfyllning i `tabIntros.ts`, `facilityDescriptions.ts`, G2-guide — allt Opus-lane med `// OPUS_COPY`

## Opus-copy-skuld

**`tabIntros.ts` och `facilityDescriptions.ts` är FYLLDA** — Opus skrev alla 14 flikar + 10 noder + introrad samma session, efter `fabcd0a4`. De är ocommittade i working-tree (KORRVANDA3 steg 1 committar dem). Inga `// OPUS_COPY`-blockare kvar i de filerna.

| Fil | Status |
|-----|--------|
| `src/domain/data/tabIntros.ts` | ✅ Klar — 14 flikar ifyllda, ocommittad |
| `src/domain/data/facilityDescriptions.ts` | ✅ Klar — 10 noder + FACILITY_INTRO ifyllda, ocommittad |
| G2 guide-text i `TransfersScreen.tsx` | ✅ Klar (inline i KORRVANDA3-spec) |
| Nemesis/legend-beats i `portalBeats.ts` | ✅ Klar — fylldes i körordningen innan KORRVANDA2 |

## Nyckelbeslut denna session

- `CoachMarks` var byggt sedan tidigare men aldrig mountat någonstans — wired till `PortalScreen`
- `OrtenTab scrollToSection` djuplänkar från `KlackenSecondary` med `section:'klack'`
- E3 (FÖRBERED) konstaterades som REDAN KLAR — `PhaseIndicatorAuto`+`getCurrentPhase` hanterar det

## Föreslagen ordning nästa session

1. **KORRVANDA3** — körorder i `docs/CODE_INSTRUKTION_KORRVANDA3_2026-06-23.md`; steg 1 = committa working-tree (`tabIntros.ts` + `facilityDescriptions.ts` + ev. G2-text), sedan resten av KORRVANDA3-punkterna
2. **G1·G3** (Värvning spelarrad-densitet) — hoppades i KORRVANDA2, kollar om det är del av KORRVANDA3 annars eget pass
3. **#2 Tränarens berättelse GAP 1–3** — Code byggde strukturen (`19a91469`), Opus skriver copy till narrativeLog-skrivställena + avskeds-ceremonin
4. **Anniversary-balansbeslut** — Jacob avgör om 1-årsgaten höjs 30→85
5. **KF4 playtest** — Jacob besöker ArrivalScene/BoardMeetingScene, bekräftar namn+pronomen rätt

## Commits denna session

```
fabcd0a4 feat: KORRVANDA2 — pre-match-ytor full sprint
2d853d42 fix: M5 already done by Opus, M6 remove cup_final_pre tautology
d59ee380 feat: M7/M8/M9 — penalty shootout feed rows + unified conversion formula
03622724 feat: M12/M15 — inbox hierarchy + Dina val outcome rows
8c0a8b80 fix: M10/M11 — cup win keeps BottomNav + standardize anslag backdrop
a4653aab chore: incoming triage — KORRVANDA3 mocks to mockups/, spec to docs/
```
(Tidigare denna dag: `19a91469` killer-apps GAP 1–3, `40aac280` killer-apps stam, `ae2a6460` legibel konsekvens m.fl.)

---

## KORRVANDA 3 — Match-flödet (tillägg)

Sprint baserad på `docs/CODE_INSTRUKTION_KORRVANDA3_2026-06-23.md`.

| Steg | Punkt | Vad |
|------|-------|-----|
| 0 | M1/M3 | MatchLaddningScene CTA: `calc(--bottom-nav-height + --safe-bottom + 12px)` (3 instanser) |
| 0 | M2 | `nav a:focus-visible` → accent-ring i global.css |
| 0 | HT | HalfTime modal `max-height` tokeniserad (`--bottom-nav-height`) |
| 1 | M5 | Redan klar (Opus) — `⬩ Cuphelgen ⬩` i cupAnslag.ts |
| 1 | M6 | Tautologi borttagen: cup_final_pre body börjar ej längre med "Cupfinal." |
| 2 | M7 | Penalty shootout-rader i CommentaryFeedStalvallen: "Omg. 1: Lindqvist ✅ · Svensson ❌ — 1–0" |
| 2 | M8 | GranskaShotmap: `scoredCount/onTargetCount` → `scoredCount/totalShots`, etikett "Målkonvertering" |
| 2 | M9 | Verifierat: penalty-fas matar ej skottstatistiken (matchCore passerar oförändrat) |
| 3 | M12 | InboxScreen: `InboxThinRow` + `InboxGroupRow` + `groupNyheter()` — Nyheter nu tunna rader + groupering |
| 3 | M15 | GranskaOversikt "Dina val": outcome rows med stripe/heading/spelare/utfall/siffra |
| 4 | M10 | GameShell: `cup_final_victory` undantagen från `sceneActive` → nav visas på cupvinst |
| 4 | M11 | `--anslag-backdrop: rgba(14,13,11,0.75)` token — `.anslag-overlay` använder token |
| — | Triage | Mocks → `docs/mockups/`, spec → `docs/`, `_RADERAS/` borttagen, `.DS_Store` borttagen |

**Godkänt utan åtgärd (Opus-ruling):** M4/M13/M14/M-straff.

## Playtest behövs (Jacob)

- M7: cuppmatch → straff → se varje runda i matchflödet
- M10: cupfinalseger → kontrollera att nav visas
- M12: inbox med ≥2 nemesis/media/identiska titlar → bekräfta gruppering
- M15: Granska efter match med kapten + trötta spelare → outcome rows

## Föreslagen ordning nästa session

1. Sessionsstart (tid, workspace-check, BACKLOG, incoming/)
2. Playtest KORRVANDA 3 (M7/M10/M12/M15 ovan)
3. G1·G3 Värvning-densitet (hoppades i KORRVANDA2)
4. Systempatch B2 blur-fix: `.anslag-overlay backdrop-filter: blur(8px)` tas bort
5. Nästa sprint ur backlog (Opus beslutar ordning)
