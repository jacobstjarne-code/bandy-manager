# HANDOVER 2026-05-24

## Vad levererades

### Trupp-redesign Fas 1+2 (commit `a001ac3` + `496ea7d`)
- **Fas 1 — PlayerRow-arketyp:** stripe-klass per position, sparkline-primitiv (6 ratingsrader), chip-pills (skada/vila/C-band/kontraktsvarning), captain-band 16×16px, storyline-rad.
- **Fas 2 — detaljer:** port mot vänster 40px, chip-pills korrekt radavstånd, captain hoist (captain alltid överst i grupp).
- Audit-fix i separat commit: Sparkline-primitiv extrakt till komponent, chip-pills layout-fix.

### Sprint 2026-05-24 — 8 uppgifter levererade (commits `7ae0b1c` + `2f6ecf7`)

**1A — Matchresultat-titel med motståndare:**
`createMatchResultItem` i `inboxService.ts` tar nu `clubs: Club[]` och bygger `"Matchresultat: HomeShort–AwayShort score–score"` (cup-variant: `"Cupen: ..."`). `roundProcessor.ts:411` skickar `game.clubs`. Fem testanrop i `inboxService.test.ts` uppdaterade med `TEST_CLUBS`.

**1B — Tränings-titel med omgång:**
`` `Träning omg ${roundNumber}: ${typeLabel}` ``

**1C — Media-byline:**
`buildByline(journalist, suppressName?)` helper i `mediaService.ts`. Journalist-namn suffixas ` — Name, Outlet` på titel istället för `getHeadlinePrefix`-prefix. `generateAbsurdityArticles` använder nu `buildByline`. `journalistService.ts`-titeln likaså.

**2 — Bud i VIKTIGT (approach a):**
`getCategory` i `InboxScreen.tsx` läser live `game.transferBids.some(b => b.playerId === item.relatedPlayerId && b.direction === 'incoming' && b.status === 'pending')`. Pendande bud → `'important'`, annars `'news'`. Inga stale inbox-mutationer.

**3A — cup_day-bias:**
`CHARACTER_BIAS.cup_day` tillägger `event_critical: 0.5, patron_demand_unmet: 0.5` i `portalBuilder.ts`.

**3B — event-priority:** Ingen ändring — communityEvent `'low'` är korrekt; 3A löser cup-helgsvikten.

**4B — Kommentar-pooler utökade:**
`supporter_goal_home` 4→8, `weather_miss_thaw` 3→5, `weather_miss_fog` 2→4, `weather_goal_heavySnow` 2→4, `weather_goal_thaw` 2→4 i `matchCommentary.ts`.

**4A — pickCommentary-minne:**
```typescript
export function pickCommentary(
  arr: CommentaryTemplate[],
  rng: () => number,
  history: Map<CommentaryTemplate[], CommentaryTemplate[]>,
): CommentaryTemplate
```
Avvisa-och-dra-igen: N = min(poolSize−1, 3). `Map<pool[], template[]>` skapad EN gång i `matchCore.ts` (per matchsimulering), skickas som param genom `matchUtils.ts` och alla ~82 anrop i `matchCore.ts`. Deterministiskt — samma seed → samma match.

### Simplify-audit (commit `bebb8a6`)
`game.transferBids.some(` — `?? []` borttaget (transferBids är icke-optional på SaveGame).

### Manager-anteckning Tier 1A (commit `da071ff`)
- `Player.ts`: `managerNote?: string  // fri text, max 80 tecken, satt av spelaren`
- `SquadScreen.tsx`: `{player.managerNote && (<div ...>✎ {player.managerNote}</div>)}` under storyline-raden. Italic Georgia, fontSize 11, paddingLeft 50. Krokhaken `{/* VÄNTAR PÅ ny datamodell */}` aktiverad.

### Grep-rapport Tier 2 (inga byggen — rapport till Opus):
- **fatigueHistory:** `number[]` platt — bara aggregerat konditionsmått. Full squad-pulse kräver ny `teamFitnessHistory: { avgFitness, avgMorale, injuryCount }[]`.
- **ActiveAnniversary.subjectPlayerId:** Ren render-data — spelas upp mot `game.players.find`. Ingen ny datamodell behövs för anniversary-eko-chipet.
- **narrativeLog Klack-typer:** Klack-systemet (`klackPresenter.ts`) skriver INTE till `narrativeLog` — det håller eget tillstånd i `game.klackEcho`. Klack-favorit-chip kräver ny `'klack'`-typ i narrativeLog + Klack-systemändring.

## Nyckelbeslut
- **Media-byline:** journalist-namn bor i titel som suffix `" — Name, Outlet"`. `getHeadlinePrefix` används INTE i `mediaService`/`inboxService`. Behålls bara i `journalistService` för presskonferens-headings.
- **Bud approach (a):** läs live `game.transferBids` vid render. Fyra resolve-punkter slipper skriva status bakåt till inbox — ett enda läs-ställe kan inte bli stale.
- **pickCommentary-deterministisk Map:** kö-state bor i Map<pool, seen[]> per matchsimulering — inte per pool globalt (det vore session-state). Samma seed → identisk match.

## Kvarstående
- **Manager-anteckning edit-yta** — design-beslutad till inline textarea i spelarmodalen. Byggs när PlayerModal-spec finns (modal inte byggd än).
- **Anniversary-eko** (Tier 2B i spec) — ren render, byggt, väntar bara på att `activeAnniversaries` skickas till trupp-vyn. Litet pass.
- **Squad-pulse-sparkline** (Tier 2C) — väntar scope-val (Jacob): enkel kondition-sparkline (fatigueHistory, ~30 min) eller full (ny teamFitnessHistory, ~2-3h). Opus rekommenderar full eller vänta.
- **Klack-favorit-chip** (Tier 1B i spec) — kräver Klack-systemändring (ny `'klack'`-typ i narrativeLog). Prioriteras med Klack-narrativ, inte isolerat.
- **P5 i BACKLOG** — rotorsak tomma commentary-events. Enda kvarv CODE_SAMLAT-paketet.

## Föreslagen ordning nästa session
1. Jacob scope-val: squad-pulse enkel vs full (påverkar BACKLOG).
2. P5 — tomma commentary-events, utred matchSimulator/matchEngine.
3. Anniversary-eko (litet, ren mekanik, redo att aktivera).
4. Playtest: manager-anteckning, inbox-bud-kategori, media-byline.
