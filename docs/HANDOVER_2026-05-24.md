# HANDOVER 2026-05-24

## Vad levererades

### Portal-kurering DEL 1–4 (commits `1e1e05b` → `9884e05`)
- **`inboxToPortal.ts`** — ny fil: 7 inbox-kinds (bigResult/scandal/playerMilestone/derbyRamning/nemesis/journalistHot/mecenat). BoardFeedback-grenen kräver titel-prefix-match FÖRST (annars null). `makeInboxStoryComponent` renderar story-slot med per-stripe gradient, border-left, korrekt typografi.
- **`portalBuilder.ts`** — story-slot (FREKVENTA ×0.5 rotation, SALLSYNTA +25 golv, recencyBonus +10/+5, roundsAgo ≤ 2 filter); CHARACTER_BIAS-tabell; staleBias floor 0.05.
- **`roundCharacter.ts`** — ny fil: `getRoundCharacter()` med 7 karaktärer. Prioritetsordning: cup_day > pre_derby > premiere (0 completed) > losing_streak ≥3 > winning_streak ≥3 > post_loss > standard. Använder exporterad `getNextManagedFixture` (playoff-elimination-aware).
- **`initCardBag.ts`** — viktsänkningar: board_objectives 87→65, tabell 30→20, ekonomi 25→18.
- **`PortalScreen.tsx`** — renderar `StorySlotComponent`, skickar `kind` till `recordPortalShown`.

### Pixel-audit — 19 avvikelser mot mockup åtgärdade
Story-slot CSS pixel-matchad: border-left på card-elementet (inte inner div), per-stripe gradientbakgrund, STRIPE_LABEL_COLOR (warm→`--warm-light`, cold→`--cold-light`, danger→`#E8A090`), padding 12px 14px, fontSize 14.5/11.5/8, lineHeight/gap/shadow.

### Render-loop fix (commit `ae90f13`)
**Rotorsak:** `buildPortal` läste `lastStorySlotType`; `recordPortalShown` skrev samma fält → loop vid 2 FREKVENTA-kandidater.
**Fix:** `currentStorySlotType` (skrivs av render) / `lastStorySlotType` (läses av buildPortal, fryst under matchdagen). `roundProcessor` promotar current→last EN gång vid matchdagsövergång.

### Layer-fix (commit `9884e05`)
`initCardBag.ts` + `inboxToPortal.ts` importerar nu `CardRenderProps` från `dashboardCardBag.ts` (domain-lager) i stället för `presentation/components/portal/portalTypes.ts`. Dubbeldefinitionen kvarstår — `portalTypes.ts` behöver inte ändras (presentation-komponenterna importerar rätt).

### Opus-text (commit `eaaa0ff`)
- Annandagen: 3 mediarubriker (val B/C/D) + 4 val-beskrivningar + intro-text i `AnnandagsValEvent.tsx`
- Kafferum: +26 GENERIC_EXCHANGES, +4 FATIGUE_WARM, +4 FATIGUE_HOT — alla `[Opus]`-platshållare borta
- Alla `grep -rn "\[Opus\]" src/` returnerar 0 träffar i körbar kod

## Nyckelbeslut
- `CardRenderProps` lever kvar i BÅDA `dashboardCardBag.ts` och `portalTypes.ts` — presentationskomponenterna importerar från `portalTypes`, domänkod importerar från `dashboardCardBag`. Ingen merge av de två definitionerna gjordes (identisk struktur, riskfri duplikation, för liten vinst att städa nu).
- Lärdom 36 tillagd i LESSONS.md: render-loop via samma fält läst av buildPortal + skrivet av render-effekt.

## Verifiering
- Build ren, 0 TypeScript-fel
- Render-loop contract verifierad av arkitektur-audit: airtight
- `grep -rn "\[Opus\]" src/` → 1 träff (kommentar i `AnnandagsValEvent.tsx:13`, ej körbar kod)

## Kvarstående frågor
- **Jacob playtest:** story-slot synlig i portal, rotationslogik (omg N journalist → N+1 bigResult vinner), round-character i kontext (inga banners, bara vikt-instrument)
- **P5 i BACKLOG:** rotorsak tomma commentary-events — CODE_SAMLAT sista paketet, ej påbörjat

## Föreslagen ordning nästa session
1. Jacob playtest: öppna portalen efter en omgång med stor inbox (derby + media). Verifiera att story-slot renderas, att round-character INTE syns som text, att vikterna inte tränger ut tabell/ekonomi.
2. P5 — utred tomma commentary-events (rotorsak i matchSimulator/matchEngine, fixa vid källan).
3. Säkerhetscheck: `currentStorySlotType` nollställs inte efter promotion i roundProcessor — harmlöst i nuläget (skrivs över vid första render av ny matchdag), men värt att notera om story-slot beter sig oväntat i edge-case (ny säsong utan inbox-items).
