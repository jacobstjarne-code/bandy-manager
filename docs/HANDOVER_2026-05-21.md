# Handover 2026-05-21 — B11 Kalender-refaktor

## Vad som levererades

### B11 T1 — seasonCalendar på SaveGame, byggd från fasta ankare
- `seasonCalendar?: MatchdaySlot[]` tillagd på SaveGame
- `isLandslagsuppehall?: boolean` tillagd på MatchdaySlot (markerar R7)
- `date?: string` och `tipoffHour?: number` tillagda på Fixture
- ROUND_WINDOWS räknas nu från Nov 1 (inte Oct 8) som bas
- Liga startar nu i November (R1 = första fredagen/helgen i november)
- Cup koncentrerad i oktober: R1/R2 = andra/tredje lördagen, R3/R4 = sista helgen (finalhelgen)
- Landslagsuppehåll-paus: gap ~10 dagar mellan R6 (offset 26-34) och R7 (offset 38-46)

### B11 T2 — Fixtures stämplade med datum+tid vid generering
- `createNewGame.ts` och `seasonEndProcessor.ts` stämplar alla liga-fixtures med `date` + `tipoffHour` från seasonCalendar-slot
- `seasonCalendar` lagras på SaveGame i båda flödena

### B11 T3 — On-demand buildSeasonCalendar-anrop rivna ut
- `roundProcessor.ts` (2 anrop) → `game.seasonCalendar`
- `matchSimProcessor.ts` → `game.seasonCalendar`
- `dailyBriefingService.ts` → `game.seasonCalendar`
- `MatchLiveScreen.tsx` (gammal) → `game.seasonCalendar`
- `match/MatchLiveScreen.tsx` → `game.seasonCalendar`
- `match/useMatchGenerator.ts` → `game.seasonCalendar`
- `gameStore.ts`: kvar som legacy matchday-migration (kommenterad som ONE-TIME)
- `getRoundDate` bevarad som fallback i roundProcessor för playoff-rundor (matchday 27+)

### B11 T4 — seasonalTone tidsbas enats med kalender
- `seasonalTone.ts`: dayOfSeason räknas nu från Nov 1 (inte Sep 1)
- Keyframe-ankare uppdaterade: day 0=Nov start, 55=Annandagen, 90=djupvinter, 130=slutspelsskärpa

### B11 T5 — Cup oktober + ett landslagsuppehåll
- Cup oktober: alla 4 rundor i oktober (ingår i T1)
- Landslagsuppehåll: R7-slot markerad med `isLandslagsuppehall: true`

### B11 T6 — Migration av gamla saves
- Version bump: `0.2.0 → 0.3.0`
- Gamla saves (saknar seasonCalendar): byggs en gång vid laddning från currentSeason
- Fixtures utan `date`/`tipoffHour`: stämplas från calendar-slots
- Completed fixtures: befintliga datum bevaras, saknade stämplas

### B11 T7 — Test-suite låser tidslinjen
- 21 nya tester i `src/domain/services/__tests__/calendarTimeline.test.ts`
- Täcker: determinism, ankare (annandagen/november/oktober-cup/finalhelgen), speldagar, single source, migration

## Commits
- `ee872a3` T1 — seasonCalendar + november start + cup oktober
- `19d7c0c` T2 — stamp fixtures vid generering
- `596d008` T3 — riv on-demand anrop
- `954ec4d` T4 — seasonalTone november-bas
- `754cb8e` T5+T6 — cup oktober + migration
- `231f02f` T7 — timeline test suite

## Testresultat
- Baseline: 979 gröna (1 pre-existing failure i seasonSignatureService.test.ts)
- Nu: **1000 gröna** (+21 nya tester), samma pre-existing failure

## Öppna frågor

### Spelaruttagning (landslagsmekanism)
Spec: "player selection mechanic (truppfrånvaro) är en separat feature". Ankaret finns nu i kalendern (`isLandslagsuppehall` på R7). Mekaniken är inte implementerad — går i BACKLOG.

### seasonalTone → design tokens
Spec flaggar: "seasonalTone har idag egna hex-värden. Om tonen ska bli riktiga design tokens är det en token-arkitekturfråga för Design (lördag)." Inte rört här — tidsbas enats, token-integrationen är separat.

### T7 inbox/event-symptom-dokumentation
Spec §8 sista acceptanskriterium: "T7 dokumenterar vilka inbox/event-symptom som försvann". Det kräver playtest — inte kan verifieras kod-mässigt. Bör checkas av Jacob i nästa playtest.

### Playlist-ordning: cup-fixture-stämpling
Cup-fixtures stämplas i migration men `generateCupFixtures` stampar inte `date`/`tipoffHour` vid skapande (det görs i `seasonEndProcessor` via `newCupFixtures` som inte slås upp i calendar). Kan berika cup-fixtures med datum i en follow-up.

## Kvarstående
- Playtest av calendar-ändringen: spelar matchen fortfarande korrekt? Är datumserierna rimliga?
- Verifiera att nyår/trettondag-triggers fortfarande funkar med november-bas
- BACKLOG: Landslagsuttagningsmekanism
- BACKLOG: seasonalTone → design tokens (Design-session)
