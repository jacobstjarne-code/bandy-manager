# HANDOVER — Bandy Manager
## 2026-05-06

---

## VAR VI ÄR JUST NU

Sessionen 2026-05-05/06 var en ren design-rensning. Inga nya features. Fyra passningar av befintlig kod mot designsystemet, plus ett revertat experiment med stripes.

---

## LEVERERAT DESSA SESSIONER (🔄 KOD KLAR — ej playtestat)

### Steg 2B — Inline CTA → `.btn.btn-primary` (11 filer)
Alla inline `linear-gradient(135deg, var(--accent-dark)...)`-knappar ersatta med `.btn.btn-primary`. Ceremonial-knappar (RoundSummary, HalfTimeSummary, QFSummary, PlayoffIntro) behåller `letterSpacing: '2px'` + `textTransform: 'uppercase'` inline som överskrivning. Standard-knappar (CommentaryFeed, portal primaries, ClubExpandedCard, IntroSequence) är rena.

Audit: `docs/sprints/SPRINT_CTA_BYTEN_AUDIT.md`

### Steg 2C — Inline borderRadius på data-kort → `.card-sharp` (4 kort)
- `SquadScreen.tsx:498` — spelarslotar i stats-sektion
- `SquadScreen.tsx:559` — låneavtals-lista
- `GameOverScreen.tsx:127` — "Din karriär"-statistikkort (byt `bg-elevated` → `bg-surface`, intentionell warm-tint)
- `ChampionScreen.tsx:161` — "SLUTSPELSRESA"-kort

Kategori 2–5 (narrativa, chips, modals, color-tinted) bevarade.

Audit: `docs/sprints/SPRINT_CARD_BYTEN_AUDIT.md`

### Steg 2D — Section-label normalisering → `.h-label` (12 ändringar, 7 filer)
`.h-label` = 8px / 600 / 2px letter-spacing / uppercase / `var(--text-muted)`.

5 inline-fixar (spans/tr), 7 `<p>` → `.h-label`. Skärm-rubriker med dramatisk effekt bevarade (SeasonSummaryScreen "ÅRSBOK" 11px/3px, GameOverScreen "Spelets slut" 11px/3px/danger).

Audit: `docs/sprints/SPRINT_LABEL_NORMALIZATION_AUDIT.md`

### HANDOFF #9 — ArrivalScene (ny fil)
`src/presentation/screens/ArrivalScene.tsx` — kontinuerlig intro-scen, 4 steg. Ingen route-byte. `background: var(--bg-scene)` och genre-etikett "⬩ Ankomsten ⬩" persistent. CTA (`scene-cta`, separat CSS) döljs tills 3.4s gått.

Routing: `/intro` lagt till i AppRouter. ClubSelectionScreen navigerar till `/intro` (inte direkt till dashboard).

`arrivalDialogue.ts`: `getStureLine(clubId)` extraherat som ny datafil — ersätter inline `STURE_VARIANTS`. Sture-texterna är fortfarande placeholder, märkta `TODO(Opus)`.

Audit: `docs/sprints/SPRINT_HANDOFF_9_AUDIT.md`

### Steg 3 B2 (Mock 1) — implementerat och reverterat samma dag
Implementerades: `.card-tap`-klass, 5 secondary cards fick hover-effekt, 6 stripes borttagna (Inbox, Tabell, RoundSummary, GranskaForlop, TransferPlayerCard, ActiveBidsList), kompletterande signaler (tint, tag-copper, 🔥) adderade.

Reverterades: Beslutet vändes 2026-05-06 — stripes och klammer är genomgående visuellt språk. Alla 6 stripes återställda. Kompletterande signaler behålls (dubbel-signal = stripe + indikator). `.card-tap` och typografi-fixar behålls.

Audit: `docs/sprints/SPRINT_B2_STRIPES_AUDIT.md` + `docs/sprints/SPRINT_B2_REVERT_AUDIT.md`

### Designsystem inläst i repo
`design-system/` kopierat till rotnivå (76 filer). Ingångspunkt: `CODE-OPUS-INSTRUCTION.md`. `docs/DESIGN_SYSTEM.md` är stub. CLAUDE.md uppdaterat.

---

## KVARSTÅENDE FRÅN DESSA SESSIONER

| # | Vad | Fil/Plats | Prioritet |
|---|-----|-----------|-----------|
| K1 | Sture per-klubb-repliker | `src/domain/data/arrivalDialogue.ts` | Opus-jobb |
| K2 | `QFSummaryScreen:38` — `letterSpacing: '3px'` → `'2px'` | `QFSummaryScreen.tsx` | Liten Code-fix |
| K3 | DIAGNOS F2 — emoji-prefix saknas på vissa labels | Opus text-pass | Opus-jobb |
| K4 | `HalfTimeSummaryScreen` + `RoundSummaryScreen` labels — oklassificerade | Oklart scope | Nästa design-pass |
| K5 | `GameOverScreen:158`, `ChampionScreen:209` — knappar utan `.btn`-klass | Väntar btn-audit | Nästa design-pass |
| K6 | `Scoreboard.tsx:145` `#A89878` | Hårdkodat hex | Nästa hex-tokens-pass |
| K7 | Mock 2-pass (14 stripe-fall som är utanför B2 scope) | Se KVAR.md § Mock 2-pass | Separat pass |

---

## VIKTIGA DESIGNBESLUT FRÅN SESSIONEN

**Stripes är genomgående visuellt språk** (formaliserat 2026-05-06). Kortversion: `borderLeft`-stripes tas inte bort utan explicit beslut per komponent. Dual-signal (stripe + tint/tag/emoji) är nu dokumenterat mönster. Se `design-system/DESIGN-DECISIONS.md § Stripes och klammer`.

**`.card-tap`** på alla klickbara kort utan chevron — brightness hover/active.

**Ceremonial vs standard CTA:** post-match-skärmar behåller `letterSpacing: '2px'` + `textTransform: 'uppercase'` som inline-överskrivning tills HANDOFF #4 (CeremonialCta) levereras.

---

## STATUS PENDING PLAYTEST

Alla ovanstående ändringar är 🔄 KOD KLAR. Ingenting har playtestats av Jacob. Prioritetsordning för playtest:

1. **ArrivalScene** — helt ny vy, mest att verifiera
2. **Steg 3 revert** — dubbel-signal på InboxScreen, Tabell, Transfer, GranskaForlop
3. **Steg 2B/C/D** — semantiska byten, lägst risk

---

## PÅGÅENDE ARBETE FRÅN FÖREGÅENDE SESSIONER (oförändrat)

Dessa är fortfarande öppna och ej adresserade i denna session:

**SPEC_GRANSKA_OMARBETNING — Fas 2 (🔄 KOD KLAR från 2026-05-04):**
- Kärnan pushad: `granskaEventClassifier.ts` korrekt, `ReaktionerKort` byggt, `getCriticalEventsForGranska(...).slice(0, 3)`.
- `EventCardInline`-pill-style reverterad (✅ fixad 2026-05-05 av Opus direkt).
- `granskaEventClassifier.ts` returnar nu `'reactions'` endast när `event.choices.length === 0` (✅ fixad 2026-05-05).
- ⚠️ Awaiting browser-playtest: alla Granska-ändringar.

**SPEC_BESLUTSEKONOMI_STEG_4** — spec-klar, ej påbörjad. Påbörjas EFTER Granska Fas 2 verifieras och Jacob bestämmer prioritet.

**EventCardInline-textpooler** — levererade i `TEXT_REVIEW_eventcardinline_2026-05-03.md`. Kod-integration utestår för `starPerformance`, `playerPraise`, `captainSpeech`.

---

## TEKNISK SKULD (ej adresserad)

Oförändrad sedan 2026-05-04. Se KVAR.md § TEKNISK SKULD för TS-4 till TS-9. Mest akuta: TS-4 (dead code DashboardScreen.tsx, ~80 KB).

---

## COMMITS DESSA SESSIONER

| Commit | Innehåll |
|--------|----------|
| `9365546` | Steg 2B/C/D + HANDOFF #9 + B2-stripes + HANDOFF #5-bugfix (Opus-batch) |
| `be4954c` | Steg 3-revert — stripes återställda |
| `2e74e4b` | ArrivalScene + arrivalDialogue.ts refactor |
| `0d87621` | design-system/ inläst + docs 2026-05-04/05/06 |
| `826a177` | season_stats.json regenerad + season_analysis upd |
